[{"id":"github:redwood-sdk-go:642358","app":"github","title":"credential lease batcher and typed stream-sink ergonomics (Go SDK)","preview":"Context and motivation: Customers running long-lived streaming clients observed token churn when multiple concurrent streams re-auth at similar windows, and SDK ergonomics for stream sinks in Go lagged behind the typed async-iterable ergonomics in other SDKs. This PR implements a credential lease batcher to reduce reauth storming, introduces a typed StreamSink interface to improve ergonomics and type-safety, and adds an adaptive retry-weight profile specifically for streaming paths. Summary of changes and motivation: 1) credential/lease: new reauth.BatchScheduler and lease.Pool interfaces (concrete implementation BatchLeaser) that coalesce near-simultaneous credential refreshes into a single token fetch per host/region window, lowering QPS to auth backends. 2) streaming: add typed stream Sink and SinkAdapter to replace less-idiomatic callback sinks while providing a compatibility shim. The new Sink exposes Push(ctx, envelope) error and Close(ctx) error with context deadlines for graceful shutdowns. 3) retry: add RetryProfileStream which uses jitter-aware backoff weights and latency-aware retry ceilings (used by streaming RPCs). 4) telemetry: hooks for cred lease metrics and per-sink backpressure/backoff telemetry. 5) version bump: Go SDK to v1.9.0, update changelog and examples. Commits (high-level): - add batcher and lease pool with unit tests - introduce typed StreamSink, adapters and examples - implement RetryProfileStream and wire into streaming client - telemetry hooks and prometheus labels - docs and migration notes - unit + integration tests and bench harness Changes by area (files, high-level): - credential/lease/batcher.go added core batcher implementation - credential/lease/pool.go pool API and tests - streaming/sink.go new typed interface and adapter shim - streaming/sink_adapter_compat.go backward compatibility shim for callback codepaths - retry/profiles_stream.go new stream retry profile - examples/streaming/typed_sink_example.go example and migration guide - telemetry/metrics.go add cred lease metrics and labels - CHANGES.md and VERSION file bump - tests/integration/stream_lease_bench_test.go bench and CI integration notes Performance and benchmarks: synthetic auth-heavy integration bench ran on a 1000-concurrent-streams workload. Observed results: median token fetches to auth service reduced by ~78% under aligned refresh windows; end-to-end mean latency for stream connect+first-event improved 6-12% depending on region; per-worker CPU usage decreased ~18% in auth hotspots because fewer goroutines blocked on repeated reauth. Safety and compatibility: The typed Sink interface is additive. To prevent breaking callers we included SinkAdapter that wraps the legacy callback-based subscription sinks. We changed Stream.Close signature to return error (previously void) to allow callers to observe drain failures; a compatibility adapter retains the old behavior for callers that ignore the returned error. Testing: Added unit tests covering batch coalescing edge cases, race detector runs for lease pool, and integration bench that exercises reauth storms. CI: initial pipeline showed a flaky integration test in the bench harness (TestStreamLease_Race) failing under Go race detector on the first run. Fix committed to use a tighter sync fence and re-run. Final CI: all checks passed including linters, unit tests, integration bench (non-flaky on rerun), and benchmarks recorded as artifacts. Review conversation summary: - Amelia Ruiz requested clearer migration notes and an explicit adapter example for the legacy callback API. Author responded with an example in examples/streaming/typed_sink_example.go and updated CHANGES.md. - Jonah Kim asked for metrics labels to include \"cred_scope\" and a clarified histogram for reauth latency; author added labels and a synthetic histogram in telemetry/metrics.go. - Sofia Alvarez flagged potential deadlock in batcher shutdown; author added explicit shutdown sequence and added a deterministic unit test that reproduced and then fixed the issue. All reviewers approved after the follow-ups. Merge and release details: Merged via squash-and-merge into main on 2026-03-05. Release notes updated and an internal rollout plan scheduled for Dedicated clusters (ENG-2148) with a feature flag for batcher enabled off by default. Migration guidance: - Users can switch to the typed StreamSink for clearer error handling and context-aware Close. - If using legacy callback sinks, no immediate action required thanks to SinkAdapter, but switching recommended for observability and structured shutdowns. - To enable credential batching cluster-side, flip feature flag \"cred.batch.enabled\"; rollout staged in Dedicated and Private deployments. Related work: coordinated with SDK teams (PY/TS) to align retry profile names and metrics; a follow-up task ENG-2199 will add the same batcher shim to Python and TypeScript SDKs. Checklist: [x] design doc reviewed internally [x] unit tests added [x] integration bench added [x] examples and migration notes [x] reviewers approved [x] CI passed [x] updated release notes","text":"Context and motivation: Customers running long-lived streaming clients observed token churn when multiple concurrent streams re-auth at similar windows, and SDK ergonomics for stream sinks in Go lagged behind the typed async-iterable ergonomics in other SDKs. This PR implements a credential lease batcher to reduce reauth storming, introduces a typed StreamSink interface to improve ergonomics and type-safety, and adds an adaptive retry-weight profile specifically for streaming paths. Summary of changes and motivation: 1) credential/lease: new reauth.BatchScheduler and lease.Pool interfaces (concrete implementation BatchLeaser) that coalesce near-simultaneous credential refreshes into a single token fetch per host/region window, lowering QPS to auth backends. 2) streaming: add typed stream Sink and SinkAdapter to replace less-idiomatic callback sinks while providing a compatibility shim. The new Sink exposes Push(ctx, envelope) error and Close(ctx) error with context deadlines for graceful shutdowns. 3) retry: add RetryProfileStream which uses jitter-aware backoff weights and latency-aware retry ceilings (used by streaming RPCs). 4) telemetry: hooks for cred lease metrics and per-sink backpressure/backoff telemetry. 5) version bump: Go SDK to v1.9.0, update changelog and examples. Commits (high-level): - add batcher and lease pool with unit tests - introduce typed StreamSink, adapters and examples - implement RetryProfileStream and wire into streaming client - telemetry hooks and prometheus labels - docs and migration notes - unit + integration tests and bench harness Changes by area (files, high-level): - credential/lease/batcher.go added core batcher implementation - credential/lease/pool.go pool API and tests - streaming/sink.go new typed interface and adapter shim - streaming/sink_adapter_compat.go backward compatibility shim for callback codepaths - retry/profiles_stream.go new stream retry profile - examples/streaming/typed_sink_example.go example and migration guide - telemetry/metrics.go add cred lease metrics and labels - CHANGES.md and VERSION file bump - tests/integration/stream_lease_bench_test.go bench and CI integration notes Performance and benchmarks: synthetic auth-heavy integration bench ran on a 1000-concurrent-streams workload. Observed results: median token fetches to auth service reduced by ~78% under aligned refresh windows; end-to-end mean latency for stream connect+first-event improved 6-12% depending on region; per-worker CPU usage decreased ~18% in auth hotspots because fewer goroutines blocked on repeated reauth. Safety and compatibility: The typed Sink interface is additive. To prevent breaking callers we included SinkAdapter that wraps the legacy callback-based subscription sinks. We changed Stream.Close signature to return error (previously void) to allow callers to observe drain failures; a compatibility adapter retains the old behavior for callers that ignore the returned error. Testing: Added unit tests covering batch coalescing edge cases, race detector runs for lease pool, and integration bench that exercises reauth storms. CI: initial pipeline showed a flaky integration test in the bench harness (TestStreamLease_Race) failing under Go race detector on the first run. Fix committed to use a tighter sync fence and re-run. Final CI: all checks passed including linters, unit tests, integration bench (non-flaky on rerun), and benchmarks recorded as artifacts. Review conversation summary: - Amelia Ruiz requested clearer migration notes and an explicit adapter example for the legacy callback API. Author responded with an example in examples/streaming/typed_sink_example.go and updated CHANGES.md. - Jonah Kim asked for metrics labels to include \"cred_scope\" and a clarified histogram for reauth latency; author added labels and a synthetic histogram in telemetry/metrics.go. - Sofia Alvarez flagged potential deadlock in batcher shutdown; author added explicit shutdown sequence and added a deterministic unit test that reproduced and then fixed the issue. All reviewers approved after the follow-ups. Merge and release details: Merged via squash-and-merge into main on 2026-03-05. Release notes updated and an internal rollout plan scheduled for Dedicated clusters (ENG-2148) with a feature flag for batcher enabled off by default. Migration guidance: - Users can switch to the typed StreamSink for clearer error handling and context-aware Close. - If using legacy callback sinks, no immediate action required thanks to SinkAdapter, but switching recommended for observability and structured shutdowns. - To enable credential batching cluster-side, flip feature flag \"cred.batch.enabled\"; rollout staged in Dedicated and Private deployments. Related work: coordinated with SDK teams (PY/TS) to align retry profile names and metrics; a follow-up task ENG-2199 will add the same batcher shim to Python and TypeScript SDKs. Checklist: [x] design doc reviewed internally [x] unit tests added [x] integration bench added [x] examples and migration notes [x] reviewers approved [x] CI passed [x] updated release notes","occurredAt":1772668800000,"entityType":"pull_request","routeKey":"apps/github/repos/redwood-sdk-go","sourcePath":"sources/github/redwood-sdk-go/pr-642358-credential-lease-batcher-stream-sink-ergonomics.json","peopleIds":["ethan-park"],"links":["ENG-2148","ENG-2199","SUP-412"],"tags":["sdk-go","auth","streaming","ergonomics","perf","release","redwood-sdk-go"]},{"id":"github:redwood-sdk-go:624","app":"github","title":"Streaming: typed disconnect errors w/ request_id + docs on safe reconnect/retry patterns","preview":"Context: This PR is part of the cross-language SDK parity work for streaming semantics and error taxonomy. We’ve had multiple reports (SUP-29505) of streaming disconnects behind proxies/load balancers where the Go SDK returned opaque io.EOF / net errors without request_id, making Support/oncall triage slow (INT-1182). Also, customers asked for clearer guidance on whether they should retry streaming calls, and under what conditions (avoid duplicated completions / side effects). What this PR does: (1) Introduces typed streaming error surfaces so callers can distinguish server-side HTTP failures (pre-stream) vs transport disconnects (mid-stream) vs user cancellation/timeouts. (2) Propagates request_id and status_code onto those error types whenever we can infer them from headers/initial response. (3) Adds docs + examples for safe retry patterns and reconnect guidance, with explicit idempotency considerations. Details: - New error types (package redwood): StreamDisconnectError (raised when the HTTP connection drops after streaming has started), StreamResponseError (raised when we receive a non-2xx before streaming begins; wraps the existing APIError shape and includes request_id/status_code/error_code), and StreamCancelledError (wrapping context.Canceled / deadline exceeded so users can distinguish client-driven aborts from server disconnects). - request_id sourcing: for pre-stream errors, request_id comes from the JSON error payload when present, falling back to the X-Request-Id response header; for mid-stream disconnects, we attach request_id from the initial response headers captured at stream start (if we got that far), otherwise request_id is empty and callers should log their own correlation ID as well. - status_code behavior: StreamResponseError always has status_code; StreamDisconnectError sets status_code to 200 when we successfully established the stream and later disconnected (since the HTTP status was OK) and includes a reason string populated from the underlying error (EOF, read: connection reset, unexpected FIN, etc.). - Retry guidance (docs): We intentionally do NOT auto-retry mid-stream because replaying a generation can duplicate user-visible output and can duplicate side effects when tool/function calls are involved. The docs now recommend: (a) retries are safe only when the request failed before any tokens/events were delivered (StreamResponseError) and only for retryable status codes (429/5xx) using the shared retry policy; (b) for mid-stream disconnects (StreamDisconnectError) treat it as a partial result; if you must resume, restart a new request using application-level state (messages so far, partial text) and ensure your downstream side effects are idempotent; (c) for any retried request that could cause side effects, set an Idempotency-Key header and persist it per user action so retries dedupe on the server where supported. - Examples: Added a small snippet showing how to log request_id and differentiate disconnect vs retryable HTTP error. - Tests: Added unit tests that simulate: non-2xx error payload with X-Request-Id header; mid-stream disconnect after N events; and cancellation via context.WithCancel. Tests assert error types and fields (request_id/status_code) and verify we don’t leak API key in error strings. Why now: This was a top gap in the parity matrix (streaming edge cases + request_id propagation) and blocked docs updates and support macros. Follow-ups: - Conformance suite: we should add a fixture that forces a streaming disconnect and asserts the error surface matches other SDKs (tracked in ENG-5127). - Platform: ensure request_id is always present on initial streaming response headers even for proxy-buffered clients (see platform PR redwood/pr-28426). Checklist: - [x] Typed errors exposed for streaming paths - [x] request_id surfaced consistently and redaction-safe - [x] Docs updated w/ retry + idempotency guidance - [x] Unit tests for disconnect + cancellation - [x] CI green","text":"Context: This PR is part of the cross-language SDK parity work for streaming semantics and error taxonomy. We’ve had multiple reports (SUP-29505) of streaming disconnects behind proxies/load balancers where the Go SDK returned opaque io.EOF / net errors without request_id, making Support/oncall triage slow (INT-1182). Also, customers asked for clearer guidance on whether they should retry streaming calls, and under what conditions (avoid duplicated completions / side effects). What this PR does: (1) Introduces typed streaming error surfaces so callers can distinguish server-side HTTP failures (pre-stream) vs transport disconnects (mid-stream) vs user cancellation/timeouts. (2) Propagates request_id and status_code onto those error types whenever we can infer them from headers/initial response. (3) Adds docs + examples for safe retry patterns and reconnect guidance, with explicit idempotency considerations. Details: - New error types (package redwood): StreamDisconnectError (raised when the HTTP connection drops after streaming has started), StreamResponseError (raised when we receive a non-2xx before streaming begins; wraps the existing APIError shape and includes request_id/status_code/error_code), and StreamCancelledError (wrapping context.Canceled / deadline exceeded so users can distinguish client-driven aborts from server disconnects). - request_id sourcing: for pre-stream errors, request_id comes from the JSON error payload when present, falling back to the X-Request-Id response header; for mid-stream disconnects, we attach request_id from the initial response headers captured at stream start (if we got that far), otherwise request_id is empty and callers should log their own correlation ID as well. - status_code behavior: StreamResponseError always has status_code; StreamDisconnectError sets status_code to 200 when we successfully established the stream and later disconnected (since the HTTP status was OK) and includes a reason string populated from the underlying error (EOF, read: connection reset, unexpected FIN, etc.). - Retry guidance (docs): We intentionally do NOT auto-retry mid-stream because replaying a generation can duplicate user-visible output and can duplicate side effects when tool/function calls are involved. The docs now recommend: (a) retries are safe only when the request failed before any tokens/events were delivered (StreamResponseError) and only for retryable status codes (429/5xx) using the shared retry policy; (b) for mid-stream disconnects (StreamDisconnectError) treat it as a partial result; if you must resume, restart a new request using application-level state (messages so far, partial text) and ensure your downstream side effects are idempotent; (c) for any retried request that could cause side effects, set an Idempotency-Key header and persist it per user action so retries dedupe on the server where supported. - Examples: Added a small snippet showing how to log request_id and differentiate disconnect vs retryable HTTP error. - Tests: Added unit tests that simulate: non-2xx error payload with X-Request-Id header; mid-stream disconnect after N events; and cancellation via context.WithCancel. Tests assert error types and fields (request_id/status_code) and verify we don’t leak API key in error strings. Why now: This was a top gap in the parity matrix (streaming edge cases + request_id propagation) and blocked docs updates and support macros. Follow-ups: - Conformance suite: we should add a fixture that forces a streaming disconnect and asserts the error surface matches other SDKs (tracked in ENG-5127). - Platform: ensure request_id is always present on initial streaming response headers even for proxy-buffered clients (see platform PR redwood/pr-28426). Checklist: - [x] Typed errors exposed for streaming paths - [x] request_id surfaced consistently and redaction-safe - [x] Docs updated w/ retry + idempotency guidance - [x] Unit tests for disconnect + cancellation - [x] CI green","occurredAt":1771027200000,"entityType":"pull_request","routeKey":"apps/github/repos/redwood-sdk-go","sourcePath":"sources/github/redwood-sdk-go/pr-624-streaming-reconnect-guidance-and-error-types.json","peopleIds":["allison-grant","ibrahim-khan","marcus-lin","mateusz-zielinski","sabrina-james"],"links":["ENG-5125","ENG-5126","ENG-5128","SUP-29505","INT-1182"],"tags":["sdk","go","streaming","reliability","docs","redwood-sdk-go"]},{"id":"github:redwood-sdk-go:619","app":"github","title":"Align timeout model + ensure context cancellation aborts HTTP and streaming reads","preview":"Context\nWe’re standardizing timeout behavior across Python/TS/Go. The Go SDK currently mixes (a) http.Client.Timeout, (b) ad-hoc context.WithTimeout usage, and (c) streaming loops that can keep blocking reads even after ctx cancellation. The result is inconsistent behavior across request types and, in a few cases, goroutine leaks + hung streaming reads when callers cancel.\n\nThis PR aligns the Go SDK with the timeout ADR direction (overall + granular connect/read) and makes ctx cancellation reliably abort:\n- non-streaming requests (request/response)\n- streaming requests (SSE/event-stream) including when blocked in io.Read / bufio reads\n\nWhat changed\n1) New unified timeout configuration\n- Introduce sdk.Timeouts:\n  - Overall: total wall-clock time for the request (0 = no overall timeout)\n  - Connect: max time to establish connection + receive response headers\n  - Read: max idle time waiting for the next byte on the response body (0 = no read timeout)\n- Client now accepts WithTimeouts(Timeouts) and applies defaults consistently.\n- Deprecate the old client option Timeout (single duration) but keep it working (maps to Overall) with a deprecation notice in docs/comments.\n\n2) Transport-level mapping (net/http)\n- We no longer use http.Client.Timeout for streaming calls by default (it can terminate long-lived streams unexpectedly).\n- For all requests:\n  - Connect maps to Transport.ResponseHeaderTimeout (covers time-to-first-byte / headers)\n  - Overall is enforced by context deadline on the request (ctx.WithTimeout)\n- For non-streaming calls:\n  - Read is enforced by our response body reader wrapper (idle timer) and by ensuring ctx cancellation closes the body.\n- For streaming calls:\n  - We enforce cancellation by closing the response body as soon as ctx.Done() fires; this unblocks the streaming read loop immediately.\n  - Read timeout (idle) is opt-in; default is 0 for streaming to avoid surprising disconnects on slow token streams. (Docs updated with guidance.)\n\n3) Context cancellation that actually interrupts streaming reads\n- Streaming previously used a buffered reader loop that could remain blocked in Read even after ctx was cancelled.\n- Now, when a stream is started, we wire a cancellation watcher:\n  - go routine waits on ctx.Done() and calls resp.Body.Close()\n  - stream loop exits with context.Canceled / context.DeadlineExceeded mapped to a typed SDK error\n- Added safeguards to avoid goroutine leaks:\n  - watcher exits when stream finishes normally\n  - we use a done channel to signal termination\n\n4) Tests\n- Add regression tests for:\n  - ctx cancellation interrupts an in-flight non-streaming request (server blocks)\n  - ctx cancellation interrupts a streaming read (server writes headers then stalls)\n  - Overall timeout triggers deadline exceeded for non-streaming\n  - streaming does NOT apply Overall by default (unless explicitly configured)\n\n5) Docs + migration notes (in-repo)\n- Update README + godoc for ClientOptions / WithTimeouts.\n- Add a short note in CHANGELOG.md about the new timeout semantics + deprecation of Timeout.\n\nDefaults (proposed standard for Go SDK)\n- Connect: 10s\n- Overall (non-streaming): 120s\n- Read (non-streaming): 60s idle\n- Streaming:\n  - Connect: 10s\n  - Overall: 0 (no overall)\n  - Read: 0 (no idle read timeout)\n\nWhy these defaults\n- Avoid retry storms / cascading failures from overly aggressive timeouts, while still preventing hung non-streaming requests.\n- Streaming is long-lived and should be caller-controlled via ctx and/or explicit timeouts.\n\nNotes on backward compatibility\n- This is a behavioral change for a subset of customers who previously relied on the legacy Timeout applying to streams. That behavior was inconsistent anyway (some streams ignored it, some were terminated by http.Client.Timeout). The new behavior is explicit: to limit stream duration, pass a context deadline or set Overall for streaming via WithTimeouts.\n\nChecklist\n- [x] Conforms to timeout ADR direction (overall + granular)\n- [x] Cancellation consistently aborts streaming reads\n- [x] Unit tests for cancellation and timeouts\n- [x] Docs/godoc updated\n\nFollow-ups\n- Conformance suite fixture for streaming cancellation (shared across SDKs) will land in ENG-5127.\n- After TS/Python land similar changes, we should align wording for streaming guidance across all docs.","text":"Context\nWe’re standardizing timeout behavior across Python/TS/Go. The Go SDK currently mixes (a) http.Client.Timeout, (b) ad-hoc context.WithTimeout usage, and (c) streaming loops that can keep blocking reads even after ctx cancellation. The result is inconsistent behavior across request types and, in a few cases, goroutine leaks + hung streaming reads when callers cancel.\n\nThis PR aligns the Go SDK with the timeout ADR direction (overall + granular connect/read) and makes ctx cancellation reliably abort:\n- non-streaming requests (request/response)\n- streaming requests (SSE/event-stream) including when blocked in io.Read / bufio reads\n\nWhat changed\n1) New unified timeout configuration\n- Introduce sdk.Timeouts:\n  - Overall: total wall-clock time for the request (0 = no overall timeout)\n  - Connect: max time to establish connection + receive response headers\n  - Read: max idle time waiting for the next byte on the response body (0 = no read timeout)\n- Client now accepts WithTimeouts(Timeouts) and applies defaults consistently.\n- Deprecate the old client option Timeout (single duration) but keep it working (maps to Overall) with a deprecation notice in docs/comments.\n\n2) Transport-level mapping (net/http)\n- We no longer use http.Client.Timeout for streaming calls by default (it can terminate long-lived streams unexpectedly).\n- For all requests:\n  - Connect maps to Transport.ResponseHeaderTimeout (covers time-to-first-byte / headers)\n  - Overall is enforced by context deadline on the request (ctx.WithTimeout)\n- For non-streaming calls:\n  - Read is enforced by our response body reader wrapper (idle timer) and by ensuring ctx cancellation closes the body.\n- For streaming calls:\n  - We enforce cancellation by closing the response body as soon as ctx.Done() fires; this unblocks the streaming read loop immediately.\n  - Read timeout (idle) is opt-in; default is 0 for streaming to avoid surprising disconnects on slow token streams. (Docs updated with guidance.)\n\n3) Context cancellation that actually interrupts streaming reads\n- Streaming previously used a buffered reader loop that could remain blocked in Read even after ctx was cancelled.\n- Now, when a stream is started, we wire a cancellation watcher:\n  - go routine waits on ctx.Done() and calls resp.Body.Close()\n  - stream loop exits with context.Canceled / context.DeadlineExceeded mapped to a typed SDK error\n- Added safeguards to avoid goroutine leaks:\n  - watcher exits when stream finishes normally\n  - we use a done channel to signal termination\n\n4) Tests\n- Add regression tests for:\n  - ctx cancellation interrupts an in-flight non-streaming request (server blocks)\n  - ctx cancellation interrupts a streaming read (server writes headers then stalls)\n  - Overall timeout triggers deadline exceeded for non-streaming\n  - streaming does NOT apply Overall by default (unless explicitly configured)\n\n5) Docs + migration notes (in-repo)\n- Update README + godoc for ClientOptions / WithTimeouts.\n- Add a short note in CHANGELOG.md about the new timeout semantics + deprecation of Timeout.\n\nDefaults (proposed standard for Go SDK)\n- Connect: 10s\n- Overall (non-streaming): 120s\n- Read (non-streaming): 60s idle\n- Streaming:\n  - Connect: 10s\n  - Overall: 0 (no overall)\n  - Read: 0 (no idle read timeout)\n\nWhy these defaults\n- Avoid retry storms / cascading failures from overly aggressive timeouts, while still preventing hung non-streaming requests.\n- Streaming is long-lived and should be caller-controlled via ctx and/or explicit timeouts.\n\nNotes on backward compatibility\n- This is a behavioral change for a subset of customers who previously relied on the legacy Timeout applying to streams. That behavior was inconsistent anyway (some streams ignored it, some were terminated by http.Client.Timeout). The new behavior is explicit: to limit stream duration, pass a context deadline or set Overall for streaming via WithTimeouts.\n\nChecklist\n- [x] Conforms to timeout ADR direction (overall + granular)\n- [x] Cancellation consistently aborts streaming reads\n- [x] Unit tests for cancellation and timeouts\n- [x] Docs/godoc updated\n\nFollow-ups\n- Conformance suite fixture for streaming cancellation (shared across SDKs) will land in ENG-5127.\n- After TS/Python land similar changes, we should align wording for streaming guidance across all docs.","occurredAt":1770336000000,"entityType":"pull_request","routeKey":"apps/github/repos/redwood-sdk-go","sourcePath":"sources/github/redwood-sdk-go/pr-619-timeout-model-and-context-cancellation.json","peopleIds":["allison-grant","ibrahim-khan","kaitlyn-nguyen","mateusz-zielinski","sean-gallagher"],"links":["ENG-5125","ENG-5121"],"tags":["sdk-go","reliability","timeouts","streaming","breaking-change-candidate","redwood-sdk-go"]},{"id":"github:redwood-sdk-go:612","app":"github","title":"sdk(go): standardize retry policy (429/5xx/network), Retry-After, jittered backoff + options","preview":"## Summary\nThis PR implements the SDK parity retry standard for the Go SDK: retryable status codes + network errors, exponential backoff with jitter, and Retry-After parsing/precedence. It also adds a public configuration surface so users can tune or disable retries consistently across languages.\n\nMotivation:\n- Fixes customer-reported behavior where the Go SDK ignored Retry-After and retried too aggressively during rate limiting (SUP-29477).\n- Aligns with the cross-language standard for predictable behavior and easier support/debugging.\n\n## What changed\n### Defaults (v1)\n- **Retryable HTTP status codes:** 408, 409, 429, 500, 502, 503, 504\n- **Retryable network errors:** connection reset, EOF mid-response before headers, temporary errors, and timeouts from the underlying net/http stack\n- **Not retried:** context cancellation/deadline exceeded initiated by the caller, and any 4xx other than 408/409/429\n- **Max attempts:** 4 total (1 initial + up to 3 retries)\n- **Backoff:** exponential (base 250ms) capped to 8s, with full jitter\n- **Retry-After:** respected when present on 429 (and generally for 503 when present), parsed from either delta-seconds or HTTP-date; capped by MaxBackoff; takes precedence over computed backoff\n\n### Public API\nNew options on client construction:\n- `RetryOptions{ MaxAttempts, InitialBackoff, MaxBackoff, Jitter, RetryableStatusCodes, RespectRetryAfter, OnRetry }`\n- `WithRetryOptions(RetryOptions)`\n- `DisableRetries()` convenience\n\n`OnRetry` is a hook invoked for observability/debugging (no secrets): attempt number, wait duration, status code (if any), and Redwood request id (if available).\n\n### Implementation details\n- Centralized retry policy in `internal/retry/policy.go` and wired through the default HTTP transport wrapper.\n- Retry classifier uses:\n  - HTTP status code classification\n  - net.Error temporary/timeout checks\n  - explicit exclusions for `context.Canceled` / `context.DeadlineExceeded` caused by caller context\n- Retry delay selection:\n  1) Retry-After (if enabled and parseable)\n  2) computed exponential backoff with jitter\n\n### Streaming behavior\nStreaming requests are **not automatically retried**. This matches the standard and avoids duplicate side effects / duplicated output. The SDK surfaces a typed error with the last known `request_id` (when available) and guidance to retry only when the user can safely do so with idempotency.\n\n## Tests\n- Unit tests for retry classifier: status codes, context cancellation, common net/http error types.\n- Unit tests for Retry-After parsing (seconds + date) including max-backoff capping.\n- Integration-style tests using `httptest.Server` to simulate:\n  - 429 with Retry-After and verifying sleep duration is derived from header\n  - 503 bursts and verifying attempts count and jittered wait range\n  - network failure on first attempt then success\n\n## Files / areas touched\n- `client/client.go`: plumb retry options into HTTP client creation\n- `client/options.go`: new `RetryOptions` and `WithRetryOptions`\n- `internal/retry/*`: retry policy + backoff/jitter helpers + Retry-After parser\n- `internal/transport/retry_round_tripper.go`: transport wrapper implementing retry loop\n- `internal/errors/*`: propagate request_id into retry errors (ties to INT-1182)\n- `docs/retries.md`: updated to match parity language and defaults\n\n## Backwards compatibility\nThis is a behavioral change (retries will now happen in more cases, and will slow down appropriately under Retry-After). To minimize surprises:\n- Defaults remain conservative (4 total attempts, capped backoff).\n- Users can disable or tune retries via `DisableRetries()` / `WithRetryOptions`.\n\n## Follow-ups\n- Conformance suite wiring to shared fixtures (tracked in ENG-5127).\n- Ensure gateway always emits Retry-After on 429 (platform work; tracked separately).\n\n---\n\n## Review conversation (highlights)\n**Allison Grant:** Can we confirm we don’t retry when the caller’s context is canceled? This was a common source of “SDK ignored cancellation” complaints.\n\n**Mateusz Zielinski (author):** Yes. The classifier checks `errors.Is(err, context.Canceled)` / `context.DeadlineExceeded` and treats those as terminal. Added a unit test covering both cases.\n\n**Sean Gallagher:** Please cap Retry-After to MaxBackoff to avoid long sleeps on misconfigured proxies.\n\n**Mateusz Zielinski (author):** Done. `retryAfterDelay()` now clamps to `MaxBackoff` and logs (via `OnRetry`) that clamp occurred.\n\n**Ibrahim Khan:** Default attempt count should match the standard (4 total). Also ensure Retry-After takes precedence over computed backoff.\n\n**Mateusz Zielinski (author):** Updated defaults to 4 total attempts and reordered delay selection to prefer Retry-After.\n\n**Kaitlyn Nguyen:** Ensure `OnRetry` never includes the API key in errors/logs. Also no request/response bodies.\n\n**Mateusz Zielinski (author):** Confirmed. Hook receives only attempt/wait/status/request_id + error type.\n\nApprovals:\n- Allison Grant ✅\n- Sean Gallagher ✅\n- Ibrahim Khan ✅\n- Kaitlyn Nguyen ✅\n\n## CI\n- go test ./...: pass\n- golangci-lint: pass\n- docs build: pass\n\n## Merge\nMerged via squash into `main` on 2026-01-28.","text":"## Summary\nThis PR implements the SDK parity retry standard for the Go SDK: retryable status codes + network errors, exponential backoff with jitter, and Retry-After parsing/precedence. It also adds a public configuration surface so users can tune or disable retries consistently across languages.\n\nMotivation:\n- Fixes customer-reported behavior where the Go SDK ignored Retry-After and retried too aggressively during rate limiting (SUP-29477).\n- Aligns with the cross-language standard for predictable behavior and easier support/debugging.\n\n## What changed\n### Defaults (v1)\n- **Retryable HTTP status codes:** 408, 409, 429, 500, 502, 503, 504\n- **Retryable network errors:** connection reset, EOF mid-response before headers, temporary errors, and timeouts from the underlying net/http stack\n- **Not retried:** context cancellation/deadline exceeded initiated by the caller, and any 4xx other than 408/409/429\n- **Max attempts:** 4 total (1 initial + up to 3 retries)\n- **Backoff:** exponential (base 250ms) capped to 8s, with full jitter\n- **Retry-After:** respected when present on 429 (and generally for 503 when present), parsed from either delta-seconds or HTTP-date; capped by MaxBackoff; takes precedence over computed backoff\n\n### Public API\nNew options on client construction:\n- `RetryOptions{ MaxAttempts, InitialBackoff, MaxBackoff, Jitter, RetryableStatusCodes, RespectRetryAfter, OnRetry }`\n- `WithRetryOptions(RetryOptions)`\n- `DisableRetries()` convenience\n\n`OnRetry` is a hook invoked for observability/debugging (no secrets): attempt number, wait duration, status code (if any), and Redwood request id (if available).\n\n### Implementation details\n- Centralized retry policy in `internal/retry/policy.go` and wired through the default HTTP transport wrapper.\n- Retry classifier uses:\n  - HTTP status code classification\n  - net.Error temporary/timeout checks\n  - explicit exclusions for `context.Canceled` / `context.DeadlineExceeded` caused by caller context\n- Retry delay selection:\n  1) Retry-After (if enabled and parseable)\n  2) computed exponential backoff with jitter\n\n### Streaming behavior\nStreaming requests are **not automatically retried**. This matches the standard and avoids duplicate side effects / duplicated output. The SDK surfaces a typed error with the last known `request_id` (when available) and guidance to retry only when the user can safely do so with idempotency.\n\n## Tests\n- Unit tests for retry classifier: status codes, context cancellation, common net/http error types.\n- Unit tests for Retry-After parsing (seconds + date) including max-backoff capping.\n- Integration-style tests using `httptest.Server` to simulate:\n  - 429 with Retry-After and verifying sleep duration is derived from header\n  - 503 bursts and verifying attempts count and jittered wait range\n  - network failure on first attempt then success\n\n## Files / areas touched\n- `client/client.go`: plumb retry options into HTTP client creation\n- `client/options.go`: new `RetryOptions` and `WithRetryOptions`\n- `internal/retry/*`: retry policy + backoff/jitter helpers + Retry-After parser\n- `internal/transport/retry_round_tripper.go`: transport wrapper implementing retry loop\n- `internal/errors/*`: propagate request_id into retry errors (ties to INT-1182)\n- `docs/retries.md`: updated to match parity language and defaults\n\n## Backwards compatibility\nThis is a behavioral change (retries will now happen in more cases, and will slow down appropriately under Retry-After). To minimize surprises:\n- Defaults remain conservative (4 total attempts, capped backoff).\n- Users can disable or tune retries via `DisableRetries()` / `WithRetryOptions`.\n\n## Follow-ups\n- Conformance suite wiring to shared fixtures (tracked in ENG-5127).\n- Ensure gateway always emits Retry-After on 429 (platform work; tracked separately).\n\n---\n\n## Review conversation (highlights)\n**Allison Grant:** Can we confirm we don’t retry when the caller’s context is canceled? This was a common source of “SDK ignored cancellation” complaints.\n\n**Mateusz Zielinski (author):** Yes. The classifier checks `errors.Is(err, context.Canceled)` / `context.DeadlineExceeded` and treats those as terminal. Added a unit test covering both cases.\n\n**Sean Gallagher:** Please cap Retry-After to MaxBackoff to avoid long sleeps on misconfigured proxies.\n\n**Mateusz Zielinski (author):** Done. `retryAfterDelay()` now clamps to `MaxBackoff` and logs (via `OnRetry`) that clamp occurred.\n\n**Ibrahim Khan:** Default attempt count should match the standard (4 total). Also ensure Retry-After takes precedence over computed backoff.\n\n**Mateusz Zielinski (author):** Updated defaults to 4 total attempts and reordered delay selection to prefer Retry-After.\n\n**Kaitlyn Nguyen:** Ensure `OnRetry` never includes the API key in errors/logs. Also no request/response bodies.\n\n**Mateusz Zielinski (author):** Confirmed. Hook receives only attempt/wait/status/request_id + error type.\n\nApprovals:\n- Allison Grant ✅\n- Sean Gallagher ✅\n- Ibrahim Khan ✅\n- Kaitlyn Nguyen ✅\n\n## CI\n- go test ./...: pass\n- golangci-lint: pass\n- docs build: pass\n\n## Merge\nMerged via squash into `main` on 2026-01-28.","occurredAt":1769558400000,"entityType":"pull_request","routeKey":"apps/github/repos/redwood-sdk-go","sourcePath":"sources/github/redwood-sdk-go/pr-612-retry-policy-standardization.json","peopleIds":["allison-grant","ibrahim-khan","kaitlyn-nguyen","mateusz-zielinski","sean-gallagher"],"links":["ENG-5120","ENG-5121","ENG-5126","ENG-5127","SUP-29477","INT-1182"],"tags":["sdk","go","reliability","retries","parity","redwood-sdk-go"]},{"id":"github:redwood-sdk-go:630","app":"github","title":"Align auth env vars + Authorization header formatting; redact secrets in debug logs","preview":"","text":"","occurredAt":1769212800000,"entityType":"pull_request","routeKey":"apps/github/repos/redwood-sdk-go","sourcePath":"sources/github/redwood-sdk-go/pr-630-auth-env-vars-and-header-format.json","peopleIds":["allison-grant","ibrahim-khan","kaitlyn-nguyen","marcus-lin","mateusz-zielinski"],"links":["ENG-5122","ENG-5126","INT-1182"],"tags":["sdk-go","auth","security","devex","parity","redwood-sdk-go"]},{"id":"github:redwood-sdk-go:412","app":"github","title":"sdk(go): add streaming reconnect helper + example for resilient chat streams","preview":"Context: We’ve seen a recurring onboarding/first-prod issue where customers try streaming in a quickstart-like environment and interpret transient disconnects (mobile networks, reverse proxies, idle timeouts) as SDK instability. Support ticket SUP-18488 captures a representative failure mode. This PR adds a small, opinionated helper for resilient streaming consumption in the Go SDK and a runnable example to show recommended patterns.\n\nWhat’s included: (1) A new streaming reconnect helper in package streamx that wraps our existing streaming client and provides controlled reconnect behavior. It retries on network/transport errors and selected HTTP statuses (408/429/5xx) with exponential backoff + jitter, respects Retry-After, and uses a bounded max elapsed time / max attempts. (2) A lightweight event cursor that lets callers preserve progress (last received event ID / last chunk index) to avoid double-processing when reconnecting. For providers/paths where we can’t resume server-side, we still guarantee client-side idempotency hooks (OnChunk) are invoked with a monotonic cursor and the helper can be configured to drop duplicates by cursor. (3) A new example (examples/streaming_reconnect_chat) showing how to wire context cancellation, timeouts, structured logging, and a simple health endpoint that validates outbound connectivity.\n\nNon-goals: This does not change default retry policy for non-streaming requests. It also does not attempt to fully replay the same server-side stream after partial completion; in most cases the correct app-level behavior is to re-issue the request with conversation state and handle duplicates defensively. The helper is intentionally explicit and opt-in.\n\nAPI shape:\n- streamx.ReconnectConfig { MaxAttempts, MaxElapsed, BaseDelay, MaxDelay, RetryOnStatuses, RespectRetryAfter, Logger, CursorStore }\n- streamx.StreamWithReconnect(ctx, cfg, func(ctx) (Stream, error), handler)\nWhere handler receives chunks/events and returns (continue bool, err error). The handler is invoked sequentially.\n\nWhy now: This maps directly to the onboarding checklist “Streaming: handle disconnects/timeouts” step and unblocks the DevEx team from pointing to a single canonical Go snippet.\n\nTesting: Unit tests cover backoff behavior, Retry-After parsing, max elapsed cutoff, duplicate suppression via cursor, and handler error propagation. Integration-ish tests run against the existing mock streaming server used elsewhere in the SDK.\n\nDocs: Adds GoDoc comments + a README in the example directory with copy/paste instructions.\n\nRollout: Safe additive change. No behavior changes unless consumers adopt streamx.StreamWithReconnect or copy the example patterns.\n\nScreenshots/log output: Example prints connection attempts, retry reasons, and final outcome; see examples/streaming_reconnect_chat/README.md.\n\nFollow-ups: Align naming with TypeScript SDK helper once that lands (ENG-5820 umbrella). Consider adding a small section in docs site linking to this example from the Console checklist deep link.","text":"Context: We’ve seen a recurring onboarding/first-prod issue where customers try streaming in a quickstart-like environment and interpret transient disconnects (mobile networks, reverse proxies, idle timeouts) as SDK instability. Support ticket SUP-18488 captures a representative failure mode. This PR adds a small, opinionated helper for resilient streaming consumption in the Go SDK and a runnable example to show recommended patterns.\n\nWhat’s included: (1) A new streaming reconnect helper in package streamx that wraps our existing streaming client and provides controlled reconnect behavior. It retries on network/transport errors and selected HTTP statuses (408/429/5xx) with exponential backoff + jitter, respects Retry-After, and uses a bounded max elapsed time / max attempts. (2) A lightweight event cursor that lets callers preserve progress (last received event ID / last chunk index) to avoid double-processing when reconnecting. For providers/paths where we can’t resume server-side, we still guarantee client-side idempotency hooks (OnChunk) are invoked with a monotonic cursor and the helper can be configured to drop duplicates by cursor. (3) A new example (examples/streaming_reconnect_chat) showing how to wire context cancellation, timeouts, structured logging, and a simple health endpoint that validates outbound connectivity.\n\nNon-goals: This does not change default retry policy for non-streaming requests. It also does not attempt to fully replay the same server-side stream after partial completion; in most cases the correct app-level behavior is to re-issue the request with conversation state and handle duplicates defensively. The helper is intentionally explicit and opt-in.\n\nAPI shape:\n- streamx.ReconnectConfig { MaxAttempts, MaxElapsed, BaseDelay, MaxDelay, RetryOnStatuses, RespectRetryAfter, Logger, CursorStore }\n- streamx.StreamWithReconnect(ctx, cfg, func(ctx) (Stream, error), handler)\nWhere handler receives chunks/events and returns (continue bool, err error). The handler is invoked sequentially.\n\nWhy now: This maps directly to the onboarding checklist “Streaming: handle disconnects/timeouts” step and unblocks the DevEx team from pointing to a single canonical Go snippet.\n\nTesting: Unit tests cover backoff behavior, Retry-After parsing, max elapsed cutoff, duplicate suppression via cursor, and handler error propagation. Integration-ish tests run against the existing mock streaming server used elsewhere in the SDK.\n\nDocs: Adds GoDoc comments + a README in the example directory with copy/paste instructions.\n\nRollout: Safe additive change. No behavior changes unless consumers adopt streamx.StreamWithReconnect or copy the example patterns.\n\nScreenshots/log output: Example prints connection attempts, retry reasons, and final outcome; see examples/streaming_reconnect_chat/README.md.\n\nFollow-ups: Align naming with TypeScript SDK helper once that lands (ENG-5820 umbrella). Consider adding a small section in docs site linking to this example from the Console checklist deep link.","occurredAt":1742947200000,"entityType":"pull_request","routeKey":"apps/github/repos/redwood-sdk-go","sourcePath":"sources/github/eval-harness/pr-412-add-streaming-reconnect-helper.json","peopleIds":["ibrahim-khan","mateusz-zielinski","monica-patel","sean-gallagher"],"links":["ENG-5820","SUP-18488"],"tags":["sdk","go","streaming","reliability","onboarding","redwood-sdk-go"]},{"id":"github:redwood-sdk-go:233","app":"github","title":"Tolerate trailing newline in API key and improve auth error messaging","preview":"Context: SUP-4902. Customers reading API keys from mounted files often include a trailing newline. The Go SDK previously validated the API key string strictly and returned a generic 'invalid api key' error, causing confusion. Changes: if key ends with CR/LF, trim only those characters; if other whitespace exists, return a clearer error message pointing to the whitespace; add unit tests; update README guidance for Kubernetes secret mounts. Release: v0.6.2.","text":"Context: SUP-4902. Customers reading API keys from mounted files often include a trailing newline. The Go SDK previously validated the API key string strictly and returned a generic 'invalid api key' error, causing confusion. Changes: if key ends with CR/LF, trim only those characters; if other whitespace exists, return a clearer error message pointing to the whitespace; add unit tests; update README guidance for Kubernetes secret mounts. Release: v0.6.2.","occurredAt":1739318400000,"entityType":"pull_request","routeKey":"apps/github/repos/redwood-sdk-go","sourcePath":"sources/github/redwood-sdk-go/pr-233-tolerate-trailing-newline-in-api-key.json","peopleIds":["elliot-price","hiro-tanaka","jordan-blake"],"links":["SUP-4902"],"tags":["sdk","go","auth","redwood-sdk-go"]},{"id":"github:redwood-sdk-go:375830","app":"github","title":"Introduce smart credential window and compact stream facade (Go SDK v1.4.1)","preview":"Motivation: Multiple customer incidents and internal benchmarks showed that short-lived credential refresh windows and ad-hoc retry knobs led to transient auth failures in long-lived streaming sessions. In parallel, Go SDK consumers requested a smaller streaming facade that supports pause/resume, context-aware cancellation, and better default backoff behavior so they don't reimplement session glue. This PR implements a 'SmartCredentialWindow' alongside a compact 'StreamFacade' and tightens default retry ergonomics for streaming APIs.\n\nWhat this PR does (single-line sections kept as a contiguous description): Introduces sdk/go/creds/window.go implementing SmartCredentialWindow which coalesces refresh attempts, exposes a small lifecycle API (StartLease, ExtendIfNeeded, Remaining) and integrates with the existing SessionManager; adds sdk/go/stream/facade.go - a compact streaming facade with Pause, Resume, and Close methods that are context-aware and surface a typed error set; refactors backoff and retry defaults under sdk/go/backoff/defaults.go to provide safer defaults for long-lived streams (longer initial jitter, capped max retries, and idempotency-aware shims); fixes edge cases around race conditions in session affinity by locking the underlying lease table (sdk/go/session/manager.go); updates tests and adds integration coverage for credential rotation + stream resume (tests/integration/stream_lease_test.go); bumps module version to v1.4.1 and updates changelog.\n\nCommit summary: 1) add SmartCredentialWindow and unit tests 2) add StreamFacade with pause/resume/close semantics 3) refactor backoff defaults and introduce stream-specific profiles 4) fix race in session manager and add mutex around lease updates 5) docs: add README snippets and usage examples 6) CI/test polish and lint fixes.\n\nFiles changed (high level): sdk/go/creds/window.go, sdk/go/stream/facade.go, sdk/go/backoff/defaults.go, sdk/go/session/manager.go, sdk/go/errors/stream_errors.go, tests/unit/cred_window_test.go, tests/integration/stream_lease_test.go, docs/sdk-go/streaming.md, go.mod, CHANGELOG.md, Makefile (test target tweaks), .golangci.yml.\n\nBackward compatibility and ergonomics: No public-breaking change to the top-level Client API. StreamFacade is additive; existing streaming constructors are preserved. SmartCredentialWindow is opt-in for customers that want automatic lease coalescing; the SessionManager exposes a small shim to opt-in during initialization. Retry default changes are conservative (scope limited to streaming paths) and are documented; customers can still explicitly override.\n\nTesting and CI: Initial CI run failed on a race detector in session manager (flaky test that surfaced under -race). Fixed by adding a fine-grained mutex and a comment explaining the rationale. Linting failures were addressed (naming and docs). Integration tests for credential rotation and resume were added and validated in CI. Final CI status: pass.\n\nChecklist: - [x] Unit tests for SmartCredentialWindow - [x] Integration test for stream resume + rotation - [x] Documentation example for StreamFacade - [x] Bumped module version and changelog - [x] CI green\n\nRelated work: ENG-2345 implements the server-side lease endpoint used here. ENG-2401 is the higher-level design RFC for stream ergonomics. See linked Linear items for more context.\n\nNotes on usage: Example snippet is in docs/sdk-go/streaming.md; basic pattern is to initialize SessionManager with EnableSmartWindow=true, then call client.StreamWithFacade(ctx, opts) to receive a StreamFacade instance, which can be paused during network backpressure and resumed when ready. The facade propagates context cancellation to underlying sockets and returns typed errors for auth/transport/order.\n\nIf anything, reviewers should watch for API name bikeshedding (StartLease vs AcquireLease) — I intentionally picked StartLease to emphasize lifecycle oriented usage rather than a blocking acquire. Open to bikeshed feedback.\n\nMerge details: squash-merged into main after 2 approvals and final CI green.","text":"Motivation: Multiple customer incidents and internal benchmarks showed that short-lived credential refresh windows and ad-hoc retry knobs led to transient auth failures in long-lived streaming sessions. In parallel, Go SDK consumers requested a smaller streaming facade that supports pause/resume, context-aware cancellation, and better default backoff behavior so they don't reimplement session glue. This PR implements a 'SmartCredentialWindow' alongside a compact 'StreamFacade' and tightens default retry ergonomics for streaming APIs.\n\nWhat this PR does (single-line sections kept as a contiguous description): Introduces sdk/go/creds/window.go implementing SmartCredentialWindow which coalesces refresh attempts, exposes a small lifecycle API (StartLease, ExtendIfNeeded, Remaining) and integrates with the existing SessionManager; adds sdk/go/stream/facade.go - a compact streaming facade with Pause, Resume, and Close methods that are context-aware and surface a typed error set; refactors backoff and retry defaults under sdk/go/backoff/defaults.go to provide safer defaults for long-lived streams (longer initial jitter, capped max retries, and idempotency-aware shims); fixes edge cases around race conditions in session affinity by locking the underlying lease table (sdk/go/session/manager.go); updates tests and adds integration coverage for credential rotation + stream resume (tests/integration/stream_lease_test.go); bumps module version to v1.4.1 and updates changelog.\n\nCommit summary: 1) add SmartCredentialWindow and unit tests 2) add StreamFacade with pause/resume/close semantics 3) refactor backoff defaults and introduce stream-specific profiles 4) fix race in session manager and add mutex around lease updates 5) docs: add README snippets and usage examples 6) CI/test polish and lint fixes.\n\nFiles changed (high level): sdk/go/creds/window.go, sdk/go/stream/facade.go, sdk/go/backoff/defaults.go, sdk/go/session/manager.go, sdk/go/errors/stream_errors.go, tests/unit/cred_window_test.go, tests/integration/stream_lease_test.go, docs/sdk-go/streaming.md, go.mod, CHANGELOG.md, Makefile (test target tweaks), .golangci.yml.\n\nBackward compatibility and ergonomics: No public-breaking change to the top-level Client API. StreamFacade is additive; existing streaming constructors are preserved. SmartCredentialWindow is opt-in for customers that want automatic lease coalescing; the SessionManager exposes a small shim to opt-in during initialization. Retry default changes are conservative (scope limited to streaming paths) and are documented; customers can still explicitly override.\n\nTesting and CI: Initial CI run failed on a race detector in session manager (flaky test that surfaced under -race). Fixed by adding a fine-grained mutex and a comment explaining the rationale. Linting failures were addressed (naming and docs). Integration tests for credential rotation and resume were added and validated in CI. Final CI status: pass.\n\nChecklist: - [x] Unit tests for SmartCredentialWindow - [x] Integration test for stream resume + rotation - [x] Documentation example for StreamFacade - [x] Bumped module version and changelog - [x] CI green\n\nRelated work: ENG-2345 implements the server-side lease endpoint used here. ENG-2401 is the higher-level design RFC for stream ergonomics. See linked Linear items for more context.\n\nNotes on usage: Example snippet is in docs/sdk-go/streaming.md; basic pattern is to initialize SessionManager with EnableSmartWindow=true, then call client.StreamWithFacade(ctx, opts) to receive a StreamFacade instance, which can be paused during network backpressure and resumed when ready. The facade propagates context cancellation to underlying sockets and returns typed errors for auth/transport/order.\n\nIf anything, reviewers should watch for API name bikeshedding (StartLease vs AcquireLease) — I intentionally picked StartLease to emphasize lifecycle oriented usage rather than a blocking acquire. Open to bikeshed feedback.\n\nMerge details: squash-merged into main after 2 approvals and final CI green.","occurredAt":1729468800000,"entityType":"pull_request","routeKey":"apps/github/repos/redwood-sdk-go","sourcePath":"sources/github/redwood-sdk-go/pr-375830-smart-cred-window-stream-facade-v1.4.1.json","peopleIds":["ava-chen"],"links":["ENG-2345","ENG-2401"],"tags":["sdk-go","auth","streaming","ergonomics","v1.4.1","perf","redwood-sdk-go"]}]
