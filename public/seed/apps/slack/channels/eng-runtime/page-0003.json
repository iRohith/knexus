[{"id":"slack:1740185509","app":"slack","title":"#eng-runtime: 1740185509","preview":"","text":"","occurredAt":1740191168000,"entityType":"thread","routeKey":"apps/slack/channels/eng-runtime","sourcePath":"sources/slack/eng-runtime/1740185509-perf-overhead-results.json","peopleIds":["caleb-johnson","connor-o-brien","julian-moore","marcus-reed","mateo-alvarez","nadia-rahman","omar-el-sayed","samir-qureshi","sofia-ivanova"],"links":[],"tags":["eng-runtime"]},{"id":"slack:1740144420","app":"slack","title":"#eng-runtime: 1740144420","preview":"Hiro Tanaka: seeing a decode p95 regression when enabling the new INT8 profile on llama3.1-70b-instruct variant x1. baseline p95 decode ~38ms/token, INT8 profile jumps to ~52ms/token on A100-80GB. encode looks fine. anyone else seeing this?\n\nNoah Patel: which profile id? and what runtime build?\n\nHiro Tanaka: profile ref `qpl:llama3.1-70b-x1:int8_w8a16:2025-02-19.3` (aka v3). runtime `runtime@b7f4c2a` from yesterday. route is pinned, no other changes.\n\nSofia Ivanova: is it only when continuous batching kicks in? (i.e. queue depth > 1)\n\nHiro Tanaka: happens even at qd=1. perf suite run `decode_only` shows it.\n\nOmar El-Sayed: any chance we’re invalidating KV cache layout due to different pack/unpack? INT8 weights shouldn’t touch KV but we do some alignment changes for fused kernels.\n\nHiro Tanaka: grabbed nsight systems trace. looks like we’re not taking the fused attention kernel on decode; falling back to a slower path.\n\nNoah Patel: can you paste the kernel names / top hotspots?\n\nHiro Tanaka: yep. from nsys summary (decode step, batch=1, seqlen=4096, new_tokens=64):\n```\nTime(%)  Total Time (ns)  Instances  Avg (ns)    Name\n46.8     2.13e+09         64         3.33e+07    void at::native::scaled_dot_product_attention...\n21.4     9.74e+08         128        7.61e+06    void cutlass::Kernel<...> gemm_f16...\n12.2     5.55e+08         64         8.67e+06    void kv_cache::gather_scatter<<<...>>>\n7.9      3.61e+08         64         5.64e+06    void layernorm<<<...>>>\n```\nBaseline run shows attention as `flash_fwd_splitkv` at ~22% instead of 46%.\n\nGrace Kim: that suggests kernel selection is flipping. do we have a guard that disables flash attention when int8 weights enabled? (i vaguely remember a conservative check in pr-18479)\n\nHiro Tanaka: I checked logs with `RUNTIME_LOG_LEVEL=debug`, seeing:\n`kernel_select: flash_attn=DISABLED reason=unsupported_combo(w8a16,int8_matmul)`\nThat seems… wrong? We shouldn’t tie attention kernel to weight GEMM mode.\n\nNoah Patel: yeah, attention is independent. the guard might be too broad: “if quantized then disable flash” type thing.\n\nSofia Ivanova: is this only for v3 profile? what about v2?\n\nHiro Tanaka: v2 profile (`...:2025-02-12.1`) is fine. v3 is the one that regresses.\n\nJonas Weber: perf-canary run from last night flagged same thing on A100, didn’t repro on H100. (reacted :eyes:)\n\nOmar El-Sayed: H100 path uses different attention backend; might hide it.\n\nGrace Kim: v3 changed anything besides weights? e.g. activation dtype or smoothquant params? if it flipped act to fp16 vs bf16 it could trip a capability check.\n\nHiro Tanaka: profile says w8a16, activations fp16 (same as v2), but it also sets `preferred_attention_backend=auto` and `max_context=8192`. v2 had `max_context=4096`.\n\nNoah Patel: ah. the “max_context” might be driving a conservative attention selection. maybe we disable flash if max_context > 4096 for that model family.\n\nSofia Ivanova: but your trace is seqlen=4096. even if max_context=8192, we should still allow flash at runtime when actual seqlen <= supported.\n\nMateo Alvarez: +1. selection should be based on actual request constraints, not profile max. unless the profile forces a different KV layout that breaks flash.\n\nOmar El-Sayed: KV layout: for >4k we allocate a different paged cache config (larger page size) if `max_context` is higher. That might cause the flash kernel to be disabled because it can’t handle that page size.\n\nHiro Tanaka: interesting. I saw `kv_cache.page_size=256` in the v3 run, baseline/v2 is `page_size=128`.\n\nOmar El-Sayed: there we go. our flash attention decode kernel only supports page_size=128 right now (and maybe 64). For 256 we fallback to sdpa.\n\nGrace Kim: can we clamp page_size based on actual seqlen? or change heuristic: don’t bump page_size just because profile advertises max_context.\n\nNoah Patel: we should also surface this as an explicit warning: “flash disabled due to kv_page_size=256” not “unsupported_combo(w8a16,int8_matmul)”. that error message is misleading.\n\nHiro Tanaka: agree. quick experiment: forced `KV_PAGE_SIZE=128` env var with v3 profile, decode p95 goes back to ~39ms/token.\n\nSelene Huang: any risk changing page size affects correctness? (i.e. address calculations)\n\nOmar El-Sayed: shouldn’t. page size is perf/memory tradeoff. correctness is same, just different indexing. But we need to ensure we don’t OOM for 8k contexts if we keep 128.\n\nJonas Weber: for A100 80GB, 70B @ 8k is already tight; bumping page size was meant to reduce metadata overhead. maybe the right fix is “page_size=128 if seqlen<=4096 else 256” (or pick dynamically at allocation).\n\nSofia Ivanova: + make it deterministic across replicas (same request -> same selection). but that’s fine if it’s purely a function of request seqlen + model.\n\nMateo Alvarez: action items:\n1) fix kernel_select log reason (don’t blame int8)\n2) adjust KV cache page sizing heuristic to be request-aware so we don’t disable flash for 4k runs\n3) add perf-canary coverage specifically for “profile max_context > request seqlen” case\n\nHiro Tanaka: I can open a quick PR for #1/#2 unless Omar wants it.\n\nOmar El-Sayed: I’ll take #2 (KV sizing). Hiro can do #1 (log reason + guard). Let’s coordinate to avoid conflicts.\n\nNoah Patel: can someone paste the relevant config code path? I think it’s in `kv_cache/config.rs`.\n\nOmar El-Sayed: it’s in `KvCacheConfig::from_profile()` currently:\n```\nif profile.max_context >= 8192 {\n  page_size = 256;\n}\n```\nWe should move it to `KvCacheConfig::from_request(model, seqlen, profile)`.\n\nGrace Kim: also update compatibility matrix docs? customers might ask why “INT8 made it slower” when it’s actually KV paging.\n\nHiro Tanaka: ok, I’ll add a metric tag too: `attention_backend=flash|sdpa` and `kv_page_size` in runtime span logs (if not already). helps with future triage.\n\nJonas Weber: please do. perf suite can then assert “flash still enabled” on the 4k decode benchmark.\n\nMateo Alvarez: keep me in the loop, this is close to beta. we should consider temporarily blocking v3 publication for A100 until fix lands (or mark as caveat).\n\nHiro Tanaka: ack. I’ll ping Arjun/Jonas separately about gating v3 for A100 with a caveat in the profile metadata.\n\nSofia Ivanova: thanks. also: can we add a regression test that enabling a profile with max_context=8192 doesn’t change decode kernel path for seqlen=4096? feels like exactly what we missed.\n\nOmar El-Sayed: +1. I’ll add a small unit/integration test around KV config selection.\n\nHiro Tanaka: cool. posting numbers for record:\n- baseline: 38.4ms/token p95 decode\n- v3 default: 52.1ms/token p95 decode (flash disabled)\n- v3 + KV_PAGE_SIZE=128: 39.2ms/token p95 decode\n\nMateo Alvarez: (reacted :thumbsup:)","text":"Hiro Tanaka: seeing a decode p95 regression when enabling the new INT8 profile on llama3.1-70b-instruct variant x1. baseline p95 decode ~38ms/token, INT8 profile jumps to ~52ms/token on A100-80GB. encode looks fine. anyone else seeing this?\n\nNoah Patel: which profile id? and what runtime build?\n\nHiro Tanaka: profile ref `qpl:llama3.1-70b-x1:int8_w8a16:2025-02-19.3` (aka v3). runtime `runtime@b7f4c2a` from yesterday. route is pinned, no other changes.\n\nSofia Ivanova: is it only when continuous batching kicks in? (i.e. queue depth > 1)\n\nHiro Tanaka: happens even at qd=1. perf suite run `decode_only` shows it.\n\nOmar El-Sayed: any chance we’re invalidating KV cache layout due to different pack/unpack? INT8 weights shouldn’t touch KV but we do some alignment changes for fused kernels.\n\nHiro Tanaka: grabbed nsight systems trace. looks like we’re not taking the fused attention kernel on decode; falling back to a slower path.\n\nNoah Patel: can you paste the kernel names / top hotspots?\n\nHiro Tanaka: yep. from nsys summary (decode step, batch=1, seqlen=4096, new_tokens=64):\n```\nTime(%)  Total Time (ns)  Instances  Avg (ns)    Name\n46.8     2.13e+09         64         3.33e+07    void at::native::scaled_dot_product_attention...\n21.4     9.74e+08         128        7.61e+06    void cutlass::Kernel<...> gemm_f16...\n12.2     5.55e+08         64         8.67e+06    void kv_cache::gather_scatter<<<...>>>\n7.9      3.61e+08         64         5.64e+06    void layernorm<<<...>>>\n```\nBaseline run shows attention as `flash_fwd_splitkv` at ~22% instead of 46%.\n\nGrace Kim: that suggests kernel selection is flipping. do we have a guard that disables flash attention when int8 weights enabled? (i vaguely remember a conservative check in pr-18479)\n\nHiro Tanaka: I checked logs with `RUNTIME_LOG_LEVEL=debug`, seeing:\n`kernel_select: flash_attn=DISABLED reason=unsupported_combo(w8a16,int8_matmul)`\nThat seems… wrong? We shouldn’t tie attention kernel to weight GEMM mode.\n\nNoah Patel: yeah, attention is independent. the guard might be too broad: “if quantized then disable flash” type thing.\n\nSofia Ivanova: is this only for v3 profile? what about v2?\n\nHiro Tanaka: v2 profile (`...:2025-02-12.1`) is fine. v3 is the one that regresses.\n\nJonas Weber: perf-canary run from last night flagged same thing on A100, didn’t repro on H100. (reacted :eyes:)\n\nOmar El-Sayed: H100 path uses different attention backend; might hide it.\n\nGrace Kim: v3 changed anything besides weights? e.g. activation dtype or smoothquant params? if it flipped act to fp16 vs bf16 it could trip a capability check.\n\nHiro Tanaka: profile says w8a16, activations fp16 (same as v2), but it also sets `preferred_attention_backend=auto` and `max_context=8192`. v2 had `max_context=4096`.\n\nNoah Patel: ah. the “max_context” might be driving a conservative attention selection. maybe we disable flash if max_context > 4096 for that model family.\n\nSofia Ivanova: but your trace is seqlen=4096. even if max_context=8192, we should still allow flash at runtime when actual seqlen <= supported.\n\nMateo Alvarez: +1. selection should be based on actual request constraints, not profile max. unless the profile forces a different KV layout that breaks flash.\n\nOmar El-Sayed: KV layout: for >4k we allocate a different paged cache config (larger page size) if `max_context` is higher. That might cause the flash kernel to be disabled because it can’t handle that page size.\n\nHiro Tanaka: interesting. I saw `kv_cache.page_size=256` in the v3 run, baseline/v2 is `page_size=128`.\n\nOmar El-Sayed: there we go. our flash attention decode kernel only supports page_size=128 right now (and maybe 64). For 256 we fallback to sdpa.\n\nGrace Kim: can we clamp page_size based on actual seqlen? or change heuristic: don’t bump page_size just because profile advertises max_context.\n\nNoah Patel: we should also surface this as an explicit warning: “flash disabled due to kv_page_size=256” not “unsupported_combo(w8a16,int8_matmul)”. that error message is misleading.\n\nHiro Tanaka: agree. quick experiment: forced `KV_PAGE_SIZE=128` env var with v3 profile, decode p95 goes back to ~39ms/token.\n\nSelene Huang: any risk changing page size affects correctness? (i.e. address calculations)\n\nOmar El-Sayed: shouldn’t. page size is perf/memory tradeoff. correctness is same, just different indexing. But we need to ensure we don’t OOM for 8k contexts if we keep 128.\n\nJonas Weber: for A100 80GB, 70B @ 8k is already tight; bumping page size was meant to reduce metadata overhead. maybe the right fix is “page_size=128 if seqlen<=4096 else 256” (or pick dynamically at allocation).\n\nSofia Ivanova: + make it deterministic across replicas (same request -> same selection). but that’s fine if it’s purely a function of request seqlen + model.\n\nMateo Alvarez: action items:\n1) fix kernel_select log reason (don’t blame int8)\n2) adjust KV cache page sizing heuristic to be request-aware so we don’t disable flash for 4k runs\n3) add perf-canary coverage specifically for “profile max_context > request seqlen” case\n\nHiro Tanaka: I can open a quick PR for #1/#2 unless Omar wants it.\n\nOmar El-Sayed: I’ll take #2 (KV sizing). Hiro can do #1 (log reason + guard). Let’s coordinate to avoid conflicts.\n\nNoah Patel: can someone paste the relevant config code path? I think it’s in `kv_cache/config.rs`.\n\nOmar El-Sayed: it’s in `KvCacheConfig::from_profile()` currently:\n```\nif profile.max_context >= 8192 {\n  page_size = 256;\n}\n```\nWe should move it to `KvCacheConfig::from_request(model, seqlen, profile)`.\n\nGrace Kim: also update compatibility matrix docs? customers might ask why “INT8 made it slower” when it’s actually KV paging.\n\nHiro Tanaka: ok, I’ll add a metric tag too: `attention_backend=flash|sdpa` and `kv_page_size` in runtime span logs (if not already). helps with future triage.\n\nJonas Weber: please do. perf suite can then assert “flash still enabled” on the 4k decode benchmark.\n\nMateo Alvarez: keep me in the loop, this is close to beta. we should consider temporarily blocking v3 publication for A100 until fix lands (or mark as caveat).\n\nHiro Tanaka: ack. I’ll ping Arjun/Jonas separately about gating v3 for A100 with a caveat in the profile metadata.\n\nSofia Ivanova: thanks. also: can we add a regression test that enabling a profile with max_context=8192 doesn’t change decode kernel path for seqlen=4096? feels like exactly what we missed.\n\nOmar El-Sayed: +1. I’ll add a small unit/integration test around KV config selection.\n\nHiro Tanaka: cool. posting numbers for record:\n- baseline: 38.4ms/token p95 decode\n- v3 default: 52.1ms/token p95 decode (flash disabled)\n- v3 + KV_PAGE_SIZE=128: 39.2ms/token p95 decode\n\nMateo Alvarez: (reacted :thumbsup:)","occurredAt":1740149786000,"entityType":"thread","routeKey":"apps/slack/channels/eng-runtime","sourcePath":"sources/slack/eng-runtime/1740144420.json","peopleIds":["grace-kim","hiro-tanaka","jonas-weber","mateo-alvarez","noah-patel","omar-el-sayed","selene-huang","sofia-ivanova"],"links":[],"tags":["eng-runtime"]},{"id":"slack:1739973309","app":"slack","title":"#eng-runtime: 1739973309","preview":"","text":"","occurredAt":1739982146000,"entityType":"thread","routeKey":"apps/slack/channels/eng-runtime","sourcePath":"sources/slack/eng-runtime/1739973309-watchdog-default-timeout-values.json","peopleIds":["aisha-bello","connor-o-brien","grace-kim","hiro-tanaka","julian-moore","marcus-reed","marissa-cole","noah-patel","omar-el-sayed","rafael-mendes","sofia-ivanova","tomas-novak","yuki-sato"],"links":[],"tags":["eng-runtime"]},{"id":"slack:1739897721","app":"slack","title":"#eng-runtime: 1739897721","preview":"","text":"","occurredAt":1739902146000,"entityType":"thread","routeKey":"apps/slack/channels/eng-runtime","sourcePath":"sources/slack/eng-runtime/1739897721-tp-heartbeat-metrics-naming.json","peopleIds":["aisha-bello","allison-grant","caleb-johnson","julian-moore","logan-wright","noah-patel","omar-el-sayed","rafael-mendes","sergio-costa"],"links":[],"tags":["eng-runtime"]},{"id":"slack:1739824101","app":"slack","title":"#eng-runtime: 1739824101","preview":"Omar El-Sayed: Need to settle the hit/miss definitions today so Samir can finish counters + Nadia can lock schema. Main confusion: KV cache hit vs prefix cache hit, and what “partial reuse” means esp multi-turn chat.\n\nSamir Qureshi: +1. Implementation wise I can emit (a) tokens reused (skipped compute) and (b) tokens eligible. But people keep asking for “request hit rate” which gets weird w partial.\n\nNoah Patel: Let’s be super explicit about numerator/denominator and *stage*. KV cache is about reuse across turns within a session (or same route) at decode-time, but prefix cache is prefill-time reuse of prompt prefix. If we mix the two it’ll be garbage.\n\nSofia Ivanova: Also continuous batching complicates attribution. Prefill for multiple reqs in one batch, we need per-request counts. We can’t have “hit” mean “batch-level cache was warm”.\n\nOmar El-Sayed: Proposal:\n- Prefix cache metrics computed at prefill stage.\n- KV cache metrics computed during decode stage.\nBut question: for prefill, is “hit” measured as prompt tokens reused (prefix reuse depth) / prompt tokens requested? Or only eligible tokens (excluding system prompt? tool schema? etc.)\n\nNadia Rahman: From schema/cardinality side: want stable semantic: “eligible_prefill_tokens” is whatever runtime attempted to prefill (after canonicalization / truncation). Don’t try to carve out “system vs user” in metrics.\n\nAllison Grant: For dashboards, SRE wants to see “why did GPU cost spike”: prefill compute went up because prefix reuse went down OR decode got more expensive because KV reuse went down. So split is good.\n\nSamir Qureshi: KV cache doesn’t really “reuse tokens” in decode; it reuses KV tensors for prior context tokens so each new token decode avoids recomputing attention over previous tokens. So the natural unit is “context tokens served from cache” per decode step.\n\nNoah Patel: Right: in decode step t, context length = L. If KV cache has entries for L tokens, it’s a hit. If not, we pay a prefill-like recompute / rebuild KV.\n\nOmar El-Sayed: This is where partial comes in: you can have KV for first K tokens but missing tail due to eviction/version mismatch. Do we count partial as hit?\n\nSofia Ivanova: We should. Otherwise every eviction makes hit-rate binary and noisy. But we need two metrics: “full hit” vs “reuse fraction”.\n\nMarcus Reed: Perf view: I care about “wasted prefill” too: tokens we prefilled that didn’t contribute to subsequent decode reuse (due to miss/evict). Partial reuse should correlate with wasted prefill.\n\nAllison Grant: For alerting, a single scalar is still useful. But it has to be stable. We can alert on drop in “reused_prefill_tokens / total_prefill_tokens” and separately on “kv_reuse_fraction”.\n\nNadia Rahman: Naming from standards doc: I’d like token-based ratios, not request-based. Request-based is workload-shape dependent and hard to compare across prompt buckets.\n\nSamir Qureshi: So for prefix cache:\n- total_prefill_tokens = prompt tokens we would have computed in prefill\n- prefix_reused_tokens = tokens loaded from prefix cache (i.e., skip compute)\n- prefix_reuse_ratio = prefix_reused_tokens / total_prefill_tokens\nAnd optional histogram on reuse depth.\n\nNoah Patel: Careful: “total_prefill_tokens” includes tokens that are *not cacheable*? Like if caching disabled for route.\n\nOmar El-Sayed: Good point. Denominator should be “cache_eligible_prefill_tokens”. Then if caching disabled, we don’t tank hit-rate, we emit misses by reason.\n\nNadia Rahman: Yes. Emit “eligible” and then “miss_reason” on the misses. That’s more actionable.\n\nAllison Grant: +1 please. Otherwise dashboard says 0% hit-rate and oncall spends 2h only to find someone flipped policy off.\n\nSamir Qureshi: OK so prefix cache:\n- eligible_prefill_tokens\n- reused_prefill_tokens\n- missed_prefill_tokens = eligible - reused\nand miss_reason breakdown: policy_disabled, ttl_expired, capacity, hash_mismatch, version_mismatch (do we need all?)\n\nOmar El-Sayed: On miss taxonomy: keep it small/stable. I’m fine with those 5.\n\nSofia Ivanova: Where does “evicted” land? That’s capacity, right? We can have eviction metrics separately.\n\nNoah Patel: For KV cache, define:\n- kv_context_tokens_needed: how many context tokens decode expects to have cached (post any truncation)\n- kv_context_tokens_present: tokens actually present in KV cache\n- kv_reuse_ratio = present / needed\nPlus a counter for “kv_miss_event” when present < needed, with miss_reason.\n\nSamir Qureshi: Implementation detail: “needed” can change within a request because of sliding window / max_seq? but our runtime uses fixed context window per request. I can compute per decode step, but that’s heavy.\n\nMarcus Reed: Please don’t do per-step metrics in prod, overhead will be awful. Aggregate per request: evaluate at first decode step only.\n\nOmar El-Sayed: Agree: define KV reuse at boundary between prefill->decode: before first decode token, how many KV entries are already there vs needed for context. That’s the moment that matters for latency spikes.\n\nNoah Patel: This also handles multi-turn chat: turn N starts with some existing cache. We measure reuse at turn start.\n\nAllison Grant: How do we treat streaming where client cancels mid-generation? Does that affect denominators?\n\nSamir Qureshi: For prefix cache, prefill already happened, so it’s counted. For KV reuse, we measure before first decode, so cancellation after doesn’t matter. That’s fine.\n\nSofia Ivanova: Retries though… gateway can retry streaming on transient. We can double count unless we de-dupe by request_id.\n\nOmar El-Sayed: We are *not* logging raw request_id as a label. But runtime can guard by “only emit once per logical request” using internal attempt counter.\n\nNadia Rahman: +1. No high-card labels. If you need debugging, annotate a trace span not metric label.\n\nAllison Grant: Exactly. Metrics: low-card. Traces: per-request.\n\nMarcus Reed: So what is “hit-rate” in English in ADR? People will ask.\n\nNoah Patel: I’d write:\n- Prefix cache hit-rate: fraction of cache-eligible prefill tokens served from prefix cache.\n- KV cache hit-rate (reuse effectiveness): fraction of context tokens already present in KV cache at the start of decoding for a request/turn.\nAnd define “partial hit” explicitly as ratio in [0,1].\n\nSamir Qureshi: Do we still need a boolean “full_hit” metric?\n\nAllison Grant: For dashboards, nice to have “full hit %” as a separate panel. But don’t call it “hit-rate”.\n\nNadia Rahman: If we do it, make it a counter: full_hit_requests_total and eligible_requests_total, ratio derived in query. But careful with workload mix changes.\n\nOmar El-Sayed: Let’s keep request-level as optional. Primary is token-based. Request-based can be added later if SRE insists.\n\nSofia Ivanova: Re: multi-turn attribution: if a chat has 10 turns, each turn emits its own prefill metrics and kv reuse metrics. That’s fine.\n\nNoah Patel: One nuance: prefix cache could be “warm” because last turn shares a lot of prefix. But if prompts include timestamps / random IDs, hash_mismatch will dominate. That’s the actionable bit.\n\nAllison Grant: That’s why miss_reason needs hash_mismatch. We’ll point customers at canonicalization.\n\nMarcus Reed: Also “version_mismatch”: model rollout invalidates cached tensors. We need this to catch SUP-3912 style regressions.\n\nNadia Rahman: OK then.\nAction items:\n- Omar: update ADR text w token-based definitions + partial semantics + stage boundaries.\n- Samir: implement eligible/reused counters and KV reuse ratio as per-turn at first decode.\n- Sofia: sanity check in continuous batching path for per-request attribution.\n- Allison: dashboard definitions: separate panels for prefix vs KV, avoid single blended “cache hit-rate”.\n\nMateo Alvarez: +1 to this direction. Keep it simple and stable. Token-based primary metric, request-based optional. Also please add a small “how to interpret” blurb in the internal readme so people stop equating KV and prefix.\n\nOmar El-Sayed: Cool. I’ll post ADR draft in #architecture for final review by EOD. :thumbsup:\n\nSamir Qureshi: I’ll adjust the enum to those 5 miss reasons and wire the denominator as eligible tokens. Will ping if I hit ambiguity around ttl_expired vs capacity.\n\nAllison Grant: When you have the metric names, drop them here so I can start panel queries.\n\nNadia Rahman: Please use the naming from standards: `cache_prefix_*` and `cache_kv_*` prefixes (and no “hit_rate” suffix for the raw counters).\n\nOmar El-Sayed: Ack. Will align names with Nadia’s doc once I paste the final list.\n\nSofia Ivanova: Also flag: scheduler can cause locality changes; not a miss reason but shows up as hit drop. Might want a tag for region/pool (low-card) so we can see it.\n\nNadia Rahman: Region is allowed. Pool is tricky (cardinality). Let’s stick to deployment_mode + region + model + route_id allowlist.\n\nAllison Grant: Works for SRE. Pool is too granular anyway.\n\nOmar El-Sayed: Closing loop: definitions above are the ones we’ll implement unless objections in the next hour.","text":"Omar El-Sayed: Need to settle the hit/miss definitions today so Samir can finish counters + Nadia can lock schema. Main confusion: KV cache hit vs prefix cache hit, and what “partial reuse” means esp multi-turn chat.\n\nSamir Qureshi: +1. Implementation wise I can emit (a) tokens reused (skipped compute) and (b) tokens eligible. But people keep asking for “request hit rate” which gets weird w partial.\n\nNoah Patel: Let’s be super explicit about numerator/denominator and *stage*. KV cache is about reuse across turns within a session (or same route) at decode-time, but prefix cache is prefill-time reuse of prompt prefix. If we mix the two it’ll be garbage.\n\nSofia Ivanova: Also continuous batching complicates attribution. Prefill for multiple reqs in one batch, we need per-request counts. We can’t have “hit” mean “batch-level cache was warm”.\n\nOmar El-Sayed: Proposal:\n- Prefix cache metrics computed at prefill stage.\n- KV cache metrics computed during decode stage.\nBut question: for prefill, is “hit” measured as prompt tokens reused (prefix reuse depth) / prompt tokens requested? Or only eligible tokens (excluding system prompt? tool schema? etc.)\n\nNadia Rahman: From schema/cardinality side: want stable semantic: “eligible_prefill_tokens” is whatever runtime attempted to prefill (after canonicalization / truncation). Don’t try to carve out “system vs user” in metrics.\n\nAllison Grant: For dashboards, SRE wants to see “why did GPU cost spike”: prefill compute went up because prefix reuse went down OR decode got more expensive because KV reuse went down. So split is good.\n\nSamir Qureshi: KV cache doesn’t really “reuse tokens” in decode; it reuses KV tensors for prior context tokens so each new token decode avoids recomputing attention over previous tokens. So the natural unit is “context tokens served from cache” per decode step.\n\nNoah Patel: Right: in decode step t, context length = L. If KV cache has entries for L tokens, it’s a hit. If not, we pay a prefill-like recompute / rebuild KV.\n\nOmar El-Sayed: This is where partial comes in: you can have KV for first K tokens but missing tail due to eviction/version mismatch. Do we count partial as hit?\n\nSofia Ivanova: We should. Otherwise every eviction makes hit-rate binary and noisy. But we need two metrics: “full hit” vs “reuse fraction”.\n\nMarcus Reed: Perf view: I care about “wasted prefill” too: tokens we prefilled that didn’t contribute to subsequent decode reuse (due to miss/evict). Partial reuse should correlate with wasted prefill.\n\nAllison Grant: For alerting, a single scalar is still useful. But it has to be stable. We can alert on drop in “reused_prefill_tokens / total_prefill_tokens” and separately on “kv_reuse_fraction”.\n\nNadia Rahman: Naming from standards doc: I’d like token-based ratios, not request-based. Request-based is workload-shape dependent and hard to compare across prompt buckets.\n\nSamir Qureshi: So for prefix cache:\n- total_prefill_tokens = prompt tokens we would have computed in prefill\n- prefix_reused_tokens = tokens loaded from prefix cache (i.e., skip compute)\n- prefix_reuse_ratio = prefix_reused_tokens / total_prefill_tokens\nAnd optional histogram on reuse depth.\n\nNoah Patel: Careful: “total_prefill_tokens” includes tokens that are *not cacheable*? Like if caching disabled for route.\n\nOmar El-Sayed: Good point. Denominator should be “cache_eligible_prefill_tokens”. Then if caching disabled, we don’t tank hit-rate, we emit misses by reason.\n\nNadia Rahman: Yes. Emit “eligible” and then “miss_reason” on the misses. That’s more actionable.\n\nAllison Grant: +1 please. Otherwise dashboard says 0% hit-rate and oncall spends 2h only to find someone flipped policy off.\n\nSamir Qureshi: OK so prefix cache:\n- eligible_prefill_tokens\n- reused_prefill_tokens\n- missed_prefill_tokens = eligible - reused\nand miss_reason breakdown: policy_disabled, ttl_expired, capacity, hash_mismatch, version_mismatch (do we need all?)\n\nOmar El-Sayed: On miss taxonomy: keep it small/stable. I’m fine with those 5.\n\nSofia Ivanova: Where does “evicted” land? That’s capacity, right? We can have eviction metrics separately.\n\nNoah Patel: For KV cache, define:\n- kv_context_tokens_needed: how many context tokens decode expects to have cached (post any truncation)\n- kv_context_tokens_present: tokens actually present in KV cache\n- kv_reuse_ratio = present / needed\nPlus a counter for “kv_miss_event” when present < needed, with miss_reason.\n\nSamir Qureshi: Implementation detail: “needed” can change within a request because of sliding window / max_seq? but our runtime uses fixed context window per request. I can compute per decode step, but that’s heavy.\n\nMarcus Reed: Please don’t do per-step metrics in prod, overhead will be awful. Aggregate per request: evaluate at first decode step only.\n\nOmar El-Sayed: Agree: define KV reuse at boundary between prefill->decode: before first decode token, how many KV entries are already there vs needed for context. That’s the moment that matters for latency spikes.\n\nNoah Patel: This also handles multi-turn chat: turn N starts with some existing cache. We measure reuse at turn start.\n\nAllison Grant: How do we treat streaming where client cancels mid-generation? Does that affect denominators?\n\nSamir Qureshi: For prefix cache, prefill already happened, so it’s counted. For KV reuse, we measure before first decode, so cancellation after doesn’t matter. That’s fine.\n\nSofia Ivanova: Retries though… gateway can retry streaming on transient. We can double count unless we de-dupe by request_id.\n\nOmar El-Sayed: We are *not* logging raw request_id as a label. But runtime can guard by “only emit once per logical request” using internal attempt counter.\n\nNadia Rahman: +1. No high-card labels. If you need debugging, annotate a trace span not metric label.\n\nAllison Grant: Exactly. Metrics: low-card. Traces: per-request.\n\nMarcus Reed: So what is “hit-rate” in English in ADR? People will ask.\n\nNoah Patel: I’d write:\n- Prefix cache hit-rate: fraction of cache-eligible prefill tokens served from prefix cache.\n- KV cache hit-rate (reuse effectiveness): fraction of context tokens already present in KV cache at the start of decoding for a request/turn.\nAnd define “partial hit” explicitly as ratio in [0,1].\n\nSamir Qureshi: Do we still need a boolean “full_hit” metric?\n\nAllison Grant: For dashboards, nice to have “full hit %” as a separate panel. But don’t call it “hit-rate”.\n\nNadia Rahman: If we do it, make it a counter: full_hit_requests_total and eligible_requests_total, ratio derived in query. But careful with workload mix changes.\n\nOmar El-Sayed: Let’s keep request-level as optional. Primary is token-based. Request-based can be added later if SRE insists.\n\nSofia Ivanova: Re: multi-turn attribution: if a chat has 10 turns, each turn emits its own prefill metrics and kv reuse metrics. That’s fine.\n\nNoah Patel: One nuance: prefix cache could be “warm” because last turn shares a lot of prefix. But if prompts include timestamps / random IDs, hash_mismatch will dominate. That’s the actionable bit.\n\nAllison Grant: That’s why miss_reason needs hash_mismatch. We’ll point customers at canonicalization.\n\nMarcus Reed: Also “version_mismatch”: model rollout invalidates cached tensors. We need this to catch SUP-3912 style regressions.\n\nNadia Rahman: OK then.\nAction items:\n- Omar: update ADR text w token-based definitions + partial semantics + stage boundaries.\n- Samir: implement eligible/reused counters and KV reuse ratio as per-turn at first decode.\n- Sofia: sanity check in continuous batching path for per-request attribution.\n- Allison: dashboard definitions: separate panels for prefix vs KV, avoid single blended “cache hit-rate”.\n\nMateo Alvarez: +1 to this direction. Keep it simple and stable. Token-based primary metric, request-based optional. Also please add a small “how to interpret” blurb in the internal readme so people stop equating KV and prefix.\n\nOmar El-Sayed: Cool. I’ll post ADR draft in #architecture for final review by EOD. :thumbsup:\n\nSamir Qureshi: I’ll adjust the enum to those 5 miss reasons and wire the denominator as eligible tokens. Will ping if I hit ambiguity around ttl_expired vs capacity.\n\nAllison Grant: When you have the metric names, drop them here so I can start panel queries.\n\nNadia Rahman: Please use the naming from standards: `cache_prefix_*` and `cache_kv_*` prefixes (and no “hit_rate” suffix for the raw counters).\n\nOmar El-Sayed: Ack. Will align names with Nadia’s doc once I paste the final list.\n\nSofia Ivanova: Also flag: scheduler can cause locality changes; not a miss reason but shows up as hit drop. Might want a tag for region/pool (low-card) so we can see it.\n\nNadia Rahman: Region is allowed. Pool is tricky (cardinality). Let’s stick to deployment_mode + region + model + route_id allowlist.\n\nAllison Grant: Works for SRE. Pool is too granular anyway.\n\nOmar El-Sayed: Closing loop: definitions above are the ones we’ll implement unless objections in the next hour.","occurredAt":1739832479000,"entityType":"thread","routeKey":"apps/slack/channels/eng-runtime","sourcePath":"sources/slack/eng-runtime/1739824101-cache-hit-rate-metric-definition-thread.json","peopleIds":["allison-grant","julian-moore","marcus-reed","mateo-alvarez","nadia-rahman","noah-patel","omar-el-sayed","samir-qureshi","sofia-ivanova"],"links":[],"tags":["eng-runtime"]},{"id":"slack:1739568844","app":"slack","title":"#eng-runtime: 1739568844","preview":"","text":"","occurredAt":1739576521000,"entityType":"thread","routeKey":"apps/slack/channels/eng-runtime","sourcePath":"sources/slack/eng-runtime/1739568844.json","peopleIds":["allison-grant","elena-popov","ethan-park","jordan-lee","logan-wright","marcus-lin","mina-haddad","nikhil-sharma","ravi-menon","sean-gallagher"],"links":[],"tags":["eng-runtime"]},{"id":"slack:1739390012","app":"slack","title":"#eng-runtime: 1739390012","preview":"Noah Patel: Heads up from last night’s rollback drill: we saw a noticeable p95/p99 spike right after rollback when we invalidate caches. Concern: rollback is “fix the regression” but we might temporarily make latency worse.\n\nNoah Patel: Specifically: rollback flips optimization config version, runtime applies + does cache invalidation (prefix/KV + some routing caches). Workers drop hot prefixes and we get a burst of cold prefill.\n\nOmar El-Sayed: yep, that matches what I’ve been seeing in local profiling. invalidation is effectively forcing prefill on first N requests. if rollback happens under load, we get queueing + p99 balloons.\n\nSofia Ivanova: do we have numbers from the gameday? how bad was the spike + how long did it last\n\nNoah Patel: from the drill: steady-state p95 ~240ms, during rollback window p95 ~410ms for ~2-3 mins. p99 went from ~900ms to ~2.6s at peak, then recovered. errors didn’t move much.\n\nSean Gallagher: from SRE POV this is a problem because the rollback itself can trip the latency gate + paging. We need to annotate dashboards as “rollback in progress” but also try to keep the spike bounded.\n\nAllison Grant: +1. also note: our latency SLO gate eval is currently blind to “expected disturbance” windows unless we explicitly mark the rollout controller state.\n\nOmar El-Sayed: what’s being invalidated today on rollback?\n\nNoah Patel: currently:\n- `PrefixCache` entries keyed by (model, tenant, normalized_prompt_prefix)\n- `KvCacheManager` reuse pool per worker\n- “route hint” cache (model variant selection / kernel profile)\n\nNoah Patel: reason was correctness: new config can change batching/caching knobs, and quant profiles can change tokenization-ish behavior / logits quality, so we wanted to avoid mixed assumptions.\n\nSofia Ivanova: for batching heuristics rollback, do we actually need to drop prefix cache? feels like it’s safe? prefixes are content-based, not dependent on batching\n\nOmar El-Sayed: prefix cache is “safe” if it’s just storing KV for exact same prefix and same model weights. but if quant profile changes, the KV state differs (weights different). using KV from v_new with v_old weights would be wrong.\n\nNoah Patel: right. but in most rollbacks we’re rolling back batching/caching knobs without changing weights/quant. the question: can we do conditional invalidation based on what fields changed.\n\nBenji Okafor: (from control plane side) diff engine already classifies “risky fields”. we could add a runtime hint like `requires_cache_flush: true/false` derived from semantic validation. would that help?\n\nNoah Patel: yes, that’s one mitigation: only flush what you must.\n\nAllison Grant: can we enumerate which changes require which flush? that also helps observability (“rollback type: warm cache safe”)\n\nOmar El-Sayed: proposal:\n1) quant_profile_id change => must flush PrefixCache + KV reuse pool (and maybe force a worker restart if we can’t guarantee no stale)\n2) cache_enabled toggle or cache_max_bytes changes => do NOT flush immediately; instead, let entries expire naturally + stop admitting new entries if disabled\n3) batching params (max_batch_tokens, target_latency_ms, etc) => no cache flush needed\n\nSofia Ivanova: +1 on (3). batching rollback should be mostly scheduler-side; cache should be independent.\n\nSean Gallagher: what about “route hint cache”? if rollback changes kernel selection thresholds, could stale hints cause a bad kernel pick?\n\nNoah Patel: route hint cache is mostly a perf optimization. stale hints are annoying but not correctness-breaking; we can keep them + revalidate on hit. we should change that code path.\n\nNoah Patel: today it’s “trust cache”. we can make it “trust but verify”: if cached hint points to kernel/profile not compatible with current config, recompute.\n\nOmar El-Sayed: that would avoid a mass invalidate too.\n\nperf-canary-bot: perf-canary (rollback-drill) summary\n- window: 10m\n- p95: 242ms -> 412ms (max)\n- p99: 0.91s -> 2.63s (max)\n- queue_depth: 0.7 -> 4.8\n- config_version header: v58 -> v57 at t+03:12\nlink: https://perf-canary.redwood/run/rollback-drill-2025-02-12\n\nSofia Ivanova: okay spike correlates exactly with version flip\n\nNoah Patel: another mitigation: staggered cache invalidation across workers. today rollback controller flips version for the whole scope, and each worker receives new config + flushes immediately. so we get synchronized cold start.\n\nOmar El-Sayed: can we random-jitter the flush (or stage apply across shards) even during rollback?\n\nSean Gallagher: rollback is expected to be “fast”. but “fast” doesn’t mean “synchronized.” if we can do 30-60s stagger and still mitigate regression, that might be a net win.\n\nConnor O'Brien: Release Eng note: if we add jitter/stagger, make sure rollback API semantics are clear. today people assume “rollback completed” means all workers are on old version. if we do stagger, we need to define “rollback initiated” vs “rollback converged”.\n\nBenji Okafor: control plane can expose status per shard/worker group, but Console currently shows a single state. We can add “converging” state and ETA.\n\nAllison Grant: for alerting: we can gate on “rollback converged” before clearing incident checklists, but also suppress latency gating while state == converging (with limits so we don’t mask real issues).\n\nNoah Patel: I like:\n- Phase 1: change config pointer immediately (so new requests start targeting old version)\n- Phase 2: worker apply + cache actions are staggered\n\nOmar El-Sayed: careful: if pointer flips, workers that haven’t applied yet are still executing with old/new mismatch? depends on whether config is pulled per request or cached\n\nNoah Patel: right now workers poll + cache config for 30s and also get push notifications. on push they apply.\n\nNoah Patel: we can implement “apply token” (epoch) and have request path include required epoch. if worker is behind, it either self-updates or rejects/redirects.\n\nSean Gallagher: reject would cause 5xx; redirect would cause retries. both bad in rollback.\n\nOmar El-Sayed: alternative: keep config read-through on request boundaries (cheap) and apply only when safe, but that’s more invasive.\n\nSofia Ivanova: could we instead keep the immediate apply but make cache flush lazy?\n\nOmar El-Sayed: lazy flush idea:\n- keep existing PrefixCache entries but tag with `config_epoch`\n- on lookup, only use entries matching current epoch; otherwise treat as miss\n- in background, evict old-epoch entries gradually\n\nNoah Patel: that’s good: avoids a “flush storm” and still prevents correctness issues.\n\nAllison Grant: from metrics, the pain is the synchronized cold prefill. lazy eviction would smooth the curve.\n\nSean Gallagher: would memory blow up (keeping 2 epochs) during rollback?\n\nOmar El-Sayed: worst case yes. but rollback is rare and we can cap: allow at most 2 epochs; when new epoch arrives, start evicting oldest epoch at fixed rate (e.g., X MB/s) until under budget.\n\nNoah Patel: for quant rollbacks, do we need stricter behavior? we can’t reuse KV across weight changes.\n\nOmar El-Sayed: with epoch tagging, KV entries from old weights wouldn’t be used after rollback (epoch mismatch). so correctness ok, but memory overhead may be higher. also performance will still be “cold,” but at least not a synchronized flush event.\n\nSofia Ivanova: but after rollback, we *want* old epoch entries (the rolled-back version) to be hot again. tagging helps: the entries that match rollback epoch might still exist if we didn’t flush them previously.\n\nNoah Patel: good point. today we flush everything, losing the chance to reuse old cache.\n\nBenji Okafor: can we store both epochs when we canary? so if we roll back, old epoch cache is still there.\n\nNoah Patel: yes. that’s the ideal. rollback should be “go back to known-good” including warmed caches if possible.\n\nSean Gallagher: operational requirement: rollback should not increase p95 by >25% for more than 2 minutes (rough target). can we hit that with lazy eviction + stagger?\n\nAllison Grant: we can add a panel specifically for “rollback disturbance window” and measure. but please also add a metric: `cache_epoch_active` and `cache_epoch_evictions_total` tagged by worker group.\n\nNoah Patel: action items I’m proposing:\n1) Change cache invalidation from “flush now” to epoch-based eligibility (lazy eviction) for PrefixCache and KV reuse pool.\n2) Make route hint cache verify compatibility rather than invalidate globally.\n3) Add optional stagger/jitter for any remaining “must flush” operations.\n4) Update rollback API/Console status wording: initiated vs converged.\n\nOmar El-Sayed: +1. I can take (1) for PrefixCache + KV pool tagging.\n\nSofia Ivanova: I’ll take (2) for route hints + compat check (scheduler-owned).\n\nAllison Grant: I’ll draft the metrics + dashboard annotation requirements and send to #eng-sre.\n\nSean Gallagher: pls also update runbook language: “expect small transient latency increase; if sustained > X, pause further changes”\n\nConnor O'Brien: once you have a PR up, tag me. we’ll need to coordinate runtime release train so it lands before limited GA.\n\nNoah Patel: cool. I’ll write up a short design note and link it here. also will sanity-check with Paula/Console team on the “converged” status naming.\n\nOmar El-Sayed: dropping a quick sketch:\n```\n// on config update\ncurrent_epoch = cfg.version_epoch\n\n// cache get\nif entry.epoch != current_epoch: miss\n\n// background\nevict(epoch != current_epoch) at rate_limit\n```\n\nNoah Patel: perfect. also: let’s make sure rollback doesn’t trigger a thundering herd of background eviction threads. one per worker, low-priority.\n\nSean Gallagher: :thumbsup: \n\nAllison Grant: :eyes: on metrics tagging: include `config_version` and `rollback_id` if available\n\nNoah Patel: yep. we can propagate rollback_id from control plane event to runtime logs/metrics.\n\nNoah Patel: will follow up with PR links.","text":"Noah Patel: Heads up from last night’s rollback drill: we saw a noticeable p95/p99 spike right after rollback when we invalidate caches. Concern: rollback is “fix the regression” but we might temporarily make latency worse.\n\nNoah Patel: Specifically: rollback flips optimization config version, runtime applies + does cache invalidation (prefix/KV + some routing caches). Workers drop hot prefixes and we get a burst of cold prefill.\n\nOmar El-Sayed: yep, that matches what I’ve been seeing in local profiling. invalidation is effectively forcing prefill on first N requests. if rollback happens under load, we get queueing + p99 balloons.\n\nSofia Ivanova: do we have numbers from the gameday? how bad was the spike + how long did it last\n\nNoah Patel: from the drill: steady-state p95 ~240ms, during rollback window p95 ~410ms for ~2-3 mins. p99 went from ~900ms to ~2.6s at peak, then recovered. errors didn’t move much.\n\nSean Gallagher: from SRE POV this is a problem because the rollback itself can trip the latency gate + paging. We need to annotate dashboards as “rollback in progress” but also try to keep the spike bounded.\n\nAllison Grant: +1. also note: our latency SLO gate eval is currently blind to “expected disturbance” windows unless we explicitly mark the rollout controller state.\n\nOmar El-Sayed: what’s being invalidated today on rollback?\n\nNoah Patel: currently:\n- `PrefixCache` entries keyed by (model, tenant, normalized_prompt_prefix)\n- `KvCacheManager` reuse pool per worker\n- “route hint” cache (model variant selection / kernel profile)\n\nNoah Patel: reason was correctness: new config can change batching/caching knobs, and quant profiles can change tokenization-ish behavior / logits quality, so we wanted to avoid mixed assumptions.\n\nSofia Ivanova: for batching heuristics rollback, do we actually need to drop prefix cache? feels like it’s safe? prefixes are content-based, not dependent on batching\n\nOmar El-Sayed: prefix cache is “safe” if it’s just storing KV for exact same prefix and same model weights. but if quant profile changes, the KV state differs (weights different). using KV from v_new with v_old weights would be wrong.\n\nNoah Patel: right. but in most rollbacks we’re rolling back batching/caching knobs without changing weights/quant. the question: can we do conditional invalidation based on what fields changed.\n\nBenji Okafor: (from control plane side) diff engine already classifies “risky fields”. we could add a runtime hint like `requires_cache_flush: true/false` derived from semantic validation. would that help?\n\nNoah Patel: yes, that’s one mitigation: only flush what you must.\n\nAllison Grant: can we enumerate which changes require which flush? that also helps observability (“rollback type: warm cache safe”)\n\nOmar El-Sayed: proposal:\n1) quant_profile_id change => must flush PrefixCache + KV reuse pool (and maybe force a worker restart if we can’t guarantee no stale)\n2) cache_enabled toggle or cache_max_bytes changes => do NOT flush immediately; instead, let entries expire naturally + stop admitting new entries if disabled\n3) batching params (max_batch_tokens, target_latency_ms, etc) => no cache flush needed\n\nSofia Ivanova: +1 on (3). batching rollback should be mostly scheduler-side; cache should be independent.\n\nSean Gallagher: what about “route hint cache”? if rollback changes kernel selection thresholds, could stale hints cause a bad kernel pick?\n\nNoah Patel: route hint cache is mostly a perf optimization. stale hints are annoying but not correctness-breaking; we can keep them + revalidate on hit. we should change that code path.\n\nNoah Patel: today it’s “trust cache”. we can make it “trust but verify”: if cached hint points to kernel/profile not compatible with current config, recompute.\n\nOmar El-Sayed: that would avoid a mass invalidate too.\n\nperf-canary-bot: perf-canary (rollback-drill) summary\n- window: 10m\n- p95: 242ms -> 412ms (max)\n- p99: 0.91s -> 2.63s (max)\n- queue_depth: 0.7 -> 4.8\n- config_version header: v58 -> v57 at t+03:12\nlink: https://perf-canary.redwood/run/rollback-drill-2025-02-12\n\nSofia Ivanova: okay spike correlates exactly with version flip\n\nNoah Patel: another mitigation: staggered cache invalidation across workers. today rollback controller flips version for the whole scope, and each worker receives new config + flushes immediately. so we get synchronized cold start.\n\nOmar El-Sayed: can we random-jitter the flush (or stage apply across shards) even during rollback?\n\nSean Gallagher: rollback is expected to be “fast”. but “fast” doesn’t mean “synchronized.” if we can do 30-60s stagger and still mitigate regression, that might be a net win.\n\nConnor O'Brien: Release Eng note: if we add jitter/stagger, make sure rollback API semantics are clear. today people assume “rollback completed” means all workers are on old version. if we do stagger, we need to define “rollback initiated” vs “rollback converged”.\n\nBenji Okafor: control plane can expose status per shard/worker group, but Console currently shows a single state. We can add “converging” state and ETA.\n\nAllison Grant: for alerting: we can gate on “rollback converged” before clearing incident checklists, but also suppress latency gating while state == converging (with limits so we don’t mask real issues).\n\nNoah Patel: I like:\n- Phase 1: change config pointer immediately (so new requests start targeting old version)\n- Phase 2: worker apply + cache actions are staggered\n\nOmar El-Sayed: careful: if pointer flips, workers that haven’t applied yet are still executing with old/new mismatch? depends on whether config is pulled per request or cached\n\nNoah Patel: right now workers poll + cache config for 30s and also get push notifications. on push they apply.\n\nNoah Patel: we can implement “apply token” (epoch) and have request path include required epoch. if worker is behind, it either self-updates or rejects/redirects.\n\nSean Gallagher: reject would cause 5xx; redirect would cause retries. both bad in rollback.\n\nOmar El-Sayed: alternative: keep config read-through on request boundaries (cheap) and apply only when safe, but that’s more invasive.\n\nSofia Ivanova: could we instead keep the immediate apply but make cache flush lazy?\n\nOmar El-Sayed: lazy flush idea:\n- keep existing PrefixCache entries but tag with `config_epoch`\n- on lookup, only use entries matching current epoch; otherwise treat as miss\n- in background, evict old-epoch entries gradually\n\nNoah Patel: that’s good: avoids a “flush storm” and still prevents correctness issues.\n\nAllison Grant: from metrics, the pain is the synchronized cold prefill. lazy eviction would smooth the curve.\n\nSean Gallagher: would memory blow up (keeping 2 epochs) during rollback?\n\nOmar El-Sayed: worst case yes. but rollback is rare and we can cap: allow at most 2 epochs; when new epoch arrives, start evicting oldest epoch at fixed rate (e.g., X MB/s) until under budget.\n\nNoah Patel: for quant rollbacks, do we need stricter behavior? we can’t reuse KV across weight changes.\n\nOmar El-Sayed: with epoch tagging, KV entries from old weights wouldn’t be used after rollback (epoch mismatch). so correctness ok, but memory overhead may be higher. also performance will still be “cold,” but at least not a synchronized flush event.\n\nSofia Ivanova: but after rollback, we *want* old epoch entries (the rolled-back version) to be hot again. tagging helps: the entries that match rollback epoch might still exist if we didn’t flush them previously.\n\nNoah Patel: good point. today we flush everything, losing the chance to reuse old cache.\n\nBenji Okafor: can we store both epochs when we canary? so if we roll back, old epoch cache is still there.\n\nNoah Patel: yes. that’s the ideal. rollback should be “go back to known-good” including warmed caches if possible.\n\nSean Gallagher: operational requirement: rollback should not increase p95 by >25% for more than 2 minutes (rough target). can we hit that with lazy eviction + stagger?\n\nAllison Grant: we can add a panel specifically for “rollback disturbance window” and measure. but please also add a metric: `cache_epoch_active` and `cache_epoch_evictions_total` tagged by worker group.\n\nNoah Patel: action items I’m proposing:\n1) Change cache invalidation from “flush now” to epoch-based eligibility (lazy eviction) for PrefixCache and KV reuse pool.\n2) Make route hint cache verify compatibility rather than invalidate globally.\n3) Add optional stagger/jitter for any remaining “must flush” operations.\n4) Update rollback API/Console status wording: initiated vs converged.\n\nOmar El-Sayed: +1. I can take (1) for PrefixCache + KV pool tagging.\n\nSofia Ivanova: I’ll take (2) for route hints + compat check (scheduler-owned).\n\nAllison Grant: I’ll draft the metrics + dashboard annotation requirements and send to #eng-sre.\n\nSean Gallagher: pls also update runbook language: “expect small transient latency increase; if sustained > X, pause further changes”\n\nConnor O'Brien: once you have a PR up, tag me. we’ll need to coordinate runtime release train so it lands before limited GA.\n\nNoah Patel: cool. I’ll write up a short design note and link it here. also will sanity-check with Paula/Console team on the “converged” status naming.\n\nOmar El-Sayed: dropping a quick sketch:\n```\n// on config update\ncurrent_epoch = cfg.version_epoch\n\n// cache get\nif entry.epoch != current_epoch: miss\n\n// background\nevict(epoch != current_epoch) at rate_limit\n```\n\nNoah Patel: perfect. also: let’s make sure rollback doesn’t trigger a thundering herd of background eviction threads. one per worker, low-priority.\n\nSean Gallagher: :thumbsup: \n\nAllison Grant: :eyes: on metrics tagging: include `config_version` and `rollback_id` if available\n\nNoah Patel: yep. we can propagate rollback_id from control plane event to runtime logs/metrics.\n\nNoah Patel: will follow up with PR links.","occurredAt":1739394029000,"entityType":"thread","routeKey":"apps/slack/channels/eng-runtime","sourcePath":"sources/slack/eng-runtime/1739390012.json","peopleIds":["allison-grant","benji-okafor","connor-o-brien","noah-patel","omar-el-sayed","sean-gallagher","sofia-ivanova"],"links":[],"tags":["eng-runtime"]},{"id":"slack:1739305589","app":"slack","title":"#eng-runtime: 1739305589","preview":"Noah Patel: heads up for quant profile library runtime wiring — a few concerns I want to sync on:\n1) determinism (same request+route should resolve same profile every time)\n2) cache interactions (prefix/KV cache + batching) w/ mixed quant modes\n3) traces/logs: how do we *prove* profile applied vs fallback\n\nSofia Ivanova: +1 esp on determinism. if control plane returns `recommended` alias and that changes under us, we’ll have \"same config\" but different kernels/weights day-to-day.\n\nJordan Lee: determinism requirement from platform side: resolved settings must be deterministic per route config snapshot. For v1 we should treat `quantization_profile_ref` as pinned (profile_id + version), not a moving alias.\n\nNoah Patel: ok so runtime should only see a fully-resolved `profile_id@version`? (or `disabled`)\n\nJordan Lee: yep. policy engine resolves route cfg -> concrete profile ref. if alias exists at all it should be resolved at write-time, not request-time.\n\nMateo Alvarez: strongly prefer that. runtime hot path shouldn’t have to interpret aliases.\n\nOmar El-Sayed: cache stuff: we need to make sure we don’t share KV cache entries across quant mode boundaries. if we have prefix caching keyed just on prompt hash + model_id, enabling INT8 on route would start reusing FP16 KV from earlier traffic :grimacing:\n\nGrace Kim: yup, and also attention kernels behave differently w/ some act quant paths. key must include at least `quant_profile_id` (or \"baseline\") + maybe some kernel selection hash.\n\nHiro Tanaka: kernel selection is a function of (gpu sku, seq len, batch shape, quant mode). If we switch quant, the selected kernel path can change. Agree cache key needs `quant_profile_id` and probably `quant_mode` is redundant if profile id is present.\n\nNoah Patel: current cache key (simplified) is:\n`cache_key = model_id + tokenizer_rev + prompt_prefix_hash + max_new_tokens_bucket`\nWe can add `quant_profile_id` and `weights_rev`.\n\nOmar El-Sayed: also watch out for \"baseline\" not being a real profile id. I suggest we normalize to `quant_profile_id=none` and include that string in the key so we don’t collide.\n\nSofia Ivanova: batching: if continuous batching mixes requests with different quant profiles on same replica, are we going to be ok? most implementations assume same weights resident.\n\nHiro Tanaka: mixing profiles in a batch is a no-go if it implies different weights / different quantized weight pack. If profiles are per-model variant and you load one profile per worker, then batch scheduler must bucket by `quant_profile_id`.\n\nGrace Kim: +1. some profiles may change weight packing layout too.\n\nNoah Patel: so options:\nA) worker pins to one profile per model variant (baseline or a given profile) and scheduler routes accordingly\nB) allow dynamic switching but then you thrash weight loads and blow latency\n\nMateo Alvarez: A for v1. treat profile selection like model variant selection. runtime: route -> (model_variant, quant_profile_id) -> pool key.\n\nSofia Ivanova: ok then queue partitioning by pool key. we already do that for model variants.\n\nOmar El-Sayed: memory impact: if we have both baseline + quant workers live, KV cache memory behavior differs (int8 activations can reduce compute but KV often stays fp16/bf16). we should confirm we’re not assuming kv dtype changes.\n\nHiro Tanaka: current plan: quantization profile affects weights (and maybe activation quant in matmuls), but KV cache dtype remains bf16. so kv memory doesn’t drop materially; decode perf might improve. need to document.\n\nNoah Patel: traces/logs: what’s the best way to surface profile application? We need to answer \"did this request run under profile X or fallback?\" quickly.\n\nCaleb Johnson: tracing side: we can tag spans with attributes. suggestion:\n- `llm.quant.profile_id` (string; \"none\" for baseline)\n- `llm.quant.applied` (true/false)\n- `llm.quant.resolution` (\"applied\" | \"fallback\" | \"disabled\")\n- `llm.quant.fallback_reason` (enum string; e.g. \"incompatible_gpu\" \"seq_len_exceeded\" \"kill_switch\" \"artifact_load_failed\")\nAlso log annotation on request log for sampling.\n\nLogan Wright: metrics too pls. if you emit `fallback_rate{route,model,profile_id,reason}` we can build alerting. but traces are essential for debugging single request IDs.\n\nGrace Kim: for fallback reason, please include kernel guardrails too (e.g. \"unsupported_kernel_path\") so we can triage quickly.\n\nNoah Patel: where do we decide fallback? policy engine may already do compatibility checks, but runtime can still fail to load artifact.\n\nJordan Lee: plan is: policy does deterministic compatibility (gpu class, max ctx, model family). runtime still needs runtime-guardrails (artifact fetch failure, checksum mismatch, unexpected precision) -> fallback with reason.\n\nMateo Alvarez: we should make sure fallback is *single hop* — don’t want loops like: apply profile -> fails -> retry policy -> re-resolves same profile -> fails again.\n\nSofia Ivanova: +1. runtime should have local \"profile load failed\" cache with TTL so we don’t hammer registry.\n\nNoah Patel: good call. Implementation thought:\n- on request: resolved_profile_ref comes from router\n- runtime attempts `ensure_profile_loaded(ref)`\n- if error: mark `ref` unhealthy for (say) 60s, emit event, then run baseline weights\n\nOmar El-Sayed: and caches should record which profile ran. otherwise prefix cache might still store entries under the \"requested\" profile id even if fallback happened.\n\nNoah Patel: yup, cache key should use *effective* profile id (after fallback). So if fallback occurs, set `effective_quant_profile_id=none`.\n\nCaleb Johnson: for trace tagging, use effective too, and add requested separately:\n- `llm.quant.requested_profile_id`\n- `llm.quant.effective_profile_id`\n\nGrace Kim: +1, otherwise you can’t tell why it didn’t apply.\n\nHiro Tanaka: kernel selection logs: can we include a compact `kernel_path` tag? even a short enum helps (e.g. `flashattn_v3_int8w` vs `flashattn_v3_fp16`).\n\nNoah Patel: we can put it in debug logs, not sure about always-on trace tags (cardinality). maybe a sampled event.\n\nLogan Wright: agree re cardinality. put kernel path behind debug sampling; but profile_id is ok.\n\nSofia Ivanova: another determinism angle: if profile artifacts stored in registry change in-place, runtime could load different bits under same id. do we have signing/hash?\n\nMateo Alvarez: profiles must be immutable by (id, version). registry should serve content-addressed artifacts, or at least enforce immutability w/ checksum validation.\n\nJordan Lee: registry team is adding signing + versioned artifact storage. runtime validates checksum before using.\n\nNoah Patel: ok. I’ll add to runtime design doc:\n- pool key includes profile_id@version\n- scheduler buckets by pool key; no mixed-profile batches\n- prefix/KV cache key includes effective profile id\n- trace tags for requested/effective + resolution + fallback reason\n- unhealthy cache to prevent thundering herd on repeated load failures\n\nOmar El-Sayed: please call out interaction w/ prefix cache invalidation: when toggling profile on a route, old prefix cache entries stay but are keyed differently, so safe (but cache hit rate resets). that’s expected.\n\nSofia Ivanova: and mention latency hit on first request after enable due to profile load. can we prewarm?\n\nNoah Patel: yeah: control plane / console can trigger \"preload\" by hitting a warmup endpoint or we can lazy-load with a clear warning. v1 probably lazy + doc.\n\nCaleb Johnson: last request: make sure request logs have a single field too. Something like `quant_profile_id=<effective>` and `quant_resolution=<...>` so oncall can grep.\n\nNoah Patel: ack. I’ll coordinate with logging/tracing. thanks all.\n\nMateo Alvarez: :thumbsup: please drop the draft in #eng-runtime once you’ve updated. want to sanity check before we commit to API shapes.\n\nNoah Patel: will do.","text":"Noah Patel: heads up for quant profile library runtime wiring — a few concerns I want to sync on:\n1) determinism (same request+route should resolve same profile every time)\n2) cache interactions (prefix/KV cache + batching) w/ mixed quant modes\n3) traces/logs: how do we *prove* profile applied vs fallback\n\nSofia Ivanova: +1 esp on determinism. if control plane returns `recommended` alias and that changes under us, we’ll have \"same config\" but different kernels/weights day-to-day.\n\nJordan Lee: determinism requirement from platform side: resolved settings must be deterministic per route config snapshot. For v1 we should treat `quantization_profile_ref` as pinned (profile_id + version), not a moving alias.\n\nNoah Patel: ok so runtime should only see a fully-resolved `profile_id@version`? (or `disabled`)\n\nJordan Lee: yep. policy engine resolves route cfg -> concrete profile ref. if alias exists at all it should be resolved at write-time, not request-time.\n\nMateo Alvarez: strongly prefer that. runtime hot path shouldn’t have to interpret aliases.\n\nOmar El-Sayed: cache stuff: we need to make sure we don’t share KV cache entries across quant mode boundaries. if we have prefix caching keyed just on prompt hash + model_id, enabling INT8 on route would start reusing FP16 KV from earlier traffic :grimacing:\n\nGrace Kim: yup, and also attention kernels behave differently w/ some act quant paths. key must include at least `quant_profile_id` (or \"baseline\") + maybe some kernel selection hash.\n\nHiro Tanaka: kernel selection is a function of (gpu sku, seq len, batch shape, quant mode). If we switch quant, the selected kernel path can change. Agree cache key needs `quant_profile_id` and probably `quant_mode` is redundant if profile id is present.\n\nNoah Patel: current cache key (simplified) is:\n`cache_key = model_id + tokenizer_rev + prompt_prefix_hash + max_new_tokens_bucket`\nWe can add `quant_profile_id` and `weights_rev`.\n\nOmar El-Sayed: also watch out for \"baseline\" not being a real profile id. I suggest we normalize to `quant_profile_id=none` and include that string in the key so we don’t collide.\n\nSofia Ivanova: batching: if continuous batching mixes requests with different quant profiles on same replica, are we going to be ok? most implementations assume same weights resident.\n\nHiro Tanaka: mixing profiles in a batch is a no-go if it implies different weights / different quantized weight pack. If profiles are per-model variant and you load one profile per worker, then batch scheduler must bucket by `quant_profile_id`.\n\nGrace Kim: +1. some profiles may change weight packing layout too.\n\nNoah Patel: so options:\nA) worker pins to one profile per model variant (baseline or a given profile) and scheduler routes accordingly\nB) allow dynamic switching but then you thrash weight loads and blow latency\n\nMateo Alvarez: A for v1. treat profile selection like model variant selection. runtime: route -> (model_variant, quant_profile_id) -> pool key.\n\nSofia Ivanova: ok then queue partitioning by pool key. we already do that for model variants.\n\nOmar El-Sayed: memory impact: if we have both baseline + quant workers live, KV cache memory behavior differs (int8 activations can reduce compute but KV often stays fp16/bf16). we should confirm we’re not assuming kv dtype changes.\n\nHiro Tanaka: current plan: quantization profile affects weights (and maybe activation quant in matmuls), but KV cache dtype remains bf16. so kv memory doesn’t drop materially; decode perf might improve. need to document.\n\nNoah Patel: traces/logs: what’s the best way to surface profile application? We need to answer \"did this request run under profile X or fallback?\" quickly.\n\nCaleb Johnson: tracing side: we can tag spans with attributes. suggestion:\n- `llm.quant.profile_id` (string; \"none\" for baseline)\n- `llm.quant.applied` (true/false)\n- `llm.quant.resolution` (\"applied\" | \"fallback\" | \"disabled\")\n- `llm.quant.fallback_reason` (enum string; e.g. \"incompatible_gpu\" \"seq_len_exceeded\" \"kill_switch\" \"artifact_load_failed\")\nAlso log annotation on request log for sampling.\n\nLogan Wright: metrics too pls. if you emit `fallback_rate{route,model,profile_id,reason}` we can build alerting. but traces are essential for debugging single request IDs.\n\nGrace Kim: for fallback reason, please include kernel guardrails too (e.g. \"unsupported_kernel_path\") so we can triage quickly.\n\nNoah Patel: where do we decide fallback? policy engine may already do compatibility checks, but runtime can still fail to load artifact.\n\nJordan Lee: plan is: policy does deterministic compatibility (gpu class, max ctx, model family). runtime still needs runtime-guardrails (artifact fetch failure, checksum mismatch, unexpected precision) -> fallback with reason.\n\nMateo Alvarez: we should make sure fallback is *single hop* — don’t want loops like: apply profile -> fails -> retry policy -> re-resolves same profile -> fails again.\n\nSofia Ivanova: +1. runtime should have local \"profile load failed\" cache with TTL so we don’t hammer registry.\n\nNoah Patel: good call. Implementation thought:\n- on request: resolved_profile_ref comes from router\n- runtime attempts `ensure_profile_loaded(ref)`\n- if error: mark `ref` unhealthy for (say) 60s, emit event, then run baseline weights\n\nOmar El-Sayed: and caches should record which profile ran. otherwise prefix cache might still store entries under the \"requested\" profile id even if fallback happened.\n\nNoah Patel: yup, cache key should use *effective* profile id (after fallback). So if fallback occurs, set `effective_quant_profile_id=none`.\n\nCaleb Johnson: for trace tagging, use effective too, and add requested separately:\n- `llm.quant.requested_profile_id`\n- `llm.quant.effective_profile_id`\n\nGrace Kim: +1, otherwise you can’t tell why it didn’t apply.\n\nHiro Tanaka: kernel selection logs: can we include a compact `kernel_path` tag? even a short enum helps (e.g. `flashattn_v3_int8w` vs `flashattn_v3_fp16`).\n\nNoah Patel: we can put it in debug logs, not sure about always-on trace tags (cardinality). maybe a sampled event.\n\nLogan Wright: agree re cardinality. put kernel path behind debug sampling; but profile_id is ok.\n\nSofia Ivanova: another determinism angle: if profile artifacts stored in registry change in-place, runtime could load different bits under same id. do we have signing/hash?\n\nMateo Alvarez: profiles must be immutable by (id, version). registry should serve content-addressed artifacts, or at least enforce immutability w/ checksum validation.\n\nJordan Lee: registry team is adding signing + versioned artifact storage. runtime validates checksum before using.\n\nNoah Patel: ok. I’ll add to runtime design doc:\n- pool key includes profile_id@version\n- scheduler buckets by pool key; no mixed-profile batches\n- prefix/KV cache key includes effective profile id\n- trace tags for requested/effective + resolution + fallback reason\n- unhealthy cache to prevent thundering herd on repeated load failures\n\nOmar El-Sayed: please call out interaction w/ prefix cache invalidation: when toggling profile on a route, old prefix cache entries stay but are keyed differently, so safe (but cache hit rate resets). that’s expected.\n\nSofia Ivanova: and mention latency hit on first request after enable due to profile load. can we prewarm?\n\nNoah Patel: yeah: control plane / console can trigger \"preload\" by hitting a warmup endpoint or we can lazy-load with a clear warning. v1 probably lazy + doc.\n\nCaleb Johnson: last request: make sure request logs have a single field too. Something like `quant_profile_id=<effective>` and `quant_resolution=<...>` so oncall can grep.\n\nNoah Patel: ack. I’ll coordinate with logging/tracing. thanks all.\n\nMateo Alvarez: :thumbsup: please drop the draft in #eng-runtime once you’ve updated. want to sanity check before we commit to API shapes.\n\nNoah Patel: will do.","occurredAt":1739309142000,"entityType":"thread","routeKey":"apps/slack/channels/eng-runtime","sourcePath":"sources/slack/eng-runtime/1739305589.json","peopleIds":["caleb-johnson","grace-kim","hiro-tanaka","jordan-lee","logan-wright","mateo-alvarez","noah-patel","omar-el-sayed","sofia-ivanova"],"links":[],"tags":["eng-runtime"]},{"id":"slack:1738865402","app":"slack","title":"#eng-runtime: 1738865402","preview":"Dev Patel: anyone remember the gotchas when adding a new upstream tokenizer to hosted? qwen2.5 looks fine in eval harness but I’m worried about stop sequences + special tokens in streaming\n\nEthan Park: yeah, two things we’ve hit: (1) `eos_token_id` mismatch when upstream publishes multiple eos candidates, and (2) our OpenAI-compat path strips leading spaces differently. check the tokenizer config in registry and make sure we set `strip_leading_space=false` if needed\n\nAisha Rahman: also for evals, run the internal prompt set with the *exact* stop sequence config we ship. we had a false pass last time because harness used default stops but prod route used `\n\n` + `</s>`\n\nConnor OBrien: rollout side: please ensure router policy has fallback #2 to a cross-family safe default (we’ve been using `rw/llama-3.1-70b-instruct`), otherwise incident response is painful if the whole family hits OOM\n\nMaya Srinivasan: +1. and don’t forget to set max_context error message. we had complaints when it returned generic 500 instead of a clean 400 w/ actionable text\n\nDev Patel: perfect. I’ll update registry PR description w/ shipped stop sequences + set the fallback chain. thanks","text":"Dev Patel: anyone remember the gotchas when adding a new upstream tokenizer to hosted? qwen2.5 looks fine in eval harness but I’m worried about stop sequences + special tokens in streaming\n\nEthan Park: yeah, two things we’ve hit: (1) `eos_token_id` mismatch when upstream publishes multiple eos candidates, and (2) our OpenAI-compat path strips leading spaces differently. check the tokenizer config in registry and make sure we set `strip_leading_space=false` if needed\n\nAisha Rahman: also for evals, run the internal prompt set with the *exact* stop sequence config we ship. we had a false pass last time because harness used default stops but prod route used `\n\n` + `</s>`\n\nConnor OBrien: rollout side: please ensure router policy has fallback #2 to a cross-family safe default (we’ve been using `rw/llama-3.1-70b-instruct`), otherwise incident response is painful if the whole family hits OOM\n\nMaya Srinivasan: +1. and don’t forget to set max_context error message. we had complaints when it returned generic 500 instead of a clean 400 w/ actionable text\n\nDev Patel: perfect. I’ll update registry PR description w/ shipped stop sequences + set the fallback chain. thanks","occurredAt":1738868120000,"entityType":"thread","routeKey":"apps/slack/channels/eng-runtime","sourcePath":"sources/slack/eng-runtime/1738865402-model-launch-gotchas.json","peopleIds":["aisha-rahman","connor-o-brien","dev-patel","ethan-park","maya-srinivasan"],"links":[],"tags":["eng-runtime"]},{"id":"slack:1738453311","app":"slack","title":"#eng-runtime: 1738453311","preview":"Jada Williams: Been digging into the latest blended $/1M output token spikes on a couple long-context routes. We’re seeing the usual pattern: output tokens flat-ish, but per-request GPU time up because prefill is huge. We need a *runtime* signal that explains this better than “avg input tokens”.\n\nJada Williams: Proposal: add a “prefill/decode ratio” metric (or prefill share) emitted per request and aggregated hourly. It would help explain unit cost shifts when prompts get longer or cache hit rate changes.\n\nAllison Grant: +1. For the dashboard folks keep asking “why did $/output token jump when output length didn’t change” and the answer is almost always prefill dominated.\n\nLogan Wright: Agree. But we should be super explicit on definition so we don’t end up with 3 different ratios.\n\nNadia Rahman: What exactly do you mean by prefill/decode ratio here? Tokens? time? FLOPs proxy?\n\nJada Williams: Two options:\n1) Token-based proxy: `prefill_tokens / max(output_tokens, 1)` (where prefill_tokens = input tokens actually processed in prefill)\n2) Time-based: `prefill_ms / max(decode_ms, 1)`\n\nJada Williams: I lean time-based for cost correlation, but token-based is easier to make consistent across models/hw. Maybe we emit both (but only chart one).\n\nFelix Schneider: If we do token-based, we need to be careful: prefill tokens != “input tokens” when prefix/KV cache hits. Ideally it’s “uncached prefill tokens”.\n\nNadia Rahman: Yep. If we already have prefix cache instrumentation we can compute effective prefill tokens as `input_tokens - cached_prefix_tokens` (or whatever the runtime calls it). Otherwise token-based will look bad for cached traffic.\n\nQuentin Harris: From model metadata side: different models have different prefill/decode perf characteristics. A time-based ratio is comparable within a model/version, less so across models. Token ratio is more apples-to-apples but less tied to $$.\n\nAllison Grant: What does the unit economics dashboard actually need?\n\nLogan Wright: For v1 dashboard we just need an explanatory signal that moves with $/output token. Finance will keep living on blended $/1M output tokens, but we want a “drivers” panel. If prefill dominates, we can say “long context / low output length / cache regression.”\n\nCaleb Johnson: Also for data quality, time-based metrics might be sampled/dropped. Token counts are already part of metering so they’re more reliable.\n\nSean Gallagher: +1 from SRE: ms-level per-request metrics can get noisy depending on sampling, p99 spikes, etc. If we add a new thing, please make sure we can SLO it or at least detect if it goes missing.\n\nJada Williams: Makes sense. How about:\n- Primary: `effective_prefill_tokens_per_output_token = effective_prefill_tokens / max(output_tokens, 1)`\n- Secondary (optional): `prefill_ms_per_output_token = prefill_ms / max(output_tokens, 1)`\n\nJada Williams: That maps directly to “cost per output token goes up when you spend more prefill per output token.”\n\nNadia Rahman: Naming matters. “prefill/decode ratio” is ambiguous because decode tokens == output tokens but decode time != output tokens. Suggest we call it what it is:\n- `prefill_tokens_per_output_token`\n- `prefill_ms_per_output_token`\n\nNadia Rahman: Then the dashboard can label it “Prefill work per output token” or similar.\n\nFelix Schneider: Implementation detail: where do we get effective_prefill_tokens? Runtime currently emits:\n- `input_tokens`\n- `output_tokens`\n- `prefix_cache_hit` (bool)\n- `prefix_cache_hit_tokens` (I think we have this only on the new path)\n\nFelix Schneider: We should standardize the field names. Otherwise aggregation will be a mess.\n\nLogan Wright: If runtime can emit `effective_input_tokens` (aka uncached tokens processed in prefill), that’s clean for the pipeline. We can still keep raw input tokens for other analyses.\n\nAllison Grant: And for the caching story, we can do a decomposition:\n- input_tokens\n- effective_prefill_tokens\n- output_tokens\n- cache hit rate\nThen we can see if the ratio went up because prompts got longer vs cache got worse.\n\nQuentin Harris: One more edge case: tool calling / structured output. Sometimes “output tokens” can be low but decode is still heavy because of stop/start? Not sure. Time-based metric would catch it.\n\nNadia Rahman: Also streaming vs non-streaming shouldn’t change the ratio itself, but streaming paths have had output token parity issues historically. If we add anything that divides by output_tokens, missing output_tokens will explode the ratio.\n\nCaleb Johnson: +1000. If output_tokens is undercounted, prefill_tokens_per_output_token will look like a regression when it’s actually metering. We need a guardrail: if output_tokens < X or null -> emit null ratio (or clamp) and flag coverage.\n\nSean Gallagher: Prefer emitting numerator/denominator separately and let the warehouse compute ratio with safe divide. That also makes it easier to alert on missing denominators.\n\nJada Williams: Works for me. Runtime emits:\n- `runtime_prefill_tokens_effective` (int)\n- `runtime_output_tokens` (int) (already)\nOptionally later:\n- `runtime_prefill_ms` and `runtime_decode_ms`\n\nJada Williams: Then aggregation computes `safe_divide(sum(prefill_tokens_effective), sum(output_tokens))` per {model, model_version, region, tier, quant_profile, streaming_flag?}.\n\nLogan Wright: +1. Keep dimensions minimal. If we add this to the economics rollups, we’ll chart by model/region/tier and show it as a driver.\n\nAllison Grant: For UI: I’ll talk to Alicia, but I think we can put it under “Workload composition” next to avg output length + cache hit rate.\n\nFelix Schneider: What about long-context specifically: should we bucket by context length? That might blow up cardinality.\n\nNadia Rahman: Please no buckets in telemetry tags. But in the warehouse we can compute percentiles of input_tokens and join. For now just ratio + maybe a simple “avg input tokens” is enough.\n\nJada Williams: Action items:\n- I’ll write up the metric definition + runtime field names and post in the unit economics signal doc.\n- @Nadia Rahman can you sanity check what we can emit across both serving paths (classic + new streaming)?\n- @Logan Wright can you confirm where you want the fields in the metering schema (so Caleb can add DQ checks)?\n\nNadia Rahman: Yep. I’ll audit current runtime event payloads. If prefix_cache_hit_tokens is missing on some paths, we can still emit effective_prefill_tokens = input_tokens when unknown (but then it won’t attribute cache savings). Better than nothing, but we should add a “effective_prefill_tokens_source=estimated|measured” flag? (maybe too much).\n\nCaleb Johnson: If you do that, make sure dashboard doesn’t mix estimated/measured silently. Even a single boolean “effective_prefill_tokens_measured” could let us track rollout coverage.\n\nSean Gallagher: From ops side, measured-vs-estimated is fine if we can alert when measured coverage drops. Otherwise we’ll be debugging phantom regressions again.\n\nLogan Wright: Let’s keep it simple: emit `effective_prefill_tokens` and `effective_prefill_tokens_is_measured` (bool). Aggregation will compute ratio only for measured by default and separately track coverage.\n\nAllison Grant: That plays nicely with the “confidence indicator” idea we’ve been discussing for dashboard panels.\n\nJada Williams: Cool. I’ll incorporate. Thanks all.\n\nNadia Rahman: Please also include a note in the definition: why we normalize by *output tokens* (because unit metric is $/1M output tokens), and common interpretations:\n- ratio up + cache hit flat => longer prompts / context growth\n- ratio up + cache hit down => cache regression\n- ratio down => more output length or better caching\n\nJada Williams: Will do.\n\nQuentin Harris: If you want a quick validation set: we have a couple routes that are known long-context (summarization + retrieval). I can send example model IDs + routes for testing.\n\nFelix Schneider: dropping here: for runtime naming I’d prefer `prefill_tokens_uncached` over “effective”, but I’m fine either way as long as we document.\n\nJada Williams: Ack. I’ll pick one and be consistent in the doc + PR.\n\nSean Gallagher: Add a runbook snippet too: “if prefill_tokens_per_output_token spikes, check output token metering completeness first.”\n\nCaleb Johnson: :thumbsup: I’ll add a DQ rule: if output_tokens drops >30% for a model/region while request_count stable, flag as metering issue, and suppress ratio alerts.\n\nAllison Grant: Great. Keep me posted when we have a first sample chart; would love to include in next dogfood update.","text":"Jada Williams: Been digging into the latest blended $/1M output token spikes on a couple long-context routes. We’re seeing the usual pattern: output tokens flat-ish, but per-request GPU time up because prefill is huge. We need a *runtime* signal that explains this better than “avg input tokens”.\n\nJada Williams: Proposal: add a “prefill/decode ratio” metric (or prefill share) emitted per request and aggregated hourly. It would help explain unit cost shifts when prompts get longer or cache hit rate changes.\n\nAllison Grant: +1. For the dashboard folks keep asking “why did $/output token jump when output length didn’t change” and the answer is almost always prefill dominated.\n\nLogan Wright: Agree. But we should be super explicit on definition so we don’t end up with 3 different ratios.\n\nNadia Rahman: What exactly do you mean by prefill/decode ratio here? Tokens? time? FLOPs proxy?\n\nJada Williams: Two options:\n1) Token-based proxy: `prefill_tokens / max(output_tokens, 1)` (where prefill_tokens = input tokens actually processed in prefill)\n2) Time-based: `prefill_ms / max(decode_ms, 1)`\n\nJada Williams: I lean time-based for cost correlation, but token-based is easier to make consistent across models/hw. Maybe we emit both (but only chart one).\n\nFelix Schneider: If we do token-based, we need to be careful: prefill tokens != “input tokens” when prefix/KV cache hits. Ideally it’s “uncached prefill tokens”.\n\nNadia Rahman: Yep. If we already have prefix cache instrumentation we can compute effective prefill tokens as `input_tokens - cached_prefix_tokens` (or whatever the runtime calls it). Otherwise token-based will look bad for cached traffic.\n\nQuentin Harris: From model metadata side: different models have different prefill/decode perf characteristics. A time-based ratio is comparable within a model/version, less so across models. Token ratio is more apples-to-apples but less tied to $$.\n\nAllison Grant: What does the unit economics dashboard actually need?\n\nLogan Wright: For v1 dashboard we just need an explanatory signal that moves with $/output token. Finance will keep living on blended $/1M output tokens, but we want a “drivers” panel. If prefill dominates, we can say “long context / low output length / cache regression.”\n\nCaleb Johnson: Also for data quality, time-based metrics might be sampled/dropped. Token counts are already part of metering so they’re more reliable.\n\nSean Gallagher: +1 from SRE: ms-level per-request metrics can get noisy depending on sampling, p99 spikes, etc. If we add a new thing, please make sure we can SLO it or at least detect if it goes missing.\n\nJada Williams: Makes sense. How about:\n- Primary: `effective_prefill_tokens_per_output_token = effective_prefill_tokens / max(output_tokens, 1)`\n- Secondary (optional): `prefill_ms_per_output_token = prefill_ms / max(output_tokens, 1)`\n\nJada Williams: That maps directly to “cost per output token goes up when you spend more prefill per output token.”\n\nNadia Rahman: Naming matters. “prefill/decode ratio” is ambiguous because decode tokens == output tokens but decode time != output tokens. Suggest we call it what it is:\n- `prefill_tokens_per_output_token`\n- `prefill_ms_per_output_token`\n\nNadia Rahman: Then the dashboard can label it “Prefill work per output token” or similar.\n\nFelix Schneider: Implementation detail: where do we get effective_prefill_tokens? Runtime currently emits:\n- `input_tokens`\n- `output_tokens`\n- `prefix_cache_hit` (bool)\n- `prefix_cache_hit_tokens` (I think we have this only on the new path)\n\nFelix Schneider: We should standardize the field names. Otherwise aggregation will be a mess.\n\nLogan Wright: If runtime can emit `effective_input_tokens` (aka uncached tokens processed in prefill), that’s clean for the pipeline. We can still keep raw input tokens for other analyses.\n\nAllison Grant: And for the caching story, we can do a decomposition:\n- input_tokens\n- effective_prefill_tokens\n- output_tokens\n- cache hit rate\nThen we can see if the ratio went up because prompts got longer vs cache got worse.\n\nQuentin Harris: One more edge case: tool calling / structured output. Sometimes “output tokens” can be low but decode is still heavy because of stop/start? Not sure. Time-based metric would catch it.\n\nNadia Rahman: Also streaming vs non-streaming shouldn’t change the ratio itself, but streaming paths have had output token parity issues historically. If we add anything that divides by output_tokens, missing output_tokens will explode the ratio.\n\nCaleb Johnson: +1000. If output_tokens is undercounted, prefill_tokens_per_output_token will look like a regression when it’s actually metering. We need a guardrail: if output_tokens < X or null -> emit null ratio (or clamp) and flag coverage.\n\nSean Gallagher: Prefer emitting numerator/denominator separately and let the warehouse compute ratio with safe divide. That also makes it easier to alert on missing denominators.\n\nJada Williams: Works for me. Runtime emits:\n- `runtime_prefill_tokens_effective` (int)\n- `runtime_output_tokens` (int) (already)\nOptionally later:\n- `runtime_prefill_ms` and `runtime_decode_ms`\n\nJada Williams: Then aggregation computes `safe_divide(sum(prefill_tokens_effective), sum(output_tokens))` per {model, model_version, region, tier, quant_profile, streaming_flag?}.\n\nLogan Wright: +1. Keep dimensions minimal. If we add this to the economics rollups, we’ll chart by model/region/tier and show it as a driver.\n\nAllison Grant: For UI: I’ll talk to Alicia, but I think we can put it under “Workload composition” next to avg output length + cache hit rate.\n\nFelix Schneider: What about long-context specifically: should we bucket by context length? That might blow up cardinality.\n\nNadia Rahman: Please no buckets in telemetry tags. But in the warehouse we can compute percentiles of input_tokens and join. For now just ratio + maybe a simple “avg input tokens” is enough.\n\nJada Williams: Action items:\n- I’ll write up the metric definition + runtime field names and post in the unit economics signal doc.\n- @Nadia Rahman can you sanity check what we can emit across both serving paths (classic + new streaming)?\n- @Logan Wright can you confirm where you want the fields in the metering schema (so Caleb can add DQ checks)?\n\nNadia Rahman: Yep. I’ll audit current runtime event payloads. If prefix_cache_hit_tokens is missing on some paths, we can still emit effective_prefill_tokens = input_tokens when unknown (but then it won’t attribute cache savings). Better than nothing, but we should add a “effective_prefill_tokens_source=estimated|measured” flag? (maybe too much).\n\nCaleb Johnson: If you do that, make sure dashboard doesn’t mix estimated/measured silently. Even a single boolean “effective_prefill_tokens_measured” could let us track rollout coverage.\n\nSean Gallagher: From ops side, measured-vs-estimated is fine if we can alert when measured coverage drops. Otherwise we’ll be debugging phantom regressions again.\n\nLogan Wright: Let’s keep it simple: emit `effective_prefill_tokens` and `effective_prefill_tokens_is_measured` (bool). Aggregation will compute ratio only for measured by default and separately track coverage.\n\nAllison Grant: That plays nicely with the “confidence indicator” idea we’ve been discussing for dashboard panels.\n\nJada Williams: Cool. I’ll incorporate. Thanks all.\n\nNadia Rahman: Please also include a note in the definition: why we normalize by *output tokens* (because unit metric is $/1M output tokens), and common interpretations:\n- ratio up + cache hit flat => longer prompts / context growth\n- ratio up + cache hit down => cache regression\n- ratio down => more output length or better caching\n\nJada Williams: Will do.\n\nQuentin Harris: If you want a quick validation set: we have a couple routes that are known long-context (summarization + retrieval). I can send example model IDs + routes for testing.\n\nFelix Schneider: dropping here: for runtime naming I’d prefer `prefill_tokens_uncached` over “effective”, but I’m fine either way as long as we document.\n\nJada Williams: Ack. I’ll pick one and be consistent in the doc + PR.\n\nSean Gallagher: Add a runbook snippet too: “if prefill_tokens_per_output_token spikes, check output token metering completeness first.”\n\nCaleb Johnson: :thumbsup: I’ll add a DQ rule: if output_tokens drops >30% for a model/region while request_count stable, flag as metering issue, and suppress ratio alerts.\n\nAllison Grant: Great. Keep me posted when we have a first sample chart; would love to include in next dogfood update.","occurredAt":1738458924000,"entityType":"thread","routeKey":"apps/slack/channels/eng-runtime","sourcePath":"sources/slack/eng-runtime/1738453311-prefill-decode-ratio-signal.json","peopleIds":["allison-grant","caleb-johnson","felix-schneider","jada-williams","logan-wright","nadia-rahman","quentin-harris","sean-gallagher"],"links":[],"tags":["eng-runtime"]},{"id":"slack:1738443210","app":"slack","title":"#eng-runtime: 1738443210","preview":"","text":"","occurredAt":1738443800000,"entityType":"thread","routeKey":"apps/slack/channels/eng-runtime","sourcePath":"sources/slack/eng-runtime/1738443210-prefetch-policy-flip-on-staging-checkpoint-bounce.json","peopleIds":["jordan-blake"],"links":[],"tags":["eng-runtime"]},{"id":"slack:1737684455","app":"slack","title":"#eng-runtime: 1737684455","preview":"Nadia Rahman: we need a batching utilization metric out of runtime asap. looking at the early unit econ views, we’re seeing $/1M output tokens drift but we can’t tell if it’s “more expensive GPUs” vs “worse batching”. cache hit rate helps a bit but doesn’t explain the spikes.\n\nNadia Rahman: asking eng-runtime: can we add a stable metric for “how full were we” during decode/prefill? something finance/product can interpret w/o reading kernel traces.\n\nLogan Wright: +1. in pipeline we can join whatever you emit, but right now we only have request-level batch_size-ish from gateway logs, and that doesn’t reflect continuous batching behavior.\n\nAllison Grant: what exactly do you mean by utilization? avg batch size? gpu occupancy? those are v diff and can be misleading.\n\nNadia Rahman: fair. not asking for GPU occupancy (too noisy + hardware-specific). i want something like “tokens processed per step / max tokens possible per step given configured limits”.\n\nSean Gallagher: SRE angle: please keep cardinality sane. if we tag this with model_version + route + customer we’ll melt prometheus. need a constrained label set.\n\nJada Williams: we already have internal counters in the scheduler for:\n- num_active_seqs per engine tick\n- max_seqs configured\n- decode_tokens_this_tick\n- prefill_tokens_this_tick\n- max_tokens_per_tick (depends on block size + max_num_batched_tokens)\n\nJada Williams: we could export a gauge (or sampled histogram) for “batch_tokens_utilization = (prefill_tokens + decode_tokens) / max_tokens_per_tick”. and also export seq_utilization = num_active_seqs / max_seqs.\n\nFelix Schneider: if you do both, dashboards can separate “we had lots of sequences but low tokens” vs “we had long sequences but seq slots empty”.\n\nAllison Grant: for continuous batching, max_tokens_per_tick is not a constant unless you’re explicitly enforcing a per-step token budget. do we have that?\n\nJada Williams: yes in runtime config: `scheduler.max_batched_tokens` (hard cap per tick). today it’s per engine instance, same value across a pool.\n\nJada Williams: caveat: during decode we often hit `max_seqs` before `max_batched_tokens` depending on avg output length. so the two utilization metrics help.\n\nCaleb Johnson: from economics side, we also need to know how to interpret “utilization < 1”. e.g. if utilization low because traffic low vs because scheduler leaving gaps. can we add a “queue_depth” or “pending_requests” metric too?\n\nSean Gallagher: +1 queue depth helps when correlating with throttling/autoscale events.\n\nNadia Rahman: minimum set i’d like:\n1) batch_tokens_utilization (0..1)\n2) batch_seq_utilization (0..1)\n3) queue_depth (int)\noptionally split prefill vs decode.\n\nMei Lin: if you split prefill/decode, please name them deterministically so we don’t get 8 variants. also the warehouse job can store them as columns if stable.\n\nFelix Schneider: proposal for metric names (prom-style):\n- `rw_runtime_batch_tokens_utilization` (gauge)\n- `rw_runtime_batch_seqs_utilization` (gauge)\n- `rw_runtime_scheduler_queue_depth` (gauge)\n\nFelix Schneider: and optionally:\n- `rw_runtime_batch_prefill_tokens_utilization`\n- `rw_runtime_batch_decode_tokens_utilization`\n\nAllison Grant: gauges sampled every tick could be a lot. what’s tick rate? 10ms-ish? don’t ship that raw.\n\nJada Williams: tick rate is ~20-50ms depending on load. but we can aggregate inside runtime and flush 10s intervals:\n- keep running sum(util)\n- count(samples)\n- export avg over interval\nalso export p50/p90? (histogram) might be heavy.\n\nSean Gallagher: avg over 10s is fine. histograms are expensive but could be okay if label set is tiny.\n\nLogan Wright: for unit economics, avg is actually good. we’ll roll up hourly anyway. a “utilization_avg” and “utilization_p10” would be nice but not required v1.\n\nNadia Rahman: label set suggestion (please push back):\n- `region`\n- `cluster` (or capacity_pool)\n- `model_id` (normalized, not marketing alias)\n- `quantization_profile` (from allowed set)\nNO customer_id, no request_route, no model_version (unless we can bucket).\n\nSean Gallagher: region + cluster + model_id + quant_profile already gets large, but manageable if model_id is normalized and quant_profile is constrained. please do not include raw model_version; if needed, we can map versions in warehouse from model metadata tables.\n\nAllison Grant: does runtime even know region? it knows “pool name” / “colo”. we can add a “deployment_region” constant at startup.\n\nJada Williams: runtime can emit `pool` and `engine_type` (e.g., vllm-ish vs our custom engine). engine_type may matter.\n\nCaleb Johnson: engine_type could be valuable for debugging regressions when we change scheduler behavior.\n\nNadia Rahman: ok include `engine` as label (small set).\n\nFelix Schneider: schema detail: utilization definition should be precise or people will argue about it in exec readouts.\n\nFelix Schneider: draft definitions:\n- `batch_tokens_utilization = (prefill_tokens_processed + decode_tokens_processed) / max_batched_tokens_configured` averaged over 10s window\n- `batch_seqs_utilization = active_sequences / max_num_seqs_configured` averaged over 10s window\n- `queue_depth = num_waiting_requests` sampled (avg) over 10s\n\nAllison Grant: one more nuance: `active_sequences` includes sequences that are “paused” due to kv cache eviction/backpressure? depends on scheduler state.\n\nJada Williams: in our scheduler, active means “in-flight and occupying a slot”. paused still occupies. so seq_utilization is about slot pressure, not GPU work.\n\nNadia Rahman: that’s fine; in fact it helps explain cases where seq slots are full but tokens_util is low (lots of tiny decodes).\n\nSean Gallagher: please add a short comment in code / docs: “seq utilization is scheduler slot utilization, not GPU utilization”. otherwise this gets misused.\n\nLogan Wright: timeline? we need this before we freeze the unit econ dashboard v1 definitions.\n\nJada Williams: i can take this. implementation plan:\n- add in scheduler stats collector (10s window)\n- export via existing metrics sink\n- add unit tests for aggregation window\n- verify label cardinality guardrails (allowed model_id/quant_profile)\n\nJada Williams: can land behind a feature flag in runtime config and enable on 1-2 pools first.\n\nAllison Grant: please also expose max configs as info metrics (e.g. `rw_runtime_scheduler_max_batched_tokens`) so dashboards can sanity check when configs change.\n\nFelix Schneider: +1. changes in `max_batched_tokens` will look like utilization changes if not visible.\n\nNadia Rahman: awesome. can you also post a tiny example payload so telemetry can map it cleanly?\n\nJada Williams: sure, sketch:\n```\n# labels: region, pool, model_id, quantization_profile, engine\nrw_runtime_batch_tokens_utilization{region=\"us-east\",pool=\"hosted-a10\",model_id=\"llama3_70b\",quantization_profile=\"fp16\",engine=\"rw-engine\"} 0.72\nrw_runtime_batch_prefill_tokens_utilization{...} 0.61\nrw_runtime_batch_decode_tokens_utilization{...} 0.79\nrw_runtime_batch_seqs_utilization{...} 0.88\nrw_runtime_scheduler_queue_depth{...} 14\nrw_runtime_scheduler_max_batched_tokens{...} 4096\nrw_runtime_scheduler_max_seqs{...} 256\n```\n\nSean Gallagher: looks good. just confirm we’re not adding pool_id that includes shard numbers (those explode cardinality).\n\nJada Williams: we’ll use pool name without shard. (cluster-wide constant)\n\nMei Lin: once this lands, please ping me/logan. we’ll add to the economics model as “batching efficiency” signals and wire to drilldowns.\n\nCaleb Johnson: also note for the dashboard: we’ll likely show utilization as % with a tooltip explaining definition + window.\n\nNadia Rahman: thanks all. action items:\n- @Jada Williams implement + enable on 2 pools\n- @Sean Gallagher review label set/cardinality\n- @Logan Wright/@Mei Lin plan ingestion + join to unit econ aggregates\n\nNadia Rahman: will follow up with a short confluence note so we have a canonical definition.\n\nSean Gallagher: :thumbsup: pls include in the note the “not GPU utilization” disclaimer and the 10s aggregation window.\n\nJada Williams: ack. i’ll open a runtime ticket and link here once up.\n\nAllison Grant: one last ask: can we include a “dropped_due_to_budget” counter? i.e., how often requests wait because max_batched_tokens is hit.\n\nJada Williams: good idea but might be v1.1. for now queue_depth + utilization should capture it. will add a TODO.\n\nLogan Wright: works. we can correlate queue_depth spikes with utilization pegged at ~1 to infer budget saturation.\n\nNadia Rahman: closing the loop later this week after first rollout.","text":"Nadia Rahman: we need a batching utilization metric out of runtime asap. looking at the early unit econ views, we’re seeing $/1M output tokens drift but we can’t tell if it’s “more expensive GPUs” vs “worse batching”. cache hit rate helps a bit but doesn’t explain the spikes.\n\nNadia Rahman: asking eng-runtime: can we add a stable metric for “how full were we” during decode/prefill? something finance/product can interpret w/o reading kernel traces.\n\nLogan Wright: +1. in pipeline we can join whatever you emit, but right now we only have request-level batch_size-ish from gateway logs, and that doesn’t reflect continuous batching behavior.\n\nAllison Grant: what exactly do you mean by utilization? avg batch size? gpu occupancy? those are v diff and can be misleading.\n\nNadia Rahman: fair. not asking for GPU occupancy (too noisy + hardware-specific). i want something like “tokens processed per step / max tokens possible per step given configured limits”.\n\nSean Gallagher: SRE angle: please keep cardinality sane. if we tag this with model_version + route + customer we’ll melt prometheus. need a constrained label set.\n\nJada Williams: we already have internal counters in the scheduler for:\n- num_active_seqs per engine tick\n- max_seqs configured\n- decode_tokens_this_tick\n- prefill_tokens_this_tick\n- max_tokens_per_tick (depends on block size + max_num_batched_tokens)\n\nJada Williams: we could export a gauge (or sampled histogram) for “batch_tokens_utilization = (prefill_tokens + decode_tokens) / max_tokens_per_tick”. and also export seq_utilization = num_active_seqs / max_seqs.\n\nFelix Schneider: if you do both, dashboards can separate “we had lots of sequences but low tokens” vs “we had long sequences but seq slots empty”.\n\nAllison Grant: for continuous batching, max_tokens_per_tick is not a constant unless you’re explicitly enforcing a per-step token budget. do we have that?\n\nJada Williams: yes in runtime config: `scheduler.max_batched_tokens` (hard cap per tick). today it’s per engine instance, same value across a pool.\n\nJada Williams: caveat: during decode we often hit `max_seqs` before `max_batched_tokens` depending on avg output length. so the two utilization metrics help.\n\nCaleb Johnson: from economics side, we also need to know how to interpret “utilization < 1”. e.g. if utilization low because traffic low vs because scheduler leaving gaps. can we add a “queue_depth” or “pending_requests” metric too?\n\nSean Gallagher: +1 queue depth helps when correlating with throttling/autoscale events.\n\nNadia Rahman: minimum set i’d like:\n1) batch_tokens_utilization (0..1)\n2) batch_seq_utilization (0..1)\n3) queue_depth (int)\noptionally split prefill vs decode.\n\nMei Lin: if you split prefill/decode, please name them deterministically so we don’t get 8 variants. also the warehouse job can store them as columns if stable.\n\nFelix Schneider: proposal for metric names (prom-style):\n- `rw_runtime_batch_tokens_utilization` (gauge)\n- `rw_runtime_batch_seqs_utilization` (gauge)\n- `rw_runtime_scheduler_queue_depth` (gauge)\n\nFelix Schneider: and optionally:\n- `rw_runtime_batch_prefill_tokens_utilization`\n- `rw_runtime_batch_decode_tokens_utilization`\n\nAllison Grant: gauges sampled every tick could be a lot. what’s tick rate? 10ms-ish? don’t ship that raw.\n\nJada Williams: tick rate is ~20-50ms depending on load. but we can aggregate inside runtime and flush 10s intervals:\n- keep running sum(util)\n- count(samples)\n- export avg over interval\nalso export p50/p90? (histogram) might be heavy.\n\nSean Gallagher: avg over 10s is fine. histograms are expensive but could be okay if label set is tiny.\n\nLogan Wright: for unit economics, avg is actually good. we’ll roll up hourly anyway. a “utilization_avg” and “utilization_p10” would be nice but not required v1.\n\nNadia Rahman: label set suggestion (please push back):\n- `region`\n- `cluster` (or capacity_pool)\n- `model_id` (normalized, not marketing alias)\n- `quantization_profile` (from allowed set)\nNO customer_id, no request_route, no model_version (unless we can bucket).\n\nSean Gallagher: region + cluster + model_id + quant_profile already gets large, but manageable if model_id is normalized and quant_profile is constrained. please do not include raw model_version; if needed, we can map versions in warehouse from model metadata tables.\n\nAllison Grant: does runtime even know region? it knows “pool name” / “colo”. we can add a “deployment_region” constant at startup.\n\nJada Williams: runtime can emit `pool` and `engine_type` (e.g., vllm-ish vs our custom engine). engine_type may matter.\n\nCaleb Johnson: engine_type could be valuable for debugging regressions when we change scheduler behavior.\n\nNadia Rahman: ok include `engine` as label (small set).\n\nFelix Schneider: schema detail: utilization definition should be precise or people will argue about it in exec readouts.\n\nFelix Schneider: draft definitions:\n- `batch_tokens_utilization = (prefill_tokens_processed + decode_tokens_processed) / max_batched_tokens_configured` averaged over 10s window\n- `batch_seqs_utilization = active_sequences / max_num_seqs_configured` averaged over 10s window\n- `queue_depth = num_waiting_requests` sampled (avg) over 10s\n\nAllison Grant: one more nuance: `active_sequences` includes sequences that are “paused” due to kv cache eviction/backpressure? depends on scheduler state.\n\nJada Williams: in our scheduler, active means “in-flight and occupying a slot”. paused still occupies. so seq_utilization is about slot pressure, not GPU work.\n\nNadia Rahman: that’s fine; in fact it helps explain cases where seq slots are full but tokens_util is low (lots of tiny decodes).\n\nSean Gallagher: please add a short comment in code / docs: “seq utilization is scheduler slot utilization, not GPU utilization”. otherwise this gets misused.\n\nLogan Wright: timeline? we need this before we freeze the unit econ dashboard v1 definitions.\n\nJada Williams: i can take this. implementation plan:\n- add in scheduler stats collector (10s window)\n- export via existing metrics sink\n- add unit tests for aggregation window\n- verify label cardinality guardrails (allowed model_id/quant_profile)\n\nJada Williams: can land behind a feature flag in runtime config and enable on 1-2 pools first.\n\nAllison Grant: please also expose max configs as info metrics (e.g. `rw_runtime_scheduler_max_batched_tokens`) so dashboards can sanity check when configs change.\n\nFelix Schneider: +1. changes in `max_batched_tokens` will look like utilization changes if not visible.\n\nNadia Rahman: awesome. can you also post a tiny example payload so telemetry can map it cleanly?\n\nJada Williams: sure, sketch:\n```\n# labels: region, pool, model_id, quantization_profile, engine\nrw_runtime_batch_tokens_utilization{region=\"us-east\",pool=\"hosted-a10\",model_id=\"llama3_70b\",quantization_profile=\"fp16\",engine=\"rw-engine\"} 0.72\nrw_runtime_batch_prefill_tokens_utilization{...} 0.61\nrw_runtime_batch_decode_tokens_utilization{...} 0.79\nrw_runtime_batch_seqs_utilization{...} 0.88\nrw_runtime_scheduler_queue_depth{...} 14\nrw_runtime_scheduler_max_batched_tokens{...} 4096\nrw_runtime_scheduler_max_seqs{...} 256\n```\n\nSean Gallagher: looks good. just confirm we’re not adding pool_id that includes shard numbers (those explode cardinality).\n\nJada Williams: we’ll use pool name without shard. (cluster-wide constant)\n\nMei Lin: once this lands, please ping me/logan. we’ll add to the economics model as “batching efficiency” signals and wire to drilldowns.\n\nCaleb Johnson: also note for the dashboard: we’ll likely show utilization as % with a tooltip explaining definition + window.\n\nNadia Rahman: thanks all. action items:\n- @Jada Williams implement + enable on 2 pools\n- @Sean Gallagher review label set/cardinality\n- @Logan Wright/@Mei Lin plan ingestion + join to unit econ aggregates\n\nNadia Rahman: will follow up with a short confluence note so we have a canonical definition.\n\nSean Gallagher: :thumbsup: pls include in the note the “not GPU utilization” disclaimer and the 10s aggregation window.\n\nJada Williams: ack. i’ll open a runtime ticket and link here once up.\n\nAllison Grant: one last ask: can we include a “dropped_due_to_budget” counter? i.e., how often requests wait because max_batched_tokens is hit.\n\nJada Williams: good idea but might be v1.1. for now queue_depth + utilization should capture it. will add a TODO.\n\nLogan Wright: works. we can correlate queue_depth spikes with utilization pegged at ~1 to infer budget saturation.\n\nNadia Rahman: closing the loop later this week after first rollout.","occurredAt":1737720198000,"entityType":"thread","routeKey":"apps/slack/channels/eng-runtime","sourcePath":"sources/slack/eng-runtime/1737684455-batching-utilization-metric-request.json","peopleIds":["allison-grant","caleb-johnson","felix-schneider","jada-williams","logan-wright","mei-lin","nadia-rahman","sean-gallagher"],"links":[],"tags":["eng-runtime"]},{"id":"slack:1737665440","app":"slack","title":"#eng-runtime: 1737665440","preview":"Diego Santos: quick q for audit logging coverage — what runtime-level actions are we calling “sensitive”? thinking model enable/disable, fleet pool bindings, etc. do we need audit events for these or do we treat as internal ops?\n\nHanae Suzuki: +1, security side assumption has been: any action that changes (a) what model can serve traffic, (b) where traffic can go, or (c) who can access capacity => sensitive.\n\nMarcus Lin: yep. also anything that changes policy enforcement (eg bypassing allow-lists), even if done via runtime config.\n\nEthan Park: to clarify, “runtime-level” here = changes in serving layer config (model registry flags, pool allocations) vs control plane policy? where are these changes initiated today?\n\nDiego Santos: mix. some via control plane (rollouts/policy), but some are “runtime admin” endpoints (mostly used by oncall) for: \n- enable/disable model version in a region\n- bind/unbind model to a GPU pool (fleet pool)\n- adjust max_batch_tokens / concurrency caps\n- toggle a “degraded mode” flag when a model is flaky\n\nAllison Grant: from observability standards POV, anything done through an “admin endpoint” should emit an audit event unless we explicitly waive it. otherwise we’ll have blind spots during incidents.\n\nJordan Lee: + for routing-related: if runtime config can change routing weights / fallback order, that’s definitely sensitive. even if it’s “just” to fix latency.\n\nDiego Santos: what about pure performance knobs like continuous batching parameters? those can impact cost/latency but not access control.\n\nHanae Suzuki: still can be sensitive if it materially changes customer experience or could be abused to DoS. but we can scope it as “sensitive: availability/controls” vs “non-sensitive: tuning” if we have a clear line.\n\nMarcus Lin: line I’d propose: audit any “state change” that affects traffic eligibility or capacity allocation at tenant/project/model level. not every per-host tunable.\n\nLogan Wright: for coverage dashboards, we need a crisp list of expected event_types. otherwise runtime will be a constant “unknown unknowns” bucket.\n\nNadia Rahman: also cardinality note: don’t dump giant config blobs. if we log diffs, keep it bounded (eg allow-listed keys + before/after strings truncated).\n\nEthan Park: can we enumerate the runtime actions that are user-initiated vs automatic? eg autoscaler increasing replicas shouldn’t spam audit logs.\n\nDiego Santos: good call. categories:\n1) operator-initiated (oncall / console admin)\n2) system-initiated (autoscaler, health manager)\n\nDiego Santos: system-initiated shouldn’t be audit, but should be normal telemetry/events.\n\nHanae Suzuki: agree. audit logs are about accountability. still, system-initiated changes that affect customer traffic might be useful for investigations, but that’s more “operational event stream” than “audit”.\n\nAllison Grant: +1. audit events require actor semantics; “actor.type=service” is ok, but we should avoid turning every reconciliation loop into audit noise.\n\nMarcus Lin: but there are a couple “system actions” we might want to treat as audit-adjacent: if the system disables a model due to safety/abuse detection or flips a policy gate.\n\nJordan Lee: that’s more policy engine / trust & safety domain, but yes: if runtime auto-disables a model version due to repeated bad outputs, that’s a big deal.\n\nDiego Santos: ok, propose “sensitive runtime actions” list:\n- model version enable\n- model version disable\n- model version pin/unpin to traffic (ie “eligible_for_routing”)\n- fleet pool binding create/update/delete (model <-> pool)\n- manual capacity override on pool (eg reserved slots, min replicas) when it changes tenant-visible throughput\n- enabling/disabling “degraded mode” / “circuit breaker” for a model if it changes routing eligibility\n\nDiego Santos: NOT sensitive (telemetry only):\n- per-host kernel selection, compilation cache, warmup\n- autoscaler scale up/down\n- dynamic batcher tuning unless it changes a hard cap / quota\n\nHanae Suzuki: looks right. re: batching: if you change max_concurrency / max_inflight for a tenant, that can be used to throttle. I’d call those sensitive when scoped to tenant/project.\n\nDiego Santos: fair. we do have an endpoint to set “tenant concurrency cap override” during incidents.\n\nMarcus Lin: that’s absolutely sensitive. it’s basically a quota override.\n\nEthan Park: that might be owned by gateway/control plane though? but if runtime applies it directly, audit should still fire at the point of change.\n\nLogan Wright: for coverage: we’ll want event_type names that are stable and map to taxonomy. can runtime align with the registry PR (ENG-4822) rather than inventing new strings?\n\nAllison Grant: please. also include correlation fields: request_id/trace_id + admin_action_id so we can connect a console click -> control plane -> runtime.\n\nNadia Rahman: and include “deployment_mode” (hosted/dedicated/private) consistently. we’ve seen private missing these fields.\n\nDiego Santos: ok. I’ll sync with eng-security on naming. tentative event_types:\n- runtime.model_version.enabled\n- runtime.model_version.disabled\n- runtime.model_version.routing_eligibility.updated\n- runtime.fleet_pool_binding.created\n- runtime.fleet_pool_binding.deleted\n- runtime.tenant_concurrency_override.updated\n\nHanae Suzuki: + include target fields (model_id, model_version, pool_id, tenant_id/project_id). and for actor, distinguish human vs service.\n\nMarcus Lin: also: if there’s an “impersonation” flow (support acting as customer admin), include impersonator in actor chain.\n\nDiego Santos: ack. for oncall, actor will be the human via SSO -> admin API token. for private, operator identity is sometimes just “kube user” from kubectl; we’ll capture best-effort.\n\nAuditBot: Reminder: use schema_version in all new audit events and validate against audit-log-schema-v1. Unknown event_type strings will be rejected once ENG-4822 lints are enabled.\n\nJordan Lee: question: when policy engine pushes down a new routing policy to runtime, do we emit audit in policy engine only, or also in runtime?\n\nAllison Grant: prefer single source of truth for “who changed policy” (policy engine/control plane). runtime can emit operational “policy applied” events, but not duplicate audit unless there’s a separate manual override.\n\nMarcus Lin: +1. avoid dupes. runtime audit should focus on runtime-owned admin actions / overrides.\n\nDiego Santos: cool. action items:\n- I’ll write up the sensitive runtime actions list + ownership boundary (policy vs runtime) and drop in #eng-security for taxonomy review\n- Logan: once we have event_types, can you add to expected list for coverage dashboard?\n\nLogan Wright: yep, send the final strings + required fields and I’ll add to the “expected events” config.\n\nHanae Suzuki: please also note which ones must be “guaranteed” vs best-effort. eg enable/disable should probably be “must emit” even if sink is degraded.\n\nDiego Santos: agreed. enable/disable + pool binding + tenant cap overrides = must emit (buffer/retry). tuning-only stays best-effort/telemetry.\n\nEthan Park: 👍\n\nMarcus Lin: 👍 keep it tight and consistent with the schema. thanks.","text":"Diego Santos: quick q for audit logging coverage — what runtime-level actions are we calling “sensitive”? thinking model enable/disable, fleet pool bindings, etc. do we need audit events for these or do we treat as internal ops?\n\nHanae Suzuki: +1, security side assumption has been: any action that changes (a) what model can serve traffic, (b) where traffic can go, or (c) who can access capacity => sensitive.\n\nMarcus Lin: yep. also anything that changes policy enforcement (eg bypassing allow-lists), even if done via runtime config.\n\nEthan Park: to clarify, “runtime-level” here = changes in serving layer config (model registry flags, pool allocations) vs control plane policy? where are these changes initiated today?\n\nDiego Santos: mix. some via control plane (rollouts/policy), but some are “runtime admin” endpoints (mostly used by oncall) for: \n- enable/disable model version in a region\n- bind/unbind model to a GPU pool (fleet pool)\n- adjust max_batch_tokens / concurrency caps\n- toggle a “degraded mode” flag when a model is flaky\n\nAllison Grant: from observability standards POV, anything done through an “admin endpoint” should emit an audit event unless we explicitly waive it. otherwise we’ll have blind spots during incidents.\n\nJordan Lee: + for routing-related: if runtime config can change routing weights / fallback order, that’s definitely sensitive. even if it’s “just” to fix latency.\n\nDiego Santos: what about pure performance knobs like continuous batching parameters? those can impact cost/latency but not access control.\n\nHanae Suzuki: still can be sensitive if it materially changes customer experience or could be abused to DoS. but we can scope it as “sensitive: availability/controls” vs “non-sensitive: tuning” if we have a clear line.\n\nMarcus Lin: line I’d propose: audit any “state change” that affects traffic eligibility or capacity allocation at tenant/project/model level. not every per-host tunable.\n\nLogan Wright: for coverage dashboards, we need a crisp list of expected event_types. otherwise runtime will be a constant “unknown unknowns” bucket.\n\nNadia Rahman: also cardinality note: don’t dump giant config blobs. if we log diffs, keep it bounded (eg allow-listed keys + before/after strings truncated).\n\nEthan Park: can we enumerate the runtime actions that are user-initiated vs automatic? eg autoscaler increasing replicas shouldn’t spam audit logs.\n\nDiego Santos: good call. categories:\n1) operator-initiated (oncall / console admin)\n2) system-initiated (autoscaler, health manager)\n\nDiego Santos: system-initiated shouldn’t be audit, but should be normal telemetry/events.\n\nHanae Suzuki: agree. audit logs are about accountability. still, system-initiated changes that affect customer traffic might be useful for investigations, but that’s more “operational event stream” than “audit”.\n\nAllison Grant: +1. audit events require actor semantics; “actor.type=service” is ok, but we should avoid turning every reconciliation loop into audit noise.\n\nMarcus Lin: but there are a couple “system actions” we might want to treat as audit-adjacent: if the system disables a model due to safety/abuse detection or flips a policy gate.\n\nJordan Lee: that’s more policy engine / trust & safety domain, but yes: if runtime auto-disables a model version due to repeated bad outputs, that’s a big deal.\n\nDiego Santos: ok, propose “sensitive runtime actions” list:\n- model version enable\n- model version disable\n- model version pin/unpin to traffic (ie “eligible_for_routing”)\n- fleet pool binding create/update/delete (model <-> pool)\n- manual capacity override on pool (eg reserved slots, min replicas) when it changes tenant-visible throughput\n- enabling/disabling “degraded mode” / “circuit breaker” for a model if it changes routing eligibility\n\nDiego Santos: NOT sensitive (telemetry only):\n- per-host kernel selection, compilation cache, warmup\n- autoscaler scale up/down\n- dynamic batcher tuning unless it changes a hard cap / quota\n\nHanae Suzuki: looks right. re: batching: if you change max_concurrency / max_inflight for a tenant, that can be used to throttle. I’d call those sensitive when scoped to tenant/project.\n\nDiego Santos: fair. we do have an endpoint to set “tenant concurrency cap override” during incidents.\n\nMarcus Lin: that’s absolutely sensitive. it’s basically a quota override.\n\nEthan Park: that might be owned by gateway/control plane though? but if runtime applies it directly, audit should still fire at the point of change.\n\nLogan Wright: for coverage: we’ll want event_type names that are stable and map to taxonomy. can runtime align with the registry PR (ENG-4822) rather than inventing new strings?\n\nAllison Grant: please. also include correlation fields: request_id/trace_id + admin_action_id so we can connect a console click -> control plane -> runtime.\n\nNadia Rahman: and include “deployment_mode” (hosted/dedicated/private) consistently. we’ve seen private missing these fields.\n\nDiego Santos: ok. I’ll sync with eng-security on naming. tentative event_types:\n- runtime.model_version.enabled\n- runtime.model_version.disabled\n- runtime.model_version.routing_eligibility.updated\n- runtime.fleet_pool_binding.created\n- runtime.fleet_pool_binding.deleted\n- runtime.tenant_concurrency_override.updated\n\nHanae Suzuki: + include target fields (model_id, model_version, pool_id, tenant_id/project_id). and for actor, distinguish human vs service.\n\nMarcus Lin: also: if there’s an “impersonation” flow (support acting as customer admin), include impersonator in actor chain.\n\nDiego Santos: ack. for oncall, actor will be the human via SSO -> admin API token. for private, operator identity is sometimes just “kube user” from kubectl; we’ll capture best-effort.\n\nAuditBot: Reminder: use schema_version in all new audit events and validate against audit-log-schema-v1. Unknown event_type strings will be rejected once ENG-4822 lints are enabled.\n\nJordan Lee: question: when policy engine pushes down a new routing policy to runtime, do we emit audit in policy engine only, or also in runtime?\n\nAllison Grant: prefer single source of truth for “who changed policy” (policy engine/control plane). runtime can emit operational “policy applied” events, but not duplicate audit unless there’s a separate manual override.\n\nMarcus Lin: +1. avoid dupes. runtime audit should focus on runtime-owned admin actions / overrides.\n\nDiego Santos: cool. action items:\n- I’ll write up the sensitive runtime actions list + ownership boundary (policy vs runtime) and drop in #eng-security for taxonomy review\n- Logan: once we have event_types, can you add to expected list for coverage dashboard?\n\nLogan Wright: yep, send the final strings + required fields and I’ll add to the “expected events” config.\n\nHanae Suzuki: please also note which ones must be “guaranteed” vs best-effort. eg enable/disable should probably be “must emit” even if sink is degraded.\n\nDiego Santos: agreed. enable/disable + pool binding + tenant cap overrides = must emit (buffer/retry). tuning-only stays best-effort/telemetry.\n\nEthan Park: 👍\n\nMarcus Lin: 👍 keep it tight and consistent with the schema. thanks.","occurredAt":1737670842000,"entityType":"thread","routeKey":"apps/slack/channels/eng-runtime","sourcePath":"sources/slack/eng-runtime/1737665440.json","peopleIds":["allison-grant","diego-santos","ethan-park","hanae-suzuki","jordan-lee","logan-wright","marcus-lin","nadia-rahman"],"links":[],"tags":["eng-runtime"]},{"id":"slack:1737601104","app":"slack","title":"#eng-runtime: 1737601104","preview":"Nadia Rahman: heads up: cache hit rate signal in metering looks noisy/possibly wrong. In the unit econ views it swings +/-20pp day to day for the same model+region, but latency + GPU-seconds don’t move. Before we bake this into the dashboard “cost drivers” section, can we sanity check what we’re actually measuring?\n\nCaleb Johnson: +1. I pulled yesterday’s hourly aggregates and the cache_hit_rate dropped to ~0.12 for `llama-70b` in us-east for ~3 hours, but request volume + output tokens were normal. That feels like instrumentation drop vs real cache behavior.\n\nLogan Wright: what’s the exact definition we’re using right now? is it (cache_hits / (cache_hits+cache_misses)) at request-level? or token-level? I’ve seen both definitions float around.\n\nNadia Rahman: current pipeline assumes token-level: `cached_prefill_tokens / total_prefill_tokens` (from runtime stats). But I’m not confident the runtime is emitting `cached_prefill_tokens` consistently across gateways/streaming/tool calls.\n\nAditya Rao: from Optimize side: we want something that correlates with $/token, not just “hit rate”. If we can estimate “compute avoided” (GPU-seconds saved) or “tokens avoided”, that’s more actionable.\n\nJada Williams: runtime details: we have two different “caches” people conflate:\n- prefix cache (prompt/prefill reuse) => should reduce prefill compute\n- KV reuse within a session / continuation => shows up differently (and can be near-100% for some chat patterns)\nOur metric today is basically prefix cache only, but the naming is ambiguous.\n\nAllison Grant: also: we’re sampling runtime metrics in a couple places. If the cache counters are in a different telemetry stream than token counters, you can get temporary skew (dropouts look like “hit rate fell off a cliff”).\n\nSean Gallagher: +1. We saw similar with other runtime counters last quarter. If the numerator is missing but denominator isn’t, you’ll alarm on “hit rate regression” when it’s just missing fields.\n\nMei Lin: I can confirm in the warehouse: in those 3 hours `cached_prefill_tokens` is NULL for a bunch of rows (by route), but `prefill_tokens` is populated. Our view is coercing NULL to 0 => hit rate tanks.\n\nCaleb Johnson: ah, that explains the cliff. We should treat NULL as “unknown” and exclude from hit rate, or have a coverage indicator.\n\nDylan Brooks: agree. For exec/finance we should not surface a driver metric that can be “0 because missing data”. Need guardrails.\n\nNadia Rahman: ok, so action items:\n1) fix aggregation semantics: NULL => unknown; compute hit_rate only when both fields present; add `cache_signal_coverage`.\n2) clarify definitions in the metric dictionary: prefix cache vs session KV reuse.\nBut I still want to answer: how do we attribute cache “savings” to blended $/1M output tokens?\n\nAditya Rao: mental model: blended $/1M output tokens is essentially (cost / output_tokens). Cache reduces compute for a given output token volume. The best “savings” metric would be something like (gpu_seconds_saved / output_tokens) or (prefill_gpu_seconds_saved per output token).\n\nJada Williams: we can’t currently emit “gpu_seconds_saved” directly. But we can emit:\n- prefill_tokens_total\n- prefill_tokens_cached\n- prefill_ms_total (already have)\nThen approximate “ms avoided” as (prefill_ms_per_token_baseline * cached_tokens). Problem: baseline depends on model, seq len, quant, hw.\n\nQuentin Harris: baseline will vary by model version + quant profile. If we try to infer savings, we need to join on model_version + quantization_profile, otherwise it’s garbage.\n\nLogan Wright: for v1 dashboard, maybe don’t compute $ savings from cache. Instead show cache hit rate + prefill/decode ratio + batching utilization as *explanatory signals*, and keep cost metric separate.\n\nCaleb Johnson: but the PRD explicitly wants “contributing signals” to be interpretable. If cache hit rate goes up, people will assume cost should go down. We need guidance like: “cache hit rate affects prefill; if your workload is decode-dominant, hit rate won’t move blended cost much.”\n\nNadia Rahman: exactly. We can pair it with `prefill_to_total_compute_ratio` (or proxy). Is there an existing runtime metric for prefill vs decode time split?\n\nJada Williams: we have `prefill_ms` and `decode_ms` in the per-request stats, but they’re not reliably emitted on the streaming path (ironic given this thread). Also tool calling can break the accounting.\n\nAllison Grant: if we’re going to rely on these signals, we need:\n- explicit schema contract\n- “required fields” completeness checks\n- a way to detect silent regressions (canary)\nOtherwise the dashboard will become a blame magnet.\n\nSean Gallagher: +1. For oncall: simplest is alert on coverage dropping below X% for required dimensions/fields. Cache signals can be “best effort” but must be labeled as such.\n\nDylan Brooks: can we converge on what we’ll ship for v1?\nMy proposal:\n- Show cache hit rate as token-level prefix cache hit rate.\n- Show a “coverage badge” (% requests w/ cache counters present).\n- Add tooltip: only impacts prefill; cost sensitivity depends on prefill share.\n- Do NOT show “$ saved” until we have a defensible model.\n\nAditya Rao: works for me. Optimize can still use the signals to prioritize work, but we won’t over-claim.\n\nMei Lin: I’ll implement the NULL-handling + coverage metric in the view. We can define:\n`cache_counter_coverage = requests_with_cached_prefill_tokens / total_requests`\n(or token-weighted coverage if preferred).\n\nLogan Wright: request-weighted is fine for coverage as long as we’re consistent. Token-weighted might hide missing on high-volume short requests.\n\nNadia Rahman: on runtime side, can we fix the streaming emission? It sounds like `cached_prefill_tokens` + `prefill_ms/decode_ms` aren’t always present.\n\nJada Williams: I can take a look. I suspect the streaming code path bypasses the “finalize stats” block when the client disconnects early. That would drop cache counters and time split. If that’s true, we need to flush partial stats on stream close.\n\nCaleb Johnson: can we add a simple parity test like: for a fixed prompt that should be cacheable, run N requests and ensure cached_prefill_tokens increases on request 2+ across streaming and non-streaming.\n\nAllison Grant: and wire that into perf-canary. It’s exactly the kind of thing that regresses quietly.\n\nredwood-bot: Reminder: please file follow-ups in Linear under Unit Economics > Runtime Signals.\n\nNadia Rahman: filed:\n- runtime: “streaming path emits cache + prefill/decode counters consistently (incl early disconnect)”\n- analytics: “cache hit rate NULL semantics + coverage metric + dashboard tooltip copy”\n\nSean Gallagher: once coverage is in place, I’ll add an alert threshold like “cache_counter_coverage < 0.95 for 2 consecutive hours” to eng-runtime/eng-sre (non-paging at first).\n\nDylan Brooks: cool. Also please make sure the dashboard doesn’t let folks drill down to anything customer-identifying when looking at cache. Aggregation only.\n\nNadia Rahman: yep. This will be at model/region/tier only.\n\nAditya Rao: last ask: can we standardize naming? “prefix_cache_hit_rate” not “cache_hit_rate”. Otherwise people will assume it includes KV reuse and request-level caching.\n\nQuentin Harris: +1. Also include quant_profile dimension where available; cache behavior can change with context window defaults and batching.\n\nNadia Rahman: agreed. I’ll update the data dictionary draft + ping Alicia (design) for label/tooltip updates.","text":"Nadia Rahman: heads up: cache hit rate signal in metering looks noisy/possibly wrong. In the unit econ views it swings +/-20pp day to day for the same model+region, but latency + GPU-seconds don’t move. Before we bake this into the dashboard “cost drivers” section, can we sanity check what we’re actually measuring?\n\nCaleb Johnson: +1. I pulled yesterday’s hourly aggregates and the cache_hit_rate dropped to ~0.12 for `llama-70b` in us-east for ~3 hours, but request volume + output tokens were normal. That feels like instrumentation drop vs real cache behavior.\n\nLogan Wright: what’s the exact definition we’re using right now? is it (cache_hits / (cache_hits+cache_misses)) at request-level? or token-level? I’ve seen both definitions float around.\n\nNadia Rahman: current pipeline assumes token-level: `cached_prefill_tokens / total_prefill_tokens` (from runtime stats). But I’m not confident the runtime is emitting `cached_prefill_tokens` consistently across gateways/streaming/tool calls.\n\nAditya Rao: from Optimize side: we want something that correlates with $/token, not just “hit rate”. If we can estimate “compute avoided” (GPU-seconds saved) or “tokens avoided”, that’s more actionable.\n\nJada Williams: runtime details: we have two different “caches” people conflate:\n- prefix cache (prompt/prefill reuse) => should reduce prefill compute\n- KV reuse within a session / continuation => shows up differently (and can be near-100% for some chat patterns)\nOur metric today is basically prefix cache only, but the naming is ambiguous.\n\nAllison Grant: also: we’re sampling runtime metrics in a couple places. If the cache counters are in a different telemetry stream than token counters, you can get temporary skew (dropouts look like “hit rate fell off a cliff”).\n\nSean Gallagher: +1. We saw similar with other runtime counters last quarter. If the numerator is missing but denominator isn’t, you’ll alarm on “hit rate regression” when it’s just missing fields.\n\nMei Lin: I can confirm in the warehouse: in those 3 hours `cached_prefill_tokens` is NULL for a bunch of rows (by route), but `prefill_tokens` is populated. Our view is coercing NULL to 0 => hit rate tanks.\n\nCaleb Johnson: ah, that explains the cliff. We should treat NULL as “unknown” and exclude from hit rate, or have a coverage indicator.\n\nDylan Brooks: agree. For exec/finance we should not surface a driver metric that can be “0 because missing data”. Need guardrails.\n\nNadia Rahman: ok, so action items:\n1) fix aggregation semantics: NULL => unknown; compute hit_rate only when both fields present; add `cache_signal_coverage`.\n2) clarify definitions in the metric dictionary: prefix cache vs session KV reuse.\nBut I still want to answer: how do we attribute cache “savings” to blended $/1M output tokens?\n\nAditya Rao: mental model: blended $/1M output tokens is essentially (cost / output_tokens). Cache reduces compute for a given output token volume. The best “savings” metric would be something like (gpu_seconds_saved / output_tokens) or (prefill_gpu_seconds_saved per output token).\n\nJada Williams: we can’t currently emit “gpu_seconds_saved” directly. But we can emit:\n- prefill_tokens_total\n- prefill_tokens_cached\n- prefill_ms_total (already have)\nThen approximate “ms avoided” as (prefill_ms_per_token_baseline * cached_tokens). Problem: baseline depends on model, seq len, quant, hw.\n\nQuentin Harris: baseline will vary by model version + quant profile. If we try to infer savings, we need to join on model_version + quantization_profile, otherwise it’s garbage.\n\nLogan Wright: for v1 dashboard, maybe don’t compute $ savings from cache. Instead show cache hit rate + prefill/decode ratio + batching utilization as *explanatory signals*, and keep cost metric separate.\n\nCaleb Johnson: but the PRD explicitly wants “contributing signals” to be interpretable. If cache hit rate goes up, people will assume cost should go down. We need guidance like: “cache hit rate affects prefill; if your workload is decode-dominant, hit rate won’t move blended cost much.”\n\nNadia Rahman: exactly. We can pair it with `prefill_to_total_compute_ratio` (or proxy). Is there an existing runtime metric for prefill vs decode time split?\n\nJada Williams: we have `prefill_ms` and `decode_ms` in the per-request stats, but they’re not reliably emitted on the streaming path (ironic given this thread). Also tool calling can break the accounting.\n\nAllison Grant: if we’re going to rely on these signals, we need:\n- explicit schema contract\n- “required fields” completeness checks\n- a way to detect silent regressions (canary)\nOtherwise the dashboard will become a blame magnet.\n\nSean Gallagher: +1. For oncall: simplest is alert on coverage dropping below X% for required dimensions/fields. Cache signals can be “best effort” but must be labeled as such.\n\nDylan Brooks: can we converge on what we’ll ship for v1?\nMy proposal:\n- Show cache hit rate as token-level prefix cache hit rate.\n- Show a “coverage badge” (% requests w/ cache counters present).\n- Add tooltip: only impacts prefill; cost sensitivity depends on prefill share.\n- Do NOT show “$ saved” until we have a defensible model.\n\nAditya Rao: works for me. Optimize can still use the signals to prioritize work, but we won’t over-claim.\n\nMei Lin: I’ll implement the NULL-handling + coverage metric in the view. We can define:\n`cache_counter_coverage = requests_with_cached_prefill_tokens / total_requests`\n(or token-weighted coverage if preferred).\n\nLogan Wright: request-weighted is fine for coverage as long as we’re consistent. Token-weighted might hide missing on high-volume short requests.\n\nNadia Rahman: on runtime side, can we fix the streaming emission? It sounds like `cached_prefill_tokens` + `prefill_ms/decode_ms` aren’t always present.\n\nJada Williams: I can take a look. I suspect the streaming code path bypasses the “finalize stats” block when the client disconnects early. That would drop cache counters and time split. If that’s true, we need to flush partial stats on stream close.\n\nCaleb Johnson: can we add a simple parity test like: for a fixed prompt that should be cacheable, run N requests and ensure cached_prefill_tokens increases on request 2+ across streaming and non-streaming.\n\nAllison Grant: and wire that into perf-canary. It’s exactly the kind of thing that regresses quietly.\n\nredwood-bot: Reminder: please file follow-ups in Linear under Unit Economics > Runtime Signals.\n\nNadia Rahman: filed:\n- runtime: “streaming path emits cache + prefill/decode counters consistently (incl early disconnect)”\n- analytics: “cache hit rate NULL semantics + coverage metric + dashboard tooltip copy”\n\nSean Gallagher: once coverage is in place, I’ll add an alert threshold like “cache_counter_coverage < 0.95 for 2 consecutive hours” to eng-runtime/eng-sre (non-paging at first).\n\nDylan Brooks: cool. Also please make sure the dashboard doesn’t let folks drill down to anything customer-identifying when looking at cache. Aggregation only.\n\nNadia Rahman: yep. This will be at model/region/tier only.\n\nAditya Rao: last ask: can we standardize naming? “prefix_cache_hit_rate” not “cache_hit_rate”. Otherwise people will assume it includes KV reuse and request-level caching.\n\nQuentin Harris: +1. Also include quant_profile dimension where available; cache behavior can change with context window defaults and batching.\n\nNadia Rahman: agreed. I’ll update the data dictionary draft + ping Alicia (design) for label/tooltip updates.","occurredAt":1737606123000,"entityType":"thread","routeKey":"apps/slack/channels/eng-runtime","sourcePath":"sources/slack/eng-runtime/1737601104-cache-hit-rate-signal-quality.json","peopleIds":["aditya-rao","allison-grant","caleb-johnson","dylan-brooks","jada-williams","logan-wright","mei-lin","nadia-rahman","quentin-harris","sean-gallagher"],"links":[],"tags":["eng-runtime"]},{"id":"slack:1737369021","app":"slack","title":"#eng-runtime: 1737369021","preview":"Noah Patel: starting a thread on runtime side for safe apply/rollback. main questions: idempotency semantics, worker cache invalidation, and what we do under partial rollout / mixed state.\n\nOmar El-Sayed: +1. esp worried about KV/prefix cache. if config toggles caching/batching knobs, a rollback could leave workers with stale cache assumptions.\n\nSofia Ivanova: also the batching heuristics changes are stateful-ish (scheduler params). we need to be clear whether “apply v123” is best-effort vs guaranteed across all workers before controller moves on.\n\nBenji Okafor: from control plane side, rollout controller will treat runtime as eventually consistent but needs a strong signal: “applied checkpoint reached for shard X” + ability to retry safely.\n\nNoah Patel: proposal: runtime applier is idempotent per (scope, config_version). Think of it as:\n- input: `tenant_id`, `model`, `region`, `config_version_id`, `effective_config_hash`\n- worker stores `last_applied_version_id` + `last_applied_hash` (persisted locally + exported metric)\n- if same version/hash comes in again => no-op\n- if newer version => apply steps with checkpoints\n\nOmar El-Sayed: where do we store that on the worker? local disk? in-memory only isn’t safe under restarts.\n\nNoah Patel: lightweight local state file (e.g., `/var/lib/redwood/runtime/opt_cfg_state.json`) or in our existing worker metadata store. doesn’t have to be perfect, but we want to avoid thrash on rolling restarts.\n\nSean Gallagher: from SRE pov, idempotency also matters when control plane retries due to network flakes. we see a lot of duplicate “apply” calls in incidents.\n\nSofia Ivanova: what’s the “apply steps with checkpoints” look like? esp for batching heuristics.\n\nNoah Patel: rough sequence per worker:\n1) fetch config blob (by version) from config service via control plane cache\n2) validate minimum compatibility (runtime version, model arch constraints) — if fail, report `apply_status=FAILED_COMPAT`\n3) stage new config into “pending” slot (don’t switch traffic)\n4) drain in-flight batches for that model worker (bounded wait, e.g., 2s)\n5) switch scheduler params + caching toggles\n6) flush/namespace caches as needed\n7) mark applied + emit metric + log/audit correlation id\n\nOmar El-Sayed: cache invalidation: I’d prefer namespacing rather than full flush if possible. if config introduces new cache key format we can version the namespace by config_version.\n\nNoah Patel: yep. for prefix cache we can include `opt_cfg_epoch` in the key prefix. then rollback just uses previous epoch and ignores “new” entries. memory will still be used though.\n\nOmar El-Sayed: memory leak risk if we keep old epochs around. can we GC older epochs? maybe keep last 2 versions per (tenant, model)?\n\nNoah Patel: GC policy: maintain `active_epoch` and `previous_epoch`; anything else eligible for eviction. on rollback, controller sets version pointer back, so `previous_epoch` flips. we can keep a small LRU per epoch.\n\nAllison Grant: metrics tagging: please please tag runtime metrics with `config_version` (or epoch) so gates can see mixed states. if a canary is 10% workers on v124, we need that dimension.\n\nNoah Patel: agreed. we’ll emit:\n- `runtime_opt_cfg_applied{tenant,model,region,config_version}` gauge=1 on worker\n- request path: add `config_version` label on latency/error metrics (careful cardinality but config_version should be low)\n\nSean Gallagher: cardinality: tenant+model+region already big. adding version might explode if customers churn versions. any cap?\n\nAllison Grant: for gates we only need it during rollout window. we can do it as an annotation/event too, but we still need *some* way to separate canary vs baseline.\n\nNoah Patel: compromise: worker emits `runtime_opt_cfg_active_version` as an info metric (Prometheus “info” style) and request metrics get `opt_cfg_bucket` = {baseline,canary} during rollout. control plane defines which workers are canary.\n\nBenji Okafor: we can pass “rollout cohort id” to runtime with the apply call. then runtime can label request metrics with cohort rather than exact version.\n\nSofia Ivanova: but how does runtime know for a request which cohort it is? the worker itself is in a cohort, so ok.\n\nNoah Patel: yep, per-worker cohort label.\n\nOmar El-Sayed: partial rollout handling: what if we have mixed config within a single multi-GPU node? like 8 workers and only 2 are canary. fine, but for shared caches (if any) we must ensure isolation.\n\nNoah Patel: we don’t have truly shared caches across workers today (each worker has its own). except some node-level artifacts like compiled kernels cache. that one shouldn’t depend on opt cfg.\n\nOmar El-Sayed: except quantization profiles could change kernel selection / weights formats. that *does* touch compiled artifacts and weight blobs.\n\nNoah Patel: good call. for quant profiles we treat it as “model artifact variant” and load weights under a versioned path. we should not hot-swap quant weights in-place without a drain.\n\nSofia Ivanova: so quant profile apply might require a full model reload, which is slow. how does canary controller treat that? timeouts?\n\nBenji Okafor: controller will support per-change “expected apply duration” and will pause if apply doesn’t converge within window. but runtime needs to report progress (DOWNLOADING, LOADING, WARMING).\n\nNoah Patel: we’ll add apply statuses:\n- PENDING\n- DOWNLOADING_ARTIFACTS\n- LOADING_MODEL\n- WARMING\n- APPLIED\n- FAILED (with reason)\n\nSean Gallagher: + include `attempt_id` and `request_id` from control plane in logs. makes debugging easier.\n\nNoah Patel: partial apply + rollback race: scenario:\n- rollout to 10% starts applying v124\n- gate fails, rollback to v123 triggered\n- some workers still mid-apply v124\nNeed deterministic outcome: rollback wins.\n\nConnor O'Brien: release eng note: please define “rollback wins” semantics precisely; we’ll need it in release checklist and gameday.\n\nNoah Patel: semantics: each apply/rollback call carries a monotonically increasing `desired_generation` per scope. worker only applies commands with generation >= current. if rollback is newer generation, any in-progress apply should abort at checkpoint boundary.\n\nOmar El-Sayed: abort mid-flight is tricky. if we’re in the middle of reloading model weights, we can’t partially revert. safest is: finish to a safe point then apply rollback.\n\nNoah Patel: right: we don’t hard-kill. we set `abort_requested=true` and at the next checkpoint we stop advancing and transition to “needs_reconcile”; then reconcile loop drives toward newest desired generation.\n\nSofia Ivanova: what are checkpoints for batching/caching changes? those can be hot-swapped quickly. quant reload is the long one.\n\nNoah Patel: checkpoint boundaries:\n- before switching scheduler params\n- before swapping cache epoch\n- before starting model reload\n- after model reload\nSo rollback could prevent starting reload if it arrives early.\n\nBenji Okafor: controller side will avoid sending conflicting commands too frequently, but we can’t rely on that.\n\nSean Gallagher: also need a safeguard against thrashing: if someone repeatedly applies/rollbacks, workers will churn and SLOs go sideways.\n\nNoah Patel: we can add a per-scope cooldown on runtime side (e.g., don’t start another expensive transition within 60s unless `force=true` from oncall). control plane likely also has a cooldown.\n\nAllison Grant: gating signals during partial rollout: we need to know if errors/latency are coming from canary only. cohort tag solves.\n\nOmar El-Sayed: cache invalidation on rollback: if we’re using epoch-based keys, rollback just flips epoch. but if config toggles caching off entirely, should we flush? otherwise memory stays high.\n\nNoah Patel: if caching disabled, we can stop reads/writes but optionally schedule background eviction. no need for synchronous flush.\n\nSofia Ivanova: batching heuristics rollback: if we change max batch size or queue timeouts, rollback might increase latency briefly during the switch.\n\nNoah Patel: we’ll drain in-flight then switch; should be low impact but we should measure. canary gating should allow a short “settle window” after apply before evaluating p95/p99.\n\nBenji Okafor: ADR draft for gates includes settle time + minimum sample size. good.\n\nConnor O'Brien: timeline wise, can you get the generation/checkpoint protocol into the next runtime train? we’re trying to align with console rollout.\n\nNoah Patel: yes if we keep it minimal: generation integer per scope, status reporting, epoch-based cache key. quant reload statuses may come in follow-up.\n\nDeployBot: buildkite #runtime-main: `redwood/runtime` build 48219 started (triggered by Noah Patel)\n\nOmar El-Sayed: can you paste a pseudo-API for the worker apply endpoint? helps me reason about what data we need.\n\nNoah Patel: sure, sketch:\n```json\n{\n  \"scope\": {\"tenant\":\"t_123\",\"model\":\"llama3_70b\",\"region\":\"us-east-1\"},\n  \"desired_generation\": 91,\n  \"config_version_id\": \"ocv_124\",\n  \"config_hash\": \"sha256:...\",\n  \"cohort\": \"canary\",\n  \"flags\": {\"force\": false}\n}\n```\nResponse:\n```json\n{\n  \"observed_generation\": 91,\n  \"status\": \"APPLIED\",\n  \"applied_version_id\": \"ocv_124\",\n  \"reason\": \"\",\n  \"checkpoint\": \"DONE\"\n}\n```\n\nSean Gallagher: looks good. please ensure reasons are stable enums; we’ll page on some of them.\n\nAllison Grant: and log it structured. we’ll want to join by `desired_generation` in dashboards.\n\nSofia Ivanova: last q: do we need a “mixed state” indicator at service level? control plane might think rollout is 10% but runtime actually 7% if some workers stuck.\n\nNoah Patel: control plane should compute observed % from worker reports (applied_version metric / status). runtime just reports accurately. we’ll expose `apply_lag_seconds` too.\n\nBenji Okafor: cool. I’ll sync this into ENG-4823/4824 comments. thanks all.\n\nNoah Patel: I’ll write up a short design note for runtime: generation protocol + cache epoching + reconcile loop. will @ here for review.","text":"Noah Patel: starting a thread on runtime side for safe apply/rollback. main questions: idempotency semantics, worker cache invalidation, and what we do under partial rollout / mixed state.\n\nOmar El-Sayed: +1. esp worried about KV/prefix cache. if config toggles caching/batching knobs, a rollback could leave workers with stale cache assumptions.\n\nSofia Ivanova: also the batching heuristics changes are stateful-ish (scheduler params). we need to be clear whether “apply v123” is best-effort vs guaranteed across all workers before controller moves on.\n\nBenji Okafor: from control plane side, rollout controller will treat runtime as eventually consistent but needs a strong signal: “applied checkpoint reached for shard X” + ability to retry safely.\n\nNoah Patel: proposal: runtime applier is idempotent per (scope, config_version). Think of it as:\n- input: `tenant_id`, `model`, `region`, `config_version_id`, `effective_config_hash`\n- worker stores `last_applied_version_id` + `last_applied_hash` (persisted locally + exported metric)\n- if same version/hash comes in again => no-op\n- if newer version => apply steps with checkpoints\n\nOmar El-Sayed: where do we store that on the worker? local disk? in-memory only isn’t safe under restarts.\n\nNoah Patel: lightweight local state file (e.g., `/var/lib/redwood/runtime/opt_cfg_state.json`) or in our existing worker metadata store. doesn’t have to be perfect, but we want to avoid thrash on rolling restarts.\n\nSean Gallagher: from SRE pov, idempotency also matters when control plane retries due to network flakes. we see a lot of duplicate “apply” calls in incidents.\n\nSofia Ivanova: what’s the “apply steps with checkpoints” look like? esp for batching heuristics.\n\nNoah Patel: rough sequence per worker:\n1) fetch config blob (by version) from config service via control plane cache\n2) validate minimum compatibility (runtime version, model arch constraints) — if fail, report `apply_status=FAILED_COMPAT`\n3) stage new config into “pending” slot (don’t switch traffic)\n4) drain in-flight batches for that model worker (bounded wait, e.g., 2s)\n5) switch scheduler params + caching toggles\n6) flush/namespace caches as needed\n7) mark applied + emit metric + log/audit correlation id\n\nOmar El-Sayed: cache invalidation: I’d prefer namespacing rather than full flush if possible. if config introduces new cache key format we can version the namespace by config_version.\n\nNoah Patel: yep. for prefix cache we can include `opt_cfg_epoch` in the key prefix. then rollback just uses previous epoch and ignores “new” entries. memory will still be used though.\n\nOmar El-Sayed: memory leak risk if we keep old epochs around. can we GC older epochs? maybe keep last 2 versions per (tenant, model)?\n\nNoah Patel: GC policy: maintain `active_epoch` and `previous_epoch`; anything else eligible for eviction. on rollback, controller sets version pointer back, so `previous_epoch` flips. we can keep a small LRU per epoch.\n\nAllison Grant: metrics tagging: please please tag runtime metrics with `config_version` (or epoch) so gates can see mixed states. if a canary is 10% workers on v124, we need that dimension.\n\nNoah Patel: agreed. we’ll emit:\n- `runtime_opt_cfg_applied{tenant,model,region,config_version}` gauge=1 on worker\n- request path: add `config_version` label on latency/error metrics (careful cardinality but config_version should be low)\n\nSean Gallagher: cardinality: tenant+model+region already big. adding version might explode if customers churn versions. any cap?\n\nAllison Grant: for gates we only need it during rollout window. we can do it as an annotation/event too, but we still need *some* way to separate canary vs baseline.\n\nNoah Patel: compromise: worker emits `runtime_opt_cfg_active_version` as an info metric (Prometheus “info” style) and request metrics get `opt_cfg_bucket` = {baseline,canary} during rollout. control plane defines which workers are canary.\n\nBenji Okafor: we can pass “rollout cohort id” to runtime with the apply call. then runtime can label request metrics with cohort rather than exact version.\n\nSofia Ivanova: but how does runtime know for a request which cohort it is? the worker itself is in a cohort, so ok.\n\nNoah Patel: yep, per-worker cohort label.\n\nOmar El-Sayed: partial rollout handling: what if we have mixed config within a single multi-GPU node? like 8 workers and only 2 are canary. fine, but for shared caches (if any) we must ensure isolation.\n\nNoah Patel: we don’t have truly shared caches across workers today (each worker has its own). except some node-level artifacts like compiled kernels cache. that one shouldn’t depend on opt cfg.\n\nOmar El-Sayed: except quantization profiles could change kernel selection / weights formats. that *does* touch compiled artifacts and weight blobs.\n\nNoah Patel: good call. for quant profiles we treat it as “model artifact variant” and load weights under a versioned path. we should not hot-swap quant weights in-place without a drain.\n\nSofia Ivanova: so quant profile apply might require a full model reload, which is slow. how does canary controller treat that? timeouts?\n\nBenji Okafor: controller will support per-change “expected apply duration” and will pause if apply doesn’t converge within window. but runtime needs to report progress (DOWNLOADING, LOADING, WARMING).\n\nNoah Patel: we’ll add apply statuses:\n- PENDING\n- DOWNLOADING_ARTIFACTS\n- LOADING_MODEL\n- WARMING\n- APPLIED\n- FAILED (with reason)\n\nSean Gallagher: + include `attempt_id` and `request_id` from control plane in logs. makes debugging easier.\n\nNoah Patel: partial apply + rollback race: scenario:\n- rollout to 10% starts applying v124\n- gate fails, rollback to v123 triggered\n- some workers still mid-apply v124\nNeed deterministic outcome: rollback wins.\n\nConnor O'Brien: release eng note: please define “rollback wins” semantics precisely; we’ll need it in release checklist and gameday.\n\nNoah Patel: semantics: each apply/rollback call carries a monotonically increasing `desired_generation` per scope. worker only applies commands with generation >= current. if rollback is newer generation, any in-progress apply should abort at checkpoint boundary.\n\nOmar El-Sayed: abort mid-flight is tricky. if we’re in the middle of reloading model weights, we can’t partially revert. safest is: finish to a safe point then apply rollback.\n\nNoah Patel: right: we don’t hard-kill. we set `abort_requested=true` and at the next checkpoint we stop advancing and transition to “needs_reconcile”; then reconcile loop drives toward newest desired generation.\n\nSofia Ivanova: what are checkpoints for batching/caching changes? those can be hot-swapped quickly. quant reload is the long one.\n\nNoah Patel: checkpoint boundaries:\n- before switching scheduler params\n- before swapping cache epoch\n- before starting model reload\n- after model reload\nSo rollback could prevent starting reload if it arrives early.\n\nBenji Okafor: controller side will avoid sending conflicting commands too frequently, but we can’t rely on that.\n\nSean Gallagher: also need a safeguard against thrashing: if someone repeatedly applies/rollbacks, workers will churn and SLOs go sideways.\n\nNoah Patel: we can add a per-scope cooldown on runtime side (e.g., don’t start another expensive transition within 60s unless `force=true` from oncall). control plane likely also has a cooldown.\n\nAllison Grant: gating signals during partial rollout: we need to know if errors/latency are coming from canary only. cohort tag solves.\n\nOmar El-Sayed: cache invalidation on rollback: if we’re using epoch-based keys, rollback just flips epoch. but if config toggles caching off entirely, should we flush? otherwise memory stays high.\n\nNoah Patel: if caching disabled, we can stop reads/writes but optionally schedule background eviction. no need for synchronous flush.\n\nSofia Ivanova: batching heuristics rollback: if we change max batch size or queue timeouts, rollback might increase latency briefly during the switch.\n\nNoah Patel: we’ll drain in-flight then switch; should be low impact but we should measure. canary gating should allow a short “settle window” after apply before evaluating p95/p99.\n\nBenji Okafor: ADR draft for gates includes settle time + minimum sample size. good.\n\nConnor O'Brien: timeline wise, can you get the generation/checkpoint protocol into the next runtime train? we’re trying to align with console rollout.\n\nNoah Patel: yes if we keep it minimal: generation integer per scope, status reporting, epoch-based cache key. quant reload statuses may come in follow-up.\n\nDeployBot: buildkite #runtime-main: `redwood/runtime` build 48219 started (triggered by Noah Patel)\n\nOmar El-Sayed: can you paste a pseudo-API for the worker apply endpoint? helps me reason about what data we need.\n\nNoah Patel: sure, sketch:\n```json\n{\n  \"scope\": {\"tenant\":\"t_123\",\"model\":\"llama3_70b\",\"region\":\"us-east-1\"},\n  \"desired_generation\": 91,\n  \"config_version_id\": \"ocv_124\",\n  \"config_hash\": \"sha256:...\",\n  \"cohort\": \"canary\",\n  \"flags\": {\"force\": false}\n}\n```\nResponse:\n```json\n{\n  \"observed_generation\": 91,\n  \"status\": \"APPLIED\",\n  \"applied_version_id\": \"ocv_124\",\n  \"reason\": \"\",\n  \"checkpoint\": \"DONE\"\n}\n```\n\nSean Gallagher: looks good. please ensure reasons are stable enums; we’ll page on some of them.\n\nAllison Grant: and log it structured. we’ll want to join by `desired_generation` in dashboards.\n\nSofia Ivanova: last q: do we need a “mixed state” indicator at service level? control plane might think rollout is 10% but runtime actually 7% if some workers stuck.\n\nNoah Patel: control plane should compute observed % from worker reports (applied_version metric / status). runtime just reports accurately. we’ll expose `apply_lag_seconds` too.\n\nBenji Okafor: cool. I’ll sync this into ENG-4823/4824 comments. thanks all.\n\nNoah Patel: I’ll write up a short design note for runtime: generation protocol + cache epoching + reconcile loop. will @ here for review.","occurredAt":1737376844000,"entityType":"thread","routeKey":"apps/slack/channels/eng-runtime","sourcePath":"sources/slack/eng-runtime/1737369021.json","peopleIds":["allison-grant","benji-okafor","connor-o-brien","ethan-park","noah-patel","omar-el-sayed","sean-gallagher","sofia-ivanova"],"links":[],"tags":["eng-runtime"]},{"id":"slack:1732109876","app":"slack","title":"#eng-runtime: 1732109876","preview":"Ava Chen: Nightly perf job artifacts size spiked — nsys captures for the KV-heavy smoke runs are ~1.6GB each and uploads are timing out. Seen this on the new kvcache evictions tests. Anyone else?\nSamir Patel: Yup. We capture a lot (full cuda context + nvtx + memory). We should trim capture scope and only collect full nsys when the run looks anomalous. :eyes:\nLiam O'Neill: What exactly is CI running atm? can you paste the command?\nAva Chen: Current CI snippet (from circle job):\n```bash\nnsys profile --output ${ARTIFACT} --trace=cuda,nvtx --force-overwrite \n  --stats=true \n  --stop-on-exit \n  python run_kvcache_smoke.py --iters 100\n```\nSamir Patel: Ok. A few levers: 1) restrict trace range (e.g. --capture-range=nvtx) 2) disable heavy memory tracking 3) use nsys --compress or switch to perfetto for lightweight timeline. Also consider sampling only when kv miss rate > threshold.\nJulie Ramos: We tried compressing but the diff dashboard can't meaningfully diff compressed blobs. We need a strategy: keep small daily artifacts for diffs and keep one raw artifact per N nights for deep inspection.\nperf-ci-bot: perf-ci-bot: Proposal: store full nsys trace for 1/7 nightly runs (weekly baseline) and lightweight perfetto+kernel-timeline JSON for daily runs. Upload prefix: s3://redwood-perf/nightly/1732109876/ (example).\nSamir Patel: Also noticed kernel-selection noise — tiny ordering differences in kernels generate huge binary diffs. Suggest we canonicalize kernel timeline before computing diffs: group by NVTX op id and sort kernels by start time within op, then normalize timestamps to op-relative.\nAva Chen: Can we make the sampler only capture full nsys when KV cache miss ratio > 5%? That would focus storage on problematic runs.\nLiam O'Neill: We already emit metrics.csv from runs with fields: total_requests, cache_hits, cache_misses. We can compute kv_miss_ratio = cache_misses / total_requests and gate capture. Proposed change to CI sampler: if kv_miss_ratio > ${THRESHOLD} -> full nsys, else perfetto small trace.\nLiam O'Neill: Pseudocode for tools/trace_sampler.py:\n```python\nmetrics = read_csv('metrics.csv')\nmiss_ratio = metrics['cache_misses'] / metrics['total_requests']\nif miss_ratio > threshold:\n    run('nsys profile --output full.ns --trace=cuda,nvtx --compress')\nelse:\n    run('python -m perfetto_cli --out lightweight.pb --timeline')\n```\nSamir Patel: Additions: (a) autosplit large nsys artifacts with --max-size flag or do on-the-fly compression, (b) multipart S3 upload + retries in uploader to avoid timeouts.\nJulie Ramos: Retention proposal: full raw nsys -> 30 days, lightweight perfetto + normalized JSON -> 90 days, diff artifacts -> 365 days. Update pruning job accordingly.\nAva Chen: Can someone own dashboard ingest changes? need to parse both nsys and perfetto, and apply kernel timeline normalization before diffing. Dashboard prototype: http://dashboards.internal/perf/kvtrace (private).\nLiam O'Neill: I'll take dashboard parsing + kernel normalization. Will add a normalization step that maps kernels to op NVTX ids and produces a stable JSON for diffs.\nperf-ci-bot: perf-ci-bot: Created tasks: PERF-4321 trace_sampler threshold & compression, PERF-4322 dashboard parser + normalization, PERF-4323 retention & pruning job update.\nJulie Ramos: One more: add an NVTX annotation hook in our test harness so traces contain consistent op labels (module.function:op) — helps the grouping.\nAva Chen: Great plan. I'll open the PR for trace_sampler with a canary rollout to nightly for 3 runs, then flip on the sampler globally if stable. Thanks all. :thumbsup:","text":"Ava Chen: Nightly perf job artifacts size spiked — nsys captures for the KV-heavy smoke runs are ~1.6GB each and uploads are timing out. Seen this on the new kvcache evictions tests. Anyone else?\nSamir Patel: Yup. We capture a lot (full cuda context + nvtx + memory). We should trim capture scope and only collect full nsys when the run looks anomalous. :eyes:\nLiam O'Neill: What exactly is CI running atm? can you paste the command?\nAva Chen: Current CI snippet (from circle job):\n```bash\nnsys profile --output ${ARTIFACT} --trace=cuda,nvtx --force-overwrite \n  --stats=true \n  --stop-on-exit \n  python run_kvcache_smoke.py --iters 100\n```\nSamir Patel: Ok. A few levers: 1) restrict trace range (e.g. --capture-range=nvtx) 2) disable heavy memory tracking 3) use nsys --compress or switch to perfetto for lightweight timeline. Also consider sampling only when kv miss rate > threshold.\nJulie Ramos: We tried compressing but the diff dashboard can't meaningfully diff compressed blobs. We need a strategy: keep small daily artifacts for diffs and keep one raw artifact per N nights for deep inspection.\nperf-ci-bot: perf-ci-bot: Proposal: store full nsys trace for 1/7 nightly runs (weekly baseline) and lightweight perfetto+kernel-timeline JSON for daily runs. Upload prefix: s3://redwood-perf/nightly/1732109876/ (example).\nSamir Patel: Also noticed kernel-selection noise — tiny ordering differences in kernels generate huge binary diffs. Suggest we canonicalize kernel timeline before computing diffs: group by NVTX op id and sort kernels by start time within op, then normalize timestamps to op-relative.\nAva Chen: Can we make the sampler only capture full nsys when KV cache miss ratio > 5%? That would focus storage on problematic runs.\nLiam O'Neill: We already emit metrics.csv from runs with fields: total_requests, cache_hits, cache_misses. We can compute kv_miss_ratio = cache_misses / total_requests and gate capture. Proposed change to CI sampler: if kv_miss_ratio > ${THRESHOLD} -> full nsys, else perfetto small trace.\nLiam O'Neill: Pseudocode for tools/trace_sampler.py:\n```python\nmetrics = read_csv('metrics.csv')\nmiss_ratio = metrics['cache_misses'] / metrics['total_requests']\nif miss_ratio > threshold:\n    run('nsys profile --output full.ns --trace=cuda,nvtx --compress')\nelse:\n    run('python -m perfetto_cli --out lightweight.pb --timeline')\n```\nSamir Patel: Additions: (a) autosplit large nsys artifacts with --max-size flag or do on-the-fly compression, (b) multipart S3 upload + retries in uploader to avoid timeouts.\nJulie Ramos: Retention proposal: full raw nsys -> 30 days, lightweight perfetto + normalized JSON -> 90 days, diff artifacts -> 365 days. Update pruning job accordingly.\nAva Chen: Can someone own dashboard ingest changes? need to parse both nsys and perfetto, and apply kernel timeline normalization before diffing. Dashboard prototype: http://dashboards.internal/perf/kvtrace (private).\nLiam O'Neill: I'll take dashboard parsing + kernel normalization. Will add a normalization step that maps kernels to op NVTX ids and produces a stable JSON for diffs.\nperf-ci-bot: perf-ci-bot: Created tasks: PERF-4321 trace_sampler threshold & compression, PERF-4322 dashboard parser + normalization, PERF-4323 retention & pruning job update.\nJulie Ramos: One more: add an NVTX annotation hook in our test harness so traces contain consistent op labels (module.function:op) — helps the grouping.\nAva Chen: Great plan. I'll open the PR for trace_sampler with a canary rollout to nightly for 3 runs, then flip on the sampler globally if stable. Thanks all. :thumbsup:","occurredAt":1732113450000,"entityType":"thread","routeKey":"apps/slack/channels/eng-runtime","sourcePath":"sources/slack/eng-runtime/1732109876-kvcache-trace-sampler-policy.json","peopleIds":["ava-chen"],"links":[],"tags":["eng-runtime"]},{"id":"slack:1712105432","app":"slack","title":"#eng-runtime: 1712105432","preview":"Maria Chen: Heads-up — planning a gated rollout for kernel-quant v3.2 tonight in us-west (change window 02:00-03:00 PT). DeployBot will run preflight smoke + perf canary. Freeze engine non-essential merges at 00:30 PT. Please ack if you can watch metrics and own rollback if needed. Release notes draft: https://redwood.internal/releases/kernel-quant/v3.2 :memo:\n\nKevin Liu: ack. I'll monitor p50/p95, error-rate, and GPU temp dashboards. Posting links in a bit.\n\nDeployBot: Preflight queued: [smoke, kv-cache-sanity, perf-runner]. Expected checks ~10m. Will report back.\n\nJamie Ortiz: Quick question — what are our rollback thresholds? Proposing: p95 latency +30ms sustained for 5m OR error-rate >0.5% sustained for 5m OR kv-miss-rate spike >2x baseline. If any hit -> immediate roll to v3.1 pin.\n\nAkshay Rao: Adding metrics to the watchlist: kv-miss-rate, batching-latency, gpu-temp, warmstart-throttle, host-spare-capacity. Also watch model-exact-match on our small eval set (top-1 diffs).\n\nPriya Singh: I can own rollbacks tonight (on call 01:30-04:00 PT). I'll standby for canary->50% decision. Leave the rollback playbook to me if alerts fire.\n\nKevin Liu: Note: this build uses matmul-v2 kernels and quant profile B. If we see small-prompt inconsistency (embedding/eval regress), fallback to matmul-v1 shader should be allowed. Confirming auto-variant fallback is configured for region us-west.\n\nSRE-Bot: Incident runbook link: https://redwood.internal/runbooks/kernel-quant-rollback. Short checklist included: reproduce on staging -> pin traffic -> escalate to oncall -> hotpatch if needed.\n\nMaria Chen: Ops checklist (short):\n- freeze merges at 00:30 PT\n- run `./scripts/preflight.sh --canary` (done by DeployBot)\n- ensure perf-runner CI passed within last 2 hrs\n- pin TTL configured to 30m for emergency rollbacks\n\nMaria Chen: Release notes snippet to use in marketplace/console: \"kernel-quant v3.2 — improved matmul throughput, reduced memory copy for KV cache, updated quant profile B for lower variance at long sequences.\" Keep it brief.\n\nDeployBot: Staging smoke passed. Canary targets: initial 10 hosts, then 25%, then 50%, then full in 10m windows. Canary started.\n\nEthan Park: FYI saw an earlier perf-runner OOM during a stress harness on a staging host — attached short repro log: `oom-kernel-quant.txt`. I isolated the container and removed it from the staging canary.\n\nMaria Chen: Thanks Ethan — removed that instance from canary target group. Good catch.\n\nDeployBot: Canary rollout event: started rollout phase at ts 1712106000. Rolling: 10% -> 25% -> 50% with 10m interstitial waits.\n\nJamie Ortiz: Emergency pin command ready if warmstart regress shows up:```/deploybot pin region=us-west engine=kernel-quant v=3.1 ttl=30m -reason \"rollback warmstart regress\"```\n\nPriya Singh: Dashboards are up in #eng-runtime: links to kv-miss, p95, error-rate. I'll post annotated baseline numbers for quick eyeball.\n\nKevin Liu: Also adding a quick curl check for canaries: ```curl -sS -H \"Authorization: Bearer $CANARY_TOKEN\" https://canary.us-west.redwood/api/v1/health | jq .latency_ms``` — drop me any anomalies.\n\nMaria Chen: If we have no blocking signals by 1712106600 we'll proceed to full rollout. Please react :thumbsup: if you saw this and are good to go.\n\nDeployBot: Canary completed 50% stage — no auto-failures detected in the automated checks. Next window scheduled.\n\nPriya Singh: Oncall note: I'll escalate to SRE if we see any error-rate spikes >0.2% in the next 20m.\n\nMaria Chen: Thanks everyone. If anything urgent ping @Priya or @Jamie directly. Will post final rollout completion once done. :rocket:","text":"Maria Chen: Heads-up — planning a gated rollout for kernel-quant v3.2 tonight in us-west (change window 02:00-03:00 PT). DeployBot will run preflight smoke + perf canary. Freeze engine non-essential merges at 00:30 PT. Please ack if you can watch metrics and own rollback if needed. Release notes draft: https://redwood.internal/releases/kernel-quant/v3.2 :memo:\n\nKevin Liu: ack. I'll monitor p50/p95, error-rate, and GPU temp dashboards. Posting links in a bit.\n\nDeployBot: Preflight queued: [smoke, kv-cache-sanity, perf-runner]. Expected checks ~10m. Will report back.\n\nJamie Ortiz: Quick question — what are our rollback thresholds? Proposing: p95 latency +30ms sustained for 5m OR error-rate >0.5% sustained for 5m OR kv-miss-rate spike >2x baseline. If any hit -> immediate roll to v3.1 pin.\n\nAkshay Rao: Adding metrics to the watchlist: kv-miss-rate, batching-latency, gpu-temp, warmstart-throttle, host-spare-capacity. Also watch model-exact-match on our small eval set (top-1 diffs).\n\nPriya Singh: I can own rollbacks tonight (on call 01:30-04:00 PT). I'll standby for canary->50% decision. Leave the rollback playbook to me if alerts fire.\n\nKevin Liu: Note: this build uses matmul-v2 kernels and quant profile B. If we see small-prompt inconsistency (embedding/eval regress), fallback to matmul-v1 shader should be allowed. Confirming auto-variant fallback is configured for region us-west.\n\nSRE-Bot: Incident runbook link: https://redwood.internal/runbooks/kernel-quant-rollback. Short checklist included: reproduce on staging -> pin traffic -> escalate to oncall -> hotpatch if needed.\n\nMaria Chen: Ops checklist (short):\n- freeze merges at 00:30 PT\n- run `./scripts/preflight.sh --canary` (done by DeployBot)\n- ensure perf-runner CI passed within last 2 hrs\n- pin TTL configured to 30m for emergency rollbacks\n\nMaria Chen: Release notes snippet to use in marketplace/console: \"kernel-quant v3.2 — improved matmul throughput, reduced memory copy for KV cache, updated quant profile B for lower variance at long sequences.\" Keep it brief.\n\nDeployBot: Staging smoke passed. Canary targets: initial 10 hosts, then 25%, then 50%, then full in 10m windows. Canary started.\n\nEthan Park: FYI saw an earlier perf-runner OOM during a stress harness on a staging host — attached short repro log: `oom-kernel-quant.txt`. I isolated the container and removed it from the staging canary.\n\nMaria Chen: Thanks Ethan — removed that instance from canary target group. Good catch.\n\nDeployBot: Canary rollout event: started rollout phase at ts 1712106000. Rolling: 10% -> 25% -> 50% with 10m interstitial waits.\n\nJamie Ortiz: Emergency pin command ready if warmstart regress shows up:```/deploybot pin region=us-west engine=kernel-quant v=3.1 ttl=30m -reason \"rollback warmstart regress\"```\n\nPriya Singh: Dashboards are up in #eng-runtime: links to kv-miss, p95, error-rate. I'll post annotated baseline numbers for quick eyeball.\n\nKevin Liu: Also adding a quick curl check for canaries: ```curl -sS -H \"Authorization: Bearer $CANARY_TOKEN\" https://canary.us-west.redwood/api/v1/health | jq .latency_ms``` — drop me any anomalies.\n\nMaria Chen: If we have no blocking signals by 1712106600 we'll proceed to full rollout. Please react :thumbsup: if you saw this and are good to go.\n\nDeployBot: Canary completed 50% stage — no auto-failures detected in the automated checks. Next window scheduled.\n\nPriya Singh: Oncall note: I'll escalate to SRE if we see any error-rate spikes >0.2% in the next 20m.\n\nMaria Chen: Thanks everyone. If anything urgent ping @Priya or @Jamie directly. Will post final rollout completion once done. :rocket:","occurredAt":1712106600000,"entityType":"thread","routeKey":"apps/slack/channels/eng-runtime","sourcePath":"sources/slack/eng-runtime/1712105432-engine-gated-quant-thermal-rollback-playbook.json","peopleIds":["ethan-park"],"links":[],"tags":["eng-runtime"]}]
