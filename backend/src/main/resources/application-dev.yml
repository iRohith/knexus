# ============================================================
# Knowledge Nexus — Development Profile
# ============================================================

spring:
  # --- DataSource ---
  datasource:
    url: jdbc:postgresql://localhost:5432/corpKRC
    username: postgres
    password: shruti
    driver-class-name: org.postgresql.Driver

  # --- JPA ---
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

  # --- Kafka ---
  kafka:
    bootstrap-servers: localhost:9092

# --- JWT ---
jwt:
  secret: ${JWT_SECRET:dev-secret-key-that-is-at-least-256-bits-long-for-hs256-signing}
  access-token-expiry-ms: 1800000     # 30 minutes (longer for dev convenience)
  refresh-token-expiry-ms: 604800000  # 7 days

# --- Cognee ---
cognee:
  base-url: https://rohith--cognee-api-serve.modal.run
  api-key: 2ad18e01c703848f01ba8176adb2887990a86b499f0eec43f845053e55693058
  connect-timeout-ms: 5000
  read-timeout-ms: 60000

# --- Logging ---
logging:
  level:
    com.corp_krc.backend: DEBUG
    org.hibernate.SQL: DEBUG
    org.hibernate.type.descriptor.sql.BasicBinder: TRACE
